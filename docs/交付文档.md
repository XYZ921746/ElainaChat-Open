# 📦 ElainaChat-Open 改造版 — 交付文档

> 基于 MIT 开源的 [ElainaChat-Open](https://github.com/shuixinggangzheng/ElainaChat-Open) 二次开发，新增 **Live2D 视频通话**、**视觉模型图片转述**、**设置分栏** 等能力。

---

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 项目名 | ElainaChat-Open（改造版） |
| 技术栈 | 纯前端单 HTML + Node 静态服务（零依赖） |
| 定位 | BYOK（自带 Key）AI 角色聊天 + Live2D 视频通话 |
| 运行方式 | `node web/serve.mjs`，端口 4173 |
| 平台 | Web + 安卓（Capacitor） |

## 2. 核心功能

### 2.1 聊天
- OpenAI 兼容 / Anthropic / Gemini / Ollama 多格式（BYOK）
- 角色卡、世界观、分层提示词、记忆整理、图片输入
- TTS 多服务商：MiniMax / 阿里 Qwen-TTS / 豆包 / OpenAI / 浏览器
- ASR：浏览器识别 / 阿里百炼

### 2.2 🎥 Live2D 视频通话（新增）
- 顶部栏「📹 视频通话」按钮 → 全屏通话界面
- **模型外挂**：应用内上传 zip（`web/live2d/models/`，gitignore 不入库）
- 模型选择下拉 + 删除
- **模型大小/位置调节（通话界面调整框）**：底部「📐 调整」按钮开关调整框——
  - 框贴合模型实际绘制范围（非画布留白），默认隐藏、每个模型独立记忆位置/大小
  - 角手柄等比缩放（Shift 自由拉伸）、边手柄单轴、框内拖动移动
  - −/＋ 按钮等比微调 ±10%
- **背景设置**（设置页：预设渐变 / 纯色 / 自定义颜色 / 图片）
- **AI 说话嘴部同步**（LipSync，嘴型幅度随情绪变化：激动张嘴大、平静/伤心小）
- **💧 水印开关（通用自动探测）**：若模型 exp 目录含"水印/watermark"开关表情（如悠小喵「水印开关」→ Param85=1、泠等），打开通话后按键盘 **1 / 0** 切换水印隐藏/显示，也可由 AI 用 `[操作:隐藏水印]` / `[操作:显示水印]` 触发（**通话界面没有水印按钮**），直接驱动参数、比 expression 播放可靠，每个模型记忆开关状态
- **🎤 说话（真实语音输入）**：底部「🎤 说话」按钮，点击开始录音（复用主应用 ASR：阿里百炼 / 小米 MiMo / 浏览器），再点结束发送；聆听时按钮变红脉冲
- **底部栏精简**：只保留 🎤 说话 / 📐 调整 / 📞 挂断（AI 声音静音开关与 −/＋ 大小按钮已移除；静音仍可由 AI `[操作:静音]` 标签触发，大小调整用 📐 调整框拖拽）
- **AI 状态驱动（标签协议，AI 自主决策）**：LLM 回复可用 `[情绪:开心|难过|生气|害羞|惊讶|委屈|思考|平静]`（自动映射表情+嘴型幅度）、`[表情:xxx]`、`[动作:xxx]`、`[位置:left|center|right|上|下]`、`[大小:大|小|特大|特小|65%]`、`[背景:深蓝|星空|…]`、`[操作:打开视频通话|结束通话|整理记忆|静音|取消静音]` 标签驱动模型表现与应用功能；无标签时按触发词/emoji 自动推断情绪；标签均不显示给用户
- **跨模型自适应（AI Agent 决策）**：模型列表接口返回每个模型的 exps/motions（.exp3/.motion3 文件名）；系统提示词**动态注入当前模型可用表情/动作列表**，由 AI 根据情景直接选用真实文件名（`[表情:哭哭]` 等），换任何 Live2D 模型都自动适配；语义键（`[表情:happy]`）与情绪词（`[情绪:开心]`）经 语义映射 → 精确 → 子串/同义词组 三级解析兜底；表情参数用 `setParameterValueById` 直接驱动（比库的 expression() 可靠）
- **⏰ AI 定时任务（未来消息）**：LLM 可用 `[任务:每天 09:00 提醒我喝水]` / `[任务:明天 14:00 …]` / `[任务:每3小时 …]` / `[任务:每周五 18:00 …]` / `[任务:30分钟后 …]` 创建定时任务；调度器每 30s 检查，到点 AI 以角色身份自动发消息（插入当前对话 + 可选浏览器通知），支持周期/一次性；设置 → 高级可查看和取消；系统提示词已注入标签指南引导模型自然使用
- **🤖 AI Agent 权限分级（设置 → 高级）**：`[操作:列出文件 路径]` / `[操作:查看文件 路径]` / `[操作:保存文件 路径|内容]` 文件操作按权限模式受限：**🔒 限制模式**（默认）仅可读写本应用文件夹（web/），**💻 允许操作电脑**模式可读写任意路径（经本地服务执行，风险自担）；操作结果以「【AI 操作结果】」消息回填对话作为上下文；应用内操作（视频通话/记忆/水印/背景/静音）两种模式都可用
- **LLM 表情/动作驱动**：回复含 `[表情:xxx]` / `[动作:xxx]` 标签 → 模型自动做表情/动作（标签不显示）
- 麦克风开关（关 = 模型闭嘴 + AI 静音）

### 2.3 👁️ 视觉模型图片转述（新增）
- 设置 → 视觉：配一个 OpenAI 兼容的视觉模型（如 Qwen-VL）
- 图片对话时：**视觉模型先看图转述成文字 → 交给主模型（如 DeepSeek）回答**
- 未配视觉模型时：图片直接发给当前模型（原行为）

### 2.4 设置分栏（新增）
设置面板改为 6 个 Tab：**角色 / 对话 / 语音 / 视觉 / Live2D / 高级**

## 3. 文件结构

```
ElainaChat-Open/
├── web/
│   ├── index.html            # 主应用（单文件，约 505KB）
│   ├── live2d-video.js       # Live2D 视频通话模块（新增）
│   ├── serve.mjs             # 静态服务 + Live2D 模型上传/删除 API（增强）
│   ├── diag-live2d.html      # Live2D 库诊断页（调试用）
│   ├── diag-asr.html         # 语音识别诊断页：检测浏览器环境 + 麦克风 + 实际识别测试（调试用）
│   ├── vendor/
│   │   ├── tailwind.js
│   │   ├── live2dcubismcore.min.js           # Live2D Cubism3/4 核心
│   │   ├── pixi-6.min.js                     # PIXI 6.5.10
│   │   └── pixi-live2d-display-cubism4.min.js # 纯 Cubism4 版（关键！）
│   └── live2d/models/        # 上传的模型（gitignore，不入库）
├── android-app/              # 安卓（Capacitor）
├── .gitignore
└── README.md
```

## 4. 运行

```bash
cd ElainaChat-Open
node web/serve.mjs
# 打开 http://localhost:4173
# 服务器默认监听 0.0.0.0：手机/平板可用 http://<本机局域网IP>:4173 访问
# （可用 HOST 环境变量覆盖，如 HOST=127.0.0.1 仅本机；PORT 可改端口）
```

## 5. 配置说明

### 5.1 对话模型（设置 → 对话）
- API 格式：openai / anthropic / gemini / ollama
- 填 BaseURL + Key + 模型名（BYOK，Key 存当前标签页 sessionStorage）

### 5.2 视觉模型（设置 → 视觉）
| 字段 | 说明 |
|---|---|
| API 地址 | OpenAI 兼容，如 `https://api.siliconflow.cn/v1` |
| API Key | 视觉模型 Key |
| 模型名 | 如 `Qwen/Qwen2.5-VL-72B-Instruct` |

### 5.3 Live2D（设置 → Live2D）
- **上传模型**：选 .zip（Live2D Cubism 3 模型，含 `.model3.json` + `.moc3` + 纹理）
- **模型选择**：下拉切换已上传模型（下拉显示友好名，值仍是目录名）
- **重命名模型**：点「✏️ 重命名」直接改模型的**文件夹名**（中文、空格都可以；重名自动加序号），每模型的缩放/水印设置会一起迁移，见 8.11
- **鼠标跟随**：开关 + **跟随幅度**（滑块 0-10 = ×0.0~×1.0；右侧数字框可填到 100 = ×10 解锁更高倍率，×1.0 以上由身体跟随补足），见 8.10
- **背景**：渐变/纯色/自定义/图片
- **大小**：通话界面 −/＋ 调节

### 5.4 语音（设置 → 语音）
- TTS：MiniMax / 阿里 Qwen-TTS / 豆包 / OpenAI
- ASR：浏览器 / 阿里百炼

## 6. 服务端 API（serve.mjs）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/live2d/upload` | POST | 上传模型 zip，解压到 `web/live2d/models/<目录名>/`；**目录名直接取 zip 里的文件夹名**（拿不到才退回 `model_<ts>_<rand>`），重名自动加 ` (2)` |
| `/api/live2d/models` | GET | 列出模型（`name`、`modelJson` 路径、exp 表情/动作列表、vtube.json） |
| `/api/live2d/models/<name>` | DELETE | 删除模型目录 |
| `/api/live2d/rename` | POST | 重命名模型：`{ name, displayName }` → **真改文件夹**，返回 `{ ok, name, newName }`（见 8.11） |
| 静态服务 | GET | 主页面 + vendor + 模型文件 |

> **模型目录名就是显示名**（上传时直接取 zip 里的文件夹名）。URL 与文件名都走 `encodeURIComponent` / `decodeURIComponent`，所以中文、空格、emoji 都能正常访问。整个 `models/` 已在 `.gitignore` 里，不会入库。

### zip 解压实现要点（已修复）
- **中文文件名**：支持 UTF-8 标志位 + GBK 兜底解码（Windows zip 常见 GBK）
- **data descriptor**：支持（PowerShell Compress-Archive 生成的 zip 带此标志）
- **目录名**：直接用 zip 里的文件夹名（中文、空格都可以；拿不到才退回 `model_<ts>_<rand>`），重名自动加 ` (2)` —— 见 8.11
- **防目录穿越**：路径白名单校验

## 7. Live2D 技术要点（踩坑记录）

### 7.1 库组合（关键！）
```
live2dcubismcore.min.js           # Cubism 3/4 核心（必须）
pixi-6.min.js                     # PIXI 6.5.10（必须 6.x）
pixi-live2d-display-cubism4.min.js # 纯 Cubism4 版（必须用这个！）
```

**⚠️ 不要用 `pixi-live2d-display` 的 `index.min.js`（混合版）**——它强制要求 Cubism 2 运行时（`live2d.min.js`），而 Cubism 2 核心很难获取（官方 CDN 404、npm 无），会导致 `PIXI.live2d` 挂载失败。

**正确文件来源**：`pixi-live2d-display@0.4.0` npm 包里的 `dist/cubism4.min.js`（纯 Cubism 4，不需要 Cubism 2）。

### 7.2 模型加载
```js
// 等待 cubism4 就绪
if (PIXI.live2d?.cubism4Ready) await PIXI.live2d.cubism4Ready;
const model = await PIXI.live2d.Live2DModel.from('/live2d/models/<name>/<model3.json>', { autoInteract: false });
```

### 7.3 嘴部同步
```js
model.internalModel?.coreModel?.setParameterValueById?.('ParamMouthOpenY', value);
// 模型 model3.json 需有 LipSync 组（悠小喵有）
```

### 7.4 表情/动作
- 表情：`model.expression('/live2d/models/<name>/exp/<表情>.exp3.json')`
- 动作：`model.motion('/live2d/models/<name>/exp/<动作>.motion3.json')`
- LLM 输出 `[表情:happy]` 等标签 → `drive()` 解析触发 → `stripTags()` 剥离

### 7.5 诊断页
`http://localhost:4173/diag-live2d.html` —— 检查三库加载、`PIXI.live2d` 是否挂载。

## 8. 已解决的问题（历史）

| 问题 | 解决方案 |
|---|---|
| 模型太大超出屏幕 | 按模型高度自适应（占屏幕 65%），底部居中 |
| 上传 zip 解压乱码/错乱 | 修复 UTF-8/GBK 编码 + data descriptor 解析 |
| 中文模型名路径问题 | 目录用英文 id，展示名保留中文 |
| `PIXI.live2d` 不存在 | 换用纯 Cubism4 版库（不用混合版） |
| 设置页每个 Tab 都显示角色卡 | Tab 用独立类 `active-panel`（避开 `.hidden` 动画类冲突） |
| 视觉模型设置位置突兀 | 移到独立「视觉」Tab |

### 8.1 上游同步（2026-08，origin/main 5888a75，PR#3 + PR#4）

合并上游最新修复到本地定制版（stash → fast-forward → pop，无冲突）：

| 上游修复 | 说明 |
|---|---|
| `replaceChildren` 兼容垫片 | Chromium < 86 无 `Element.prototype.replaceChildren`，发送图片会在 `consumePendingComposerImage` 抛 TypeError。脚本开头仅在缺失时注入垫片（`web/index.html` + `android-app/www/index.html`） |
| `inset` 长属性兜底 | 旧 WebView（Chromium < 87）不支持 `inset` 简写，fixed 浮层会飘到左上角。改为 `top/right/bottom/left` 长属性 + 新增 `.fixed.inset-0 { top:0; right:0; bottom:0; left:0 }` 兜底规则 |
| ASR 防重入修复 | 桌面 Chrome 连续 `start()` 报 "recognition has already started"。新增 `asrStarting` / `asrRecognitionActive` / `asrSessionId` 三态守卫：startListening 防重入、onend 重启校验会话 id、切回浏览器 ASR 前先 abort/stop 旧会话 |

**本地定制额外兜底**：`web/live2d-video.js` 内联样式中的 `#live2d-call-host`（fixed 全屏通话层）与 `.live2d-canvas-wrap` 同样改用长属性 `top/right/bottom/left`，避免旧 WebView 下视频通话界面飘左上角（上游补丁不覆盖此文件）。

> 注意：`android-app/www/index.html` 已同步上游修复，但**未包含** Live2D/视觉/设置分栏定制（维持前开发者"安卓端纯开源版"的约定）。如需同步定制版到安卓，运行 `node scripts/sync-web.mjs` 前请先确认 vendor 里三个 Live2D 库文件已复制到 `android-app/www/vendor/`。

### 8.2 Live2D 模型调整（通话界面调整框，最终方案）

原 −/＋ 按"高度占屏幕比例"缩放存在两个问题：加载后立即读 `model.height`（未就绪时为 0 → scale 异常）且无法调整比例/位置。曾尝试设置页实时预览 + 滑块方案，但用户更偏好通话界面直接拖拽，最终定为**通话界面调整框**（`web/live2d-video.js`）：

- **「📐 调整」按钮开关**：底部栏新增，点击显示/隐藏调整框（默认隐藏，按钮高亮表示开启）
- **调整框贴合模型实际绘制范围**（非画布留白），每帧跟随；角手柄等比缩放（Shift 自由拉伸）、边手柄单轴、框内拖动移动
- 存储为绝对 PIXI scale + 中心位置比例，**每个模型独立记忆**（`live2d.resize.<模型名>`），自动迁移旧 `live2d.resize` / `live2d.scale`；渲染高度钳制在屏幕高的 2%~600%
- −/＋ 按钮等比微调 ±10%（保留）

### 8.3 修复记录（2026-08 二轮）

| 问题 | 根因 | 修复 |
|---|---|---|
| 每个分栏都有角色设定卡 | 上游旧版遗留 JS `settingsContent.appendChild(characterSettingsSection)` 在页面加载时把角色卡**移出** `tab-character`，落到设置内容末尾；它不是 tab panel（无 display:none），任何 Tab 下都可见 | 删除该移动逻辑，角色卡保留在 `tab-character` 内 |
| 调整框与模型对不齐、模型比例变形 | **`live2d-canvas-wrap` 在 HTML 里是 class，代码却用 `getElementById` 获取 → 永远 null**：`resizeTo` 失效、手动 resize 被跳过，canvas 保持默认 800×600 被 CSS 拉伸（764×485），模型变形、框偏移 | ① 给 div 补上 `id="live2d-canvas-wrap"`；② 渲染循环每帧核对 canvas 内部分辨率与容器 CSS 尺寸，不一致立即 `renderer.resize` 并重算布局；③ 框定位乘 `canvas.clientWidth/canvas.width` 校正因子（双保险） |
| 模型水印隐藏（悠小喵/泠 等） | 模型作者在 VTube Studio 里绑定了数字键（悠小喵=1、泠=0）触发"水印开关"表情（`水印开关.exp3.json` → `Param85=1`），但 pixi-live2d-display 不解析 vtube.json，且其 `expression()` 播放 Add 参数不生效 | **通用水印开关**：`serve.mjs` 模型列表接口新增返回 `exps`/`vtube`；前端自动探测 exp 目录含"水印/watermark"的表情 → 读出参数 Id 与隐藏值 → 键盘 1/0 切换（也可由 AI 用 `[操作:隐藏水印]` 触发），**直接 `setParameterValueById` 驱动参数**（比 expression 可靠），按模型记忆开关状态 |
| Live2D 背景换色无反馈 | 设置页点击色块/自定义颜色/图片/清除背景后直接应用，无任何 UI 反馈，用户不确定是否成功 | 新增 toast 提示（"✅ 背景已应用"等）+ 色块选中高亮（`.l2d-bg-opt.selected` 描边），打开设置时同步当前背景的选中状态 |
| 无法局域网访问 | `serve.mjs` 默认监听 `127.0.0.1` | 默认改为监听 `0.0.0.0`，启动时打印本机局域网 IP 地址（如 `http://<你的局域网IP>:4173`），可用 `HOST`/`PORT` 环境变量覆盖 |
| 浏览器语音识别报错难懂（network / not-allowed 等） | 报错直接透出 Chrome 的英文错误码，用户不知道怎么办 | ① `friendlyAsrError()` 把错误码翻译成可操作提示（network→"需连接在线语音服务，建议阿里云"、not-allowed→"权限/环境限制，建议 localhost/https 或阿里云"、audio-capture/no-speech 等）；② `onerror` 降级扩展：network / service-not-allowed 与权限错误一样，配置了阿里云时自动切"录音+云端最终识别"不打断用户；③ `startListening` 检测非安全上下文（局域网 IP/手机 http 访问）直接友好提示 |
| 语音识别浏览器支持不明确 | Firefox 等无 SpeechRecognition API，提示含糊 | **明确仅支持 Chrome**：`isChromiumBrowser()` 检测（UA + userAgentData 双通道），非 Chromium 浏览器点击麦克风直接提示"语音识别仅支持 Chrome（含 Edge 等 Chromium 内核浏览器）"；Chromium 但 API 缺失提示"未启用/版本过旧，请用最新版 Chrome" |
| 语音报"recognition has already started" | Chrome 的竞态：abort/stop 后立即 start()（或 onend 后立即 start()）会抛该错误；更严重的场景是**旧实例卡死**（某次异常退出 onend 未触发，实例永久停留 running，abort 也救不回），之后每次 start 都失败 | 新增 `safeStartRecognition()` + **实例重建**：捕获 "already started" → `abort()` 重置 → **`rebuildBrowserRecognition()` 丢弃旧实例、`createBrowserRecognition()` 创建全新实例并重新绑定事件** → 300ms 后重试（最多 3 次）。新实例状态干净必定可启动；`onstart` 严格置位、`endListening` 提前清标志；重试耗尽才提示"引擎状态冲突，请稍候/刷新页面" |
| 浏览器语音识别反复不可用 | Chrome 语音识别受网络（依赖 Google 服务）与环境（需 https/localhost）双重限制，用户环境无法稳定使用 | **标注"暂不可用"**：设置 → 语音中"浏览器内置识别"加红色「（暂不可用）」标注，选中时显示警告提示改用阿里云 |
| 阿里云 ASR Key 无处配置/难验证 | ASR 的 Key 之前藏在 TTS 配置里（只有 TTS 选阿里千问才显示），且无法快速验证 Key 有效性 | **ASR 复用 TTS 的 DashScope Key**（阿里云百炼 Key 通用 TTS/ASR，一个 Key 即可）+ 语音输入区显示 Key 状态（✓ 已配置/未配置）+ 「测试」按钮（发 0.5s 静音调 `qwen3-asr-flash`，立即区分：✅ Key 有效 / 401 Key 无效 / 403·404 模型未开通）；接口地址与请求格式经实测正确（`multimodal-generation/generation` + `qwen3-asr-flash` + `X-DashScope-DataInspection: enable`） |
| 新增小米 MiMo ASR 语音识别 | 用户提供接口文档 | 语音输入新增「小米 MiMo」服务商：`https://api.xiaomimimo.com/v1/chat/completions`，OpenAI 兼容 `input_audio`（wav/mp3 data URL），模型 `mimo-v2.5-asr`，认证头 `api-key: $MIMO_API_KEY`（或 Bearer）；设置页可配 Key/接口地址/模型名 + 「测试 MiMo 识别」按钮；`canUseCloudFinalAsr`/转写分发支持 aliyun/mimo 双云端，浏览器不可用时自动降级录音+云端识别 |
| 阿里云 ASR 与 MiMo 配置体验不一致 | 阿里云 ASR 接口地址/模型名硬编码，无法像 MiMo 一样调整 | 阿里百炼同样开放**接口地址 + ASR 模型**配置（默认 `dashscope.aliyuncs.com/.../multimodal-generation/generation` + `qwen3-asr-flash`），Key 仍复用 TTS 的 DashScope Key；测试按钮按配置值请求 |
| 接入阿里云千问（Qwen）对话模型 | 用户提供 OpenAI 兼容示例（`compatible-mode/v1`） | API 格式新增**「阿里云千问 Qwen」预设**：默认地址 `https://dashscope.aliyuncs.com/compatible-mode/v1`、默认模型 `qwen3.8-max`（可改）；走 OpenAI 兼容请求；**对话 API Key 留空时自动复用 TTS/ASR 的 DashScope Key**（阿里云公用 Key）；placeholder 提示"留空则复用语音里已填的 DashScope Key" |
| 设置无"恢复默认"入口 | 用户改乱了配置后只能手动逐个改回 | 设置面板底部新增**「↺ 恢复默认设置」**：确认后对话/语音/视觉等恢复 `DEFAULT_SETTINGS` 并立即持久化；**API Key 全部保留**（安全存储不动）、角色卡不变（角色卡有独立恢复按钮）；表单填充逻辑抽取为 `fillSettingsForm()` 供打开设置/恢复后复用 |
| ASR 报错无详细日志（测试能通、实际报错） | `throwProviderResponseError` 只抛错不打日志，弹窗只显示通用文案，无法排查"测试 0.5s 静音成功、实际录音失败" | 完整日志链路：识别开始（录音时长/wav 大小/模型名）→ `[Provider] 接口返回错误 HTTP x`（含状态码+服务商返回原文+解析 message）→ `[ASR] 云端识别失败`（provider/code/status/message）；跳过原因（录音太短/能量过低带 stats）、返回空/无效文本、网络/超时/CORS 异常均有专门日志；弹窗 UPSTREAM_UNAVAILABLE 时附加服务商返回的具体错误 |
| 进入页面强制填 API Key 的引导 | 首次使用弹窗要求当场输入 DeepSeek Key 才能进入，不友好 | 改为**提示弹窗**：进入页面显示"欢迎使用独立开源版，对话/语音等能力需要先在「设置」中配置对应服务商的 API 密钥"，按钮「我知道了」直接关闭（不再校验 Key、不再显示输入框），点击遮罩也可关闭 |

> 均通过 headless Chrome（CDP）实测：canvas 内部尺寸 = CSS 尺寸（scaleFactor=1，无拉伸）、框与 `model.getBounds()` 误差为 0、拖拽等比/移动/开关/关闭清理全部正常、角色卡只存在于 tab-character、设置页已无预览区块；水印开关：探测到「水印开关」→ Param85 0→1、键盘 1/0 来回切换正常。

### 8.4 第三轮：参数"静默失效"专项修复（2026-09）

这一轮的核心发现：**Cubism 运行时对不存在的参数名不报错**。`setParameterValueById('不存在的参数', 1)` 会被静默接受，`getParameterValueById` 甚至能读回刚写入的值（运行时把未知 id 追加到了参数表末尾），但该参数没有任何绘制绑定，画面完全不变。结果是代码"看起来生效、实际零效果"，且不产生任何日志。同理，**参数存在于模型里 ≠ 被绑定到绘制**——作者可能留下未使用的标准参数。

因此新增了一条硬规则：**情绪参数一律先取模型真实参数表，再按语义槽解析成真实参数名；解析不到就跳过并告警，绝不硬写。**

#### 实测数据（悠小喵，168 个参数）

冻结模型（停 motionManager / eyeBlink / breath / physics / pose，漂移基线压到 0）后逐个设值做像素 diff：

| 参数 | 结果 |
| --- | --- |
| `ParamCheekPuff` / `ParamEyeLOpen` / `ParamEyeROpen` / `ParamMouthOpenY` / `ParamMouthForm` / `ParamEyeBallX` | ✅ 有画面效果（已绑定） |
| `ParamSmile` / `ParamEyeLSmile` / `ParamEyeRSmile` / `ParamCheek` | ❌ 存在但未绑定 |
| `ParamBrowL` / `ParamBrowR` / `ParamBrowLY` / `ParamBrowRY` / `ParamBrowLForm` / `ParamBrowRForm` | ❌ 全部未绑定（该模型没绑眉毛） |
| `ParamEyeLOpening` | ❌ 存在但未绑定（实际用的是 `ParamEyeLOpen`） |
| **`ParamAngleX` / `ParamAngleY`** | ❌ **存在但未绑定** |
| **`ParamAngleX1` / `ParamAngleY1` / `ParamAngleY2`** | ✅ **真正绑定的转头参数** |

#### 修复清单

| 问题 | 根因 | 修复 |
| --- | --- | --- |
| 情绪参数组完全无效（P0） | `EMOTION_PARAM_SETS` 写死 `ParamSmile`/`ParamCheek`/`ParamBrowL`/`ParamEyeLOpening` 等，悠小喵里一个都不存在，且静默不报错 | 新增参数解析层：`refreshModelParamIds()` 取模型真实参数表 → `resolveParamId(slot)` 按语义槽 + 候选名/关键字解析；解析不到跳过并 `console.warn`。`EMOTION_PARAM_SETS` 改为语义槽（`smile`/`cheek`/`browL`/`eyeL`/`mouth`…） |
| `surprised` / `calm` 是死代码（P1） | `driveEmotion` 用 `emo.exp`（表情文件键）去查参数组，两边词表不是一套：惊讶 exp 是 `dizzy` → 错套"晕"的参数组（半闭眼，语义相反）；平静 exp 是 `smile` → 查不到 → 什么都不加 | 改为按**情绪键**（`happy`/`sad`/`surprised`/`calm`…）查表；`smile` 槽候选顺序调整为 `ParamSmile → ParamMouthForm → ParamEyeLSmile/RSmile`（嘴型是"笑"最直接的可见线索，且该模型只绑了 `ParamMouthForm`） |
| **鼠标"头部追踪"其实一直没生效** | 只驱动 `ParamAngleX`/`ParamAngleY`，而该模型把转头绑在 `ParamAngleX1`/`ParamAngleY1` 上（标准名留空） | 新增 `headX`/`headY`/`eyeBallX`/`eyeBallY` 语义槽，按 `resolveParamIds()` 解析真实参数；`close()` 复位同样按解析结果来。**候选范围后来在 8.6 收窄**（排除 `ParamAngleY2`，它会推动整个模型） |
| 表情文件解析不到时静默 404 | `resolveExpressionFile` 匹配不到仍返回原名 → fetch 必然 404 → 回退 `model.expression()` 也失败 | 匹配不到时返回空串、复位上一个表情的参数并告警，不再发无效请求 |
| ASR 网络提示看不到 | network 分支先写 `statusText` 再调 `updateUI()`，被 `statusMap[voiceState]` 覆盖回"正在聆听..." | 调整顺序：先 `updateUI()` 再写提示 |
| `[操作:静音]` 等于没静音 | `aiVoiceMuted` 只停了一下嘴型，主应用 TTS 照常播放；且它引用的 `#live2d-mute-btn` 早已移除 | 导出 `isVoiceMuted()`；主应用在 `startVoicePlaybackOnce` 前检查，静音时跳过播放但**照常提交并保存文字回复**；`speakStart()` 也加了静音守卫 |
| 首次打开时自定义背景被盖住 | `applyBackground()` 在 `app` 创建**之前**调用，渲染器还是不透明底色，把 CSS 背景整个盖住 | 把 `applyBackground()` 移到 app 创建之后（重复调用幂等） |
| 关闭通话后残留音量 | `close()` 没有复位 `voiceEnergy`，下次通话嘴型一开始就是张着的 | `close()` 中补 `voiceEnergy = null` |
| 调整框坐标系混用 | `resizeCenter()` 返回 canvas 内部像素，而鼠标事件是 CSS 像素 | 统一乘 `canvasPixelScale()` 换算 |
| 过期文案 | 提示"点右上角 ⬆️ 上传 zip"，但通话界面早已没有上传按钮 | 改为「设置 → Live2D」 |

#### 验证方式（可复现）

> 关键方法论：**验证 Live2D 参数是否真的生效，必须先停掉 motionManager / eyeBlink / breath / physics / pose，把漂移基线压到 0 再做像素 diff**。否则待机动画本身会产生约 15000 像素的噪声，任何结论都不可信（曾据此误判"参数有效"）。

修复后实测：左右移动鼠标 → `ParamAngleX1` 从 −17.9 变到 +17.8，画面变化 5929 像素（修复前为 0）；`[情绪:平静]` → `ParamMouthForm = 0.3`，画面变化 9 像素（修复前为 0）。

### 8.5 局域网访问密码（新增）

服务默认监听 `0.0.0.0`（手机/平板可访问），但 Agent 文件接口没有鉴权、权限模式又由请求方自己传参决定 —— 不设访问控制时，同一局域网内任何设备都能打开应用、甚至读写本机文件。

规则：

- **本机（127.0.0.1）自动视为管理员**：免密进入，且可修改访问密码 —— 这也是忘记密码时的找回入口
- **局域网设备必须登录**：登录后种 HttpOnly Cookie 会话（7 天）
- 首次启动生成 12 位随机密码并在 cmd 窗口醒目打印；用户改过密码后只存 **scrypt 加盐哈希**，控制台不再显示明文
- 登录失败 5 次锁定 60 秒
- **CSRF 防护**：非 GET 请求校验 `Origin` 与 `Host` 同源，否则拒绝（防止任意网页在浏览器里悄悄改本机密码）
- **隐藏文件禁止静态访问**：`.local-auth.json` 返回 403，避免密码文件被直接下载
- 改密码后清空所有会话，局域网设备需重新登录
- 忘记密码：本机打开 `http://127.0.0.1:4173` → 设置 → 高级 → 访问密码；或 `node web/serve.mjs --reset-password` 重启

凭据文件 `web/.local-auth.json` 已加入 `.gitignore`。

实测（curl）：局域网访问首页 302→`/login`、未登录 API 401、错误密码 401、正确密码 200+Set-Cookie、局域网改密码 403、本机改密码 200、旧密码失效、改密码后旧会话失效、伪造 Origin 403、`.local-auth.json` 403。

界面侧也做了端到端验证（设置 → 高级 → 访问密码）：面板能识别本机管理员并显示当前初始密码；两次输入不一致、密码少于 6 位都会被拦下并给出提示；正常修改后弹窗确认、状态文案变为"已由你自行设置"、输入框清空；随后用 curl 确认**旧密码失效、界面里刚设的新密码可在局域网登录**，且磁盘上只留下 `salt` + `hash`，没有明文。

> ⚠️ 局域网是明文 HTTP，密码在传输中不加密，建议仅在家庭/可信局域网使用。

**登录页配色**：与应用内**实际渲染值**（pixso 主题，非 `:root` 里那套）保持一致——橄榄绿画布 `#a8b974`、白色面板（`26px` 圆角 + `#d9c7c0` 描边 + `0 26px 70px rgba(58,39,29,.24)` 投影）、米色输入框 `#f8f5f2`、绿色药丸按钮 `#a7bb6c`、棕色文字 `#4c3123`、橙色小标 `#ff7d4d`。样式参数直接对齐应用内的 `.modal-panel` / `.modal-input` / `.btn-primary`，并加了一个和欢迎弹窗同款的橙色 eyebrow 小标。

> 踩坑记录：一开始抄的是 `:root` 里的 `--color-primary: #ec4899` 那套粉紫变量，但应用在运行时会用 pixso 主题覆盖它们，实际渲染出来是暖米色 + 棕 + 绿。**要取配色必须读渲染后的 computed style，不能只看 CSS 变量定义。**

**控制台输出精简**：`启动.bat` 的横幅（Server / Local / LAN / Press Ctrl+C）与 `serve.mjs` 里的接口清单、诊断页地址等提示行全部移除，只保留必要信息：

```
  本机访问:   http://127.0.0.1:4173   （免密码）
  局域网访问: http://<你的局域网IP>:4173   （需要访问密码）

  访问密码: xxxxxxxxxxxx
  可在「设置 → 高级 → 访问密码」改成自己的，改完这里就不再显示。
```

### 8.6 鼠标跟随：修正"整体位移"并加开关（2026-09）

修好头部追踪后暴露出的新问题：鼠标一动，**整个模型会朝鼠标方向下沉前倾**，看着像扑过来，很突兀。

逐参数看变化区域后定位到元凶：

| 参数 | 变化像素 | 变化区域 |
| --- | --- | --- |
| `ParamAngleX1` | 6075 | 宽 128 × 高 417（局部） |
| `ParamAngleY1` | 4896 | 宽 132 × 高 417（局部） |
| **`ParamAngleY2`** | **29343** | **宽 158 × 高 525（连水印层一起动）** |

`ParamAngleY2` 并不是"低头/抬头"，而是把整个模型（含作者加的水印层）一起推移——单独设它就复现了"靠过来"的画面。

修复：`headX` / `headY` 的候选只保留 **标准名** 和 **1 号变体**，不再驱动 `2` 号变体（`ParamAngleX2` / `ParamAngleY2`）。带 `2` 的变体在不同模型里可能是整体位移或整体旋转，语义不可靠，宁可不驱动。

同时新增 **鼠标跟随开关**（设置 → Live2D → 鼠标跟随），可整体关闭；关闭时立即把角度参数复位，模型保持不动。状态存在 `localStorage` 的 `live2d.mouseFollow`。

修复后实测：`ParamAngleY2` 恒为 0；鼠标左右移动时 `ParamAngleX1` 从 −17.9 → +17.9，画面变化 5930 像素、变化区域高度从 519 降到 417（只剩局部）；关闭开关后移动鼠标，所有角度参数保持 0，模型完全不动。

### 8.7 标签解析：情绪标签会吞掉操作标签（重要修复）

`live2d-video.js` 的 `driveText()` 里原本是：

```js
const emo = t.match(/\[情绪[:：]\s*([^\]]+)\]/);
if (emo) {
  driveEmotion(emo[1].trim());
  return;   // ← 问题在这里
}
// 后面的 [位置] / [大小] / [背景] / [操作] 全部在这行下面
```

那个 `return` 的本意只是"有显式情绪就别再做自动推断"，但它把**后面所有标签一起跳过了**。而系统提示词明确要求 AI **每条回复都带一个情绪标签**（`LIVE2D_TAG_GUIDE`：「每条回复根据内容带 1 个情绪/表情标签」）。

后果：`[操作:…]` 标签（文件操作、打开/结束视频通话、整理记忆、静音、隐藏水印）**在实战中几乎永远不会被执行**；`[位置]` / `[大小]` / `[背景]` 同样失效。更糟的是 `stripTags()` 会把标签从显示文本里剥掉，用户看不到标签被忽略，AI 还可能顺着说"已经帮你打开了"——正好踩中提示词里明令禁止的"编造操作结果"。

> 补充：`[任务:…]` 不受影响，它由 `index.html` 的 `extractAndCreateTasks()` 独立解析。

**修复**：把"跳过自动推断"改成只用一个布尔标志控制，不再提前返回；顺带支持**一条回复里出现多个 `[操作:…]`**（例如同时"打开视频通话"+"隐藏水印"）；未知情绪词也不再整条跳过，而是回退到自动推断。

修复后实测（对 `agentActions` 打桩记录调用，7/7 通过）：

| 场景 | 结果 |
| --- | --- |
| `[情绪:开心]…[操作:打开视频通话]` | ✅ 操作不再被吞 |
| `[操作:打开视频通话][操作:整理记忆]` | ✅ 两个操作都执行 |
| `[情绪:难过][操作:列出文件 D:\笔记]` | ✅ 参数完整传递 |
| `[情绪:平静]`（只有情绪） | ✅ 不误触发操作 |
| `[情绪:开心][背景:星空]` | ✅ 背景生效 |
| 无情绪标签时 | ✅ 原路径未改坏 |
| `[情绪:莫名其妙]…`（未知情绪） | ✅ 回退推断且操作照常执行 |

### 8.8 鼠标跟随失效：关闭通话后再打开丢监听（2026-09）

**现象**：换了新模型后"无法跟随鼠标移动"，看起来像是模型不支持头部追踪。

**排查结论（先说结论：模型没问题，代码有 bug）**：

1. 新模型（`model_mtxxm2cp_mii7` / LSS）的 `ParamAngleX` / `ParamAngleY` **是真正绑定的**，且效果比悠小喵更强——左右各 ±20° 的变化量下，LSS 改变 28791 像素、悠小喵只改 4178 像素。
2. 应用驱动参数也正常：鼠标从最左到最右，`ParamAngleX` 从 −17.56 走到 +17.54。
3. 真正的问题是：**鼠标追踪的 `pointermove` 监听器只在 `buildUI()` 里挂载，而 `buildUI()` 只在页面首次打开通话时执行一次**。`close()` 会把监听器移除并把 `mouseHandler` 置空，于是：

   > 打开通话（正常）→ 关闭通话 → 再次打开通话 → 监听器永远不会重新挂载 → **鼠标跟随永久失效**，直到刷新页面。

   这正好命中"关掉通话去「设置 → Live2D」上传新模型，然后重新打开通话"这个最常见的操作路径，所以很容易被误判成"新模型不支持跟随"。

**修复**：把挂载逻辑抽成幂等的 `ensureMouseTracking()`，在每次 `open()` 时调用；顺带做了两处改进——

- 监听目标从 `#live2d-canvas-wrap` 换成整个通话层 `#live2d-stage`：鼠标移到顶栏 / 底栏附近时也能继续跟随；
- 拖拽调整框时（`dragState` 非空）跳过，避免一边调位置一边转头。

**顺带的体验修复**：鼠标跟随关闭时，通话界面会显示一条可点击的提示 **「🖱️ 鼠标跟随已关闭 · 点此开启」**（`#live2d-follow-hint`）。以前这个开关藏在「设置 → Live2D」里，关掉后模型完全不动，很容易被误认为模型坏了。

**实测（headless Chrome + 真实鼠标事件，非合成事件）**：

| 场景 | `ParamAngleX`（鼠标左 → 右） | 结果 |
| --- | --- | --- |
| 第 1 次打开通话（悠小喵） | −17.55 → +17.49 | ✅ |
| 关闭 → 再打开（修复前） | 0 → 0 | ❌ 复现 bug |
| 关闭 → 再打开（修复后） | −17.55 → +17.53 | ✅ |
| 再切换到 LSS（新模型） | −17.50 → +17.52 | ✅ |
| 鼠标跟随 = 关 | 0 → 0 | 预期行为（界面显示提示条） |

> ⚠️ 排查时的一个大坑：**「欢迎使用独立开源版」引导弹窗（`#announcementOverlay`）是全屏浮层，只要它还开着，真实鼠标事件就全被它吃掉**，`pointermove` 永远到不了通话层（合成事件 `dispatchEvent` 会绕过命中测试，所以用合成事件测会得到"正常"的假象）。用自动化测鼠标跟随时，必须先关掉弹窗、并且用真实鼠标事件。

### 附：新增参数绑定诊断页 `web/diag-param-bind.html`

排查上面这类"参数看起来生效、实际没反应"的问题，新增了一个诊断页（与 `diag-live2d.html` / `diag-asr.html` 同一套思路）：

- 逐个参数设值，对比**所有 drawable 的骨骼顶点坐标最大位移**，判定"已绑定 / 存在但未绑定"；
- 自动列出鼠标跟随依赖的语义槽（`headX` / `headY` / `eyeBallX` / `eyeBallY` / `bodyX` / `bodyY`）在该模型上的解析结果；
- 并给出每个参数的**量程**与「幅度 ×N 到顶」标注 —— 参数值会被 Cubism 按模型自身 min/max 静默截断，这列直接告诉你「设置 → Live2D → 跟随幅度」还能放大多少倍（见 8.10）；
- 比像素 diff 更快更稳，且**不需要冻结 motionManager / eyeBlink / breath / physics / pose**——因为它是直接对比顶点、绕过了待机动画的噪声。

用法：`node web/serve.mjs` → 打开 `http://127.0.0.1:4173/diag-param-bind.html` → 选模型 → 「开始扫描」。

> 判定阈值：最大顶点位移 > 0.01 视为已绑定。这个方法是当时定位 `ParamAngleX` / `ParamAngleY` 是否绑定、以及确认 `ParamAngleY2` 会推动整个模型时用的同款手段。

### 8.9 安全与稳定性加固（2026-09，一轮完整代码审计后的修复）

对 `web/serve.mjs`、`web/live2d-video.js`、`web/index.html` 做了一遍逐行审计，按严重程度修复。**每一项都做了回归验证**（服务端 39 项 + 前端 25 项，全部通过）。

#### A. 高危：三个可被外部利用的口子

| 问题 | 触发条件 | 修复 |
| --- | --- | --- |
| **zip 解压没有"解压后体积"上限** | `MAX_UPLOAD(200MB)` 只限制压缩包本身。实测 119.8 KB 的 zip（120MB 全零）→ 落盘 120.0 MB，**放大 1025×**，`inflateRawSync` 还会在内存里整块展开 | 新增 `MAX_EXTRACTED(512MB)` / `MAX_ENTRY_SIZE(256MB)`，`inflateRawSync(..., { maxOutputLength })` + 累加校验，超限直接拒绝 |
| **`/api/agent/read`、`/api/agent/ls` 是 GET，绕过 CSRF** | 第三方网页放 `<img src="…/api/agent/read?path=…&permission=computer">`，请求由**本机浏览器**发出 → 被判为"本机管理员" → `permission=computer`（**由请求方自己传参**）生效 → 任意本地文件被读取 | ① 对 `/api/*` **不区分方法**做同站校验：优先看 `Sec-Fetch-Site`，缺失时退回比对 `Origin` 与 `Host`；② 非浏览器客户端（curl / 原生插件）不带这些头，照旧放行 |
| **Host 头未校验 → DNS rebinding 可冒充本机管理员** | 攻击者域名解析到 `127.0.0.1`，浏览器请求同时满足 `remoteAddress=127.0.0.1`（管理员）与 `Origin.host === Host`（旧 CSRF 校验用 Host 做基准）→ 拿到任意文件读写 | 新增 **Host 白名单**：只放行 `localhost` / `127.0.0.1` / `::1` / 本机网卡地址（v4+v6）/ 主机名；可用 `ALLOWED_HOSTS=你的域名` 追加。启动时打印提示 |

> 审计时靠"看代码像漏洞"的两条，实测后**排除了**：zip 目录穿越（`../` 会被跳过）、客户端中途 RST 断连（6 次实测服务不崩）。已实测确认的问题才写进来。

#### B. 中危：可靠性与副作用

| 问题 | 说明 | 修复 |
| --- | --- | --- |
| **data descriptor 靠扫签名定位 → 静默损坏数据** | `flags & 0x8` 时用 `for (…) if (字节 === 50 4B 07 08)` 找描述符。实测：内容含该 4 字节的文件，54 字节只解出 5 字节；更糟的是偏移错位后**后续条目被整体丢弃**（第二个条目 `.model3.json` 直接消失，接口却仍返回 `ok`）。而 PowerShell `Compress-Archive` 生成的 zip 正是这种 | **改为以中央目录（EOCD + CD）为权威元数据**读取名称/压缩大小/数据偏移，不再猜。没有可用中央目录且条目带 bit3 时，直接报"请用标准工具重新打包"，而不是猜 |
| **危险类型可落盘并被同源执行** | 上传的 zip 里若含 `.html/.svg/.js`，会被静态服务按 `text/html` 返回 → 同源 XSS → 可读 `sessionStorage` 里的 API Key、调用 Agent 接口 | ① 解压时**跳过** `.html/.js/.svg/.exe` 等扩展名并在响应里回报 `blocked`；② `live2d/models/` 下**一律以 `application/octet-stream`** 返回；③ 全站加 `X-Content-Type-Options: nosniff`；④ 上传界面提示"已跳过 N 个不允许的文件" |
| **`computer` 模式下 AI 可无声覆盖任意文件** | 提示词被图片文字/长期记忆/第三方模型输出污染后，一次模型输出就能写任意路径 | 客户端在 `permission === 'computer'` 写文件前弹确认，列出目标路径（同一路径只问一次）；把权限改回"限制模式"可彻底关闭 |
| **`agentWrite` 请求体无上限** | `readJsonBody` 有 64KB 上限，`agentWrite` 自己读全部 chunk | 新增 `AGENT_WRITE_LIMIT = 1MB`，超限 413 |
| **密码校验用同步 `scryptSync`** | 每次登录尝试阻塞事件循环数十毫秒，可被并发请求拖死服务 | 改用 `promisify(scrypt)` 异步；`sessions` / `loginFails` 增加 10 分钟定期清理（`unref`），登录失败计数在锁定期过后重新计数 |
| **静态文件流没有 error 处理** | `createReadStream(...).pipe(response)` 不转发错误 | 加 `stream.on('error')` + `response.on('close')` 释放句柄；`stat` 改 `lstat` 拒绝符号链接；响应头已发送时不再二次 `writeHead` |

#### C. 前端（`live2d-video.js` / `index.html`）

| 问题 | 修复 |
| --- | --- |
| 同类标签只有第一个生效（`[表情:a][表情:b]` → 只有 a；`[操作:]` 在 8.7 已改全局，其它标签没跟上） | 统一抽出 `lastTagValue()`，**同类标签最后一个生效**，位置/大小/背景同步统一 |
| `setExpression` 是 async 却被"即发即忘"调用 → 连续回复时 `currentExpParams` 互相覆盖（表情切了脸还停在上一张） | 加自增 `expressionSeq`，每次 await 后校验"我是不是最后一次"，不是就直接丢弃结果 |
| `loadModel()` 无并发保护 → 快速切换模型会创建多个实例，旧的没 `destroy()`（实测创建 7 个） | 加 `modelLoadSeq`，过期结果直接 `destroy()` 丢弃。回归实测：创建 7 个、舞台上始终只有 1 个 |
| `autoStart:true` 的 PIXI 已渲染，自建 rAF 里又 `app.ticker.update()` → 每帧重复渲染一遍（注：不是 2 倍速，模型动画由 PIXI 自己的 ticker 驱动，只多了一次无效渲染） | 去掉手动 `ticker.update()`；`close()` 里 `app.ticker.stop()`、`open()` 里 `start()`，隐藏时不再空转渲染 |
| `[背景:url(https://evil/…)]` 由 AI 可控 → 写进 CSS 就是外链信标 | 新增 `sanitizeBackground()`：只放行 data: 图片 / 本服务内相对路径 / 渐变 / 纯色，禁止 `;` `{}` `expression(` `javascript:` 等 |
| 静音完全没有界面反馈（操作的是早已移除的 `#live2d-mute-btn`） | 新增 `#live2d-mute-hint` 提示条 |
| 表情/动作名直接拼进 URL（`encodeURIComponent` 不编码 `.`，Android 走本地文件系统时 `[表情:../../x]` 可穿越） | 新增 `sanitizeAssetName()`：含 `/` `\` `..` 或 `.-` 开头一律拒绝（实测 `setExpression('../../serve.mjs')` 直接返回 false，不发请求） |
| `fuzzyMatchFileName` 双向 `includes`：单字符 target 会命中任意文件 | 要求 target 与文件名长度均 ≥ 2 |
| **模型完全没有 exp 目录时，表情标签必然 404**（8.4 只修了"列表非空但匹配不到"的情况；回归测试抓到 1 次真实的 404 请求） | 新增 `modelAssetListKnown` 区分"清单未知"与"确定为空"：确定为空时直接忽略并告警，不再发注定 404 的请求。修复后 404 归零 |
| `escapeHtml` 不转义单引号，多处把 id 拼进 `onclick="…'${id}'"` | `escapeHtml` 补 `'` 与反引号；新增 `safeAttrId()`（白名单剔除）用于内联 `onclick` 里的 id —— 注意**不能**靠 `&#39;`，HTML 解析器会先解码实体再交给 JS |

#### D. 启动体验

`server.on('error')` 现在会给出可读提示（端口占用 → 提示换端口，退出码 1），不再抛裸栈。

#### E. 回归验证

- **服务端 39 项**：Host 白名单（伪造 Host 403 / 本机与网卡地址放行）、跨站 GET 与 Origin 拦截、静态服务（nosniff、符号链接、隐藏文件、models 下强制二进制）、zip 正常包/描述符包/危险类型/穿越/炸弹、请求体上限、鉴权（401/429）、中途断连稳定性。
- **前端 25 项**：模型加载、渲染与待机动画、鼠标跟随（含关闭再打开回归）、标签驱动（情绪不吞操作、多个操作标签）、背景消毒（恶意外链被丢弃、分号注入被拒）、静音提示、同类标签、表情名穿越被拒、调整框、并发切换模型无泄漏、无 404、无 JS 异常。

> 测试方法备忘：**验证 WebGL 画布内容不能用 2D canvas `drawImage` 读取**（PIXI 默认 `preserveDrawingBuffer: false`，读到的永远是空白），必须用页面截图比对。

### 8.10 鼠标跟随幅度可调（跟随幅度滑块 + 解锁更高倍率，2026-09）

**需求**：同一个"转头"参数在不同模型上的视觉幅度差别很大 —— 同样的设置有的模型几乎不动、有的一下转到底。用户需要在「设置 → Live2D」里手动拉平。

**UI**：滑块 `0-10`（标准范围，`10 = ×1.0` 原始幅度）+ 右侧数字框可填更大数字解锁更高倍率（上限 `100 = ×10`）。数字框失焦/回车会把实际生效值（已截断）回写。倍率即时写入 `localStorage['live2d.mouseFollowScale']`，默认 `10`（老用户无此配置时保持原幅度）。

**关键发现（决定了实现方式）**：一开始只做了"倍率乘数"，实测发现**倍率超过约 ×1.5 就完全失效**。原因是参数值会被 Cubism 按模型自身量程截断：

| 参数 | 量程 | 是否绑定到绘制 |
|---|---|---|
| LSS `ParamAngleX` | ±30 | ✅ 已绑定 |
| LSS `ParamBodyAngleX/Y/Z` | ±10 | ✅ 全部绑定 |
| LSS `ParamAngleZ` | ±30 | ✅ 已绑定 |
| LSS `ParamEyeBallX/Y` | ±1 | ❌ **未绑定（设了没反应）** |
| 悠小喵 `ParamAngleX` | ±30 | ❌ 未绑定 |
| 悠小喵 `ParamAngleX1` | ±30 | ✅ 已绑定 |
| 悠小喵 `ParamBodyAngle*` | ±10 | 存在但（单参数扫描时）位移为 0 |
| 悠小喵 `ParamEyeBallX/Y` | ±1 | ❌ **未绑定** |

头部基础驱动值是 `20`，而量程上限是 `30` → **×1.0 就用掉了三分之二，×1.5 到顶**。所以单纯放大倍率在头部上是空转。

**实现**：把"解锁更高倍率"接到**身体跟随**上，让高倍率真的产生变化：

- 头部：`x * 20 * k`、`y * 16 * k`（`k = 倍率/10`）—— ×1.0 与旧行为完全一致，之后到量程上限为止
- 眼球：基准值从 `1.3` 降到 `0.8`。原来 `1.3` 超出量程 `±1`，导致眼睛一动就顶到边界、永远贴着边；现在全偏转时到 `0.8`，不再截断。倍率按 `√k` 缓动
- **身体**：`bodyK = clamp((k-1)/2, 0, 1)`，驱动 `ParamBodyAngleX/Y` 到各自量程上限。**×1.0 时身体完全不动（保证向后兼容），×3.0 时身体转到该模型的上限**
- 新增 `getFollowAmpInfo()`：返回 `{ loaded, headSaturateAt, bodyParamCount }`。设置面板在倍率 > ×1.0 时直接显示 **「头部上限 ×1.5，更高倍率由身体跟随补足」**，免得用户以为"数字填了没用"是 bug

**实测（headless Chrome + 页面截图比对，设置弹窗必须先关闭）**：

| 倍率 | LSS `ParamAngleX` | LSS 身体 | LSS 像素变化 | 悠小喵 像素变化 |
|---|---|---|---|---|
| ×0.0 | 0 | 0 | — | — |
| ×1.0 | 19.14 | 0 | 44 845 px | 5 899 px |
| ×2.0 | **30（到顶）** | 4.79 | 58 058 px | 16 319 px |
| ×3.0 | 30 | 9.57（满） | **63 271 px** | **17 296 px** |
| ×6.0 | 30 | 9.57 | 63 271（平台） | 17 296（平台） |

两个模型的像素变化都随倍率单调增大，×3.0 起进入平台（身体已满量程）；变化区域始终是局部（悠小喵 128×417 → 153×417），**没有出现 8.6 那种"整个模型位移"的回归**。UI 侧 9 项 + 实效 6 项，共 10/10 通过。

> ⚠️ 测这类功能的坑：**设置弹窗（`#settingsOverlay`）盖住画布时，像素对比会全部得到 0**，看起来像"参数完全没生效"。必须 `document.getElementById('closeSettings').click()` 之后再截图。第一轮就是这么误判的。

**注意**：`ParamEyeBallX/Y` 在两个内置模型上都未绑定，所以"视线跟随"实际不生效 —— 这是模型作者没绑，不是代码问题。`diag-param-bind.html` 可以扫任意模型的绑定情况。

### 8.11 模型重命名 + 修复"鼠标静止时改倍率不生效"（2026-09）

**需求**：上传后的模型在列表里只显示 `model_mty0qqda_eyr8` 这种服务端生成的 id，用户分不清哪个是哪个。

**做法：目录名即显示名，重命名就真改文件夹。**

一开始担心"目录名是 URL 的一部分，改了会出事"，所以先做了一版"目录名保持 id + 另存 `.names.json` 显示名映射表"。但那多了一层状态，用户看到的文件夹名和界面里的名字对不上，反而更难排查。实测确认了那三个担心其实都能解决：

| 担心 | 实际情况 |
|---|---|
| 每模型设置失联（`localStorage['live2d.resize.<name>']`、`live2d.wm.<name>`） | 改名后**客户端主动迁移**这两个 key，设置不丢 |
| 模型正在通话中加载会 404 | 已加载的模型资源在内存里，画面不受影响；切换/重开走新路径，正常 |
| 中文/空格进路径会出问题 | `serve.mjs` 第 804 行本来就 `decodeURIComponent(url.pathname)`，客户端也用 `encodeURIComponent(name)` 拼 URL；而且模型内部文件名（`悠小喵.moc3`、`悠小喵.8192/texture_00.png`）一直是中文，早就跑通了 |

于是删掉 `.names.json`，改成：

- **上传时目录名直接取 zip 里的文件夹名**（拿不到才退回 `model_<ts>_<rand>`）—— 这样新传的模型一进来就不是 `model_xxx` 了
- `POST /api/live2d/rename { name, displayName }` → `fs.rename()` 真改目录，返回 `{ ok, name, newName }`
- **重名自动加序号**（`伊蕾娜 · 默认装 (2)`）—— 否则又回到"两个都叫同一个名字"
- 名字清洗 `sanitizeModelDirName()`：去控制字符、`\ / : * ? " < > |` → `_`、压缩空白、剥掉首尾的点与空格（Windows 不允许尾点/尾空格）、拒绝 `.`/`..`/Windows 保留设备名（`CON`/`NUL`/`COM1`…）、限长 60
- 客户端 `applyRenamedModel(old, new)`：迁移 `live2d.resize.*` 与 `live2d.wm.*`、把 `selectedModel` 指到新名字、刷新通话内下拉
- 删除模型时同时清掉这两个 key（原来只清了 resize，水印状态会残留）

UI：设置 → Live2D 的模型下拉旁新增「✏️ 重命名」，用 `showCustomModal({ input: true })` 弹输入框并预填当前名字。

**顺带修了用户实际遇到的报错**：服务端是旧版时 `/api/live2d/rename` 会落到静态处理器返回纯文本 `Not found`，前端 `res.json()` 抛出 `Unexpected token 'N', "Not found" is not valid JSON` —— 完全看不懂。新增 `readJsonSafe()`，非 JSON 响应给出**「服务端没有这个接口（HTTP 404）。多半是 serve.mjs 改了但服务没重启 —— 到 cmd 窗口按 Ctrl+C，再重新双击「启动.bat」」**。上传/删除/列表也统一走它。

**实测（curl 15 项 + UI 16 项全过）**：

- 服务端：中文名改名成功且**磁盘上真的改名** ✅、静态访问中文目录下的 model3.json 返回 200 且内容正确 ✅、重名加 `(2)` ✅、非法字符 `a/b\c:d*e?f"g<h>i|j` → `a_b_c_d_e_f_g_h_i_j` ✅、只有点/空格 → 400 ✅、空名 → 400 ✅、`CON` → 400 ✅、首尾点被剥离 ✅、超长截断到 60 ✅、同名 → `unchanged:true` ✅、路径穿越 → 404 ✅、不存在 → 404 ✅
- 上传：zip 内文件夹名 `测试用模型` → 目录名就是 `测试用模型` ✅、再传一次 → `测试用模型 (2)` ✅
- UI：下拉更新 ✅、选中项指向新名 ✅、`live2d.resize.*` / `live2d.wm.*` 双双迁移且旧 key 清空 ✅、服务端列表同步 ✅、**改名后的模型在通话里真的加载出来**（标签=新名字、尺寸 4359×8500、168 个参数）✅、能改回原名 ✅

> 踩坑：第一版 UI 测试只断言了"有模型加载出来"，但默认加载的是列表里第一个模型 —— 改名后的那个根本没被加载，测试给了假阳性。改成**断言通话内名字标签等于新名字**才真正验到。测"改名/换模型"这类功能时，一定要断言"加载的是哪一个"，不能只断言"加载成功了"。

#### 顺带修掉一个 bug：鼠标静止时改倍率不生效

写完 8.10 后复查发现：`mouseTrackTick` 是**插值到稳定就 `clearInterval`** 的，而 `setMouseFollowScale` 只在**归零**时才主动复位参数。所以「鼠标不动、只调倍率」时模型会一直停在旧倍率的姿态上。

实测确认：倍率 ×3.0 且鼠标静止 → 头部 30 / 身体 9.58；改回 ×1.0 **不动鼠标** → 仍然是 30 / 9.58（应该变成 19.14 / 0）。

修复：把驱动部分抽成 `applyFollowDrive()`，`setMouseFollowScale` 在非零时立即调用一次重算。修后同样场景 → 头部 19.16 / 身体 0 ✅。

## 9. 已知限制 / 待办

- [ ] 模型表情/动作映射基于 exp 目录文件名，不同模型需调整 `EXPRESSION_MAP`
- [x] ~~嘴部动画是模拟正弦波（非真实音频驱动）~~ → 已改为用 Web Audio AnalyserNode 读真实 TTS 音量驱动 `ParamMouthOpenY`，无音量时自动回落到正弦波
- [ ] **参数绑定情况由模型作者决定，换模型必须重新验证**：悠小喵没有绑定任何眉毛参数（`ParamBrowL/R`、`ParamBrowLY/RY`、`ParamBrowLForm/RForm` 设了都没反应），所以情绪里的"皱眉/挑眉"在这个模型上无法通过参数表达；`ParamSmile`/`ParamEyeLSmile` 同样未绑定。要支持眉毛只能依赖模型的 exp 表情文件，或换模型
- [ ] 参数"是否存在"与"是否绑定"无法在运行时便宜地探测（需要逐个设值做像素 diff），目前靠候选名 + 关键字兜底；遇到命名特殊的模型仍可能解析到未绑定的参数
- [ ] 带 `2` 及以上后缀的角度变体（`ParamAngleX2` / `ParamAngleY2`）语义不可靠：在悠小喵上 `ParamAngleY2` 会推动整个模型而不是转头，因此已排除不驱动。若某个模型把头部的俯仰**只**绑在 `Y2` 上，需要单独适配
- [x] ~~鼠标跟随的幅度（X 轴 ±20°、Y 轴 ±16°）是固定值，未做设置项~~ → 已加「设置 → Live2D → 跟随幅度」：滑块 0-10（×0.0~×1.0）+ 数字框解锁到 100（×10）。头部到量程上限后由身体跟随补足，见 8.10
- [ ] 头部转动幅度受模型自身参数量程锁死（实测两个内置模型都是 ±30，约 ×1.5 到顶）。倍率再往上只能靠身体跟随，而身体参数是否绑定由模型作者决定（悠小喵的 `ParamBodyAngle*` 存在但单参数扫描位移为 0，实际介入幅度有限）
- [ ] `ParamEyeBallX/Y` 在两个内置模型上都**未绑定**（设了没反应），所以"视线跟随"实际不生效。只有模型作者绑定过才有眼球跟随；`diag-param-bind.html` 可扫描
- [ ] Live2D 模型二次发布限制：模型目录已 gitignore，但**用户上传的模型版权需自行注意**
- [ ] 安卓（Capacitor）端 Live2D 未实测（网格手柄交互同样未在安卓 WebView 实测）
- [ ] 上传大 zip（>50MB）内存占用较高（整包读入内存）
- [ ] 尺寸记忆按模型名存储，但不同屏幕尺寸（手机/桌面）共用一份，跨端切换屏幕后比例会按当前屏幕高度钳制

## 10. 常见问题（FAQ）

**Q: 视频通话提示"模型加载失败：Network error"？**
A: 模型文件不在 `web/live2d/models/` 里（被清理或未上传）。重新在设置 → Live2D 上传，或把模型文件夹放进去。

**Q: 上传 zip 后找不到 model3.json？**
A: zip 需包含 `.model3.json` 文件（Cubism 3 模型）。确认 zip 里有，且解压后路径正确。

**Q: 模型列表里全是 `model_mty0qqda_eyr8` 这种名字，分不清哪个是哪个？**
A: 设置 → Live2D，选中模型后点「✏️ 重命名」，起个能认出来的名字（例如「伊蕾娜 · 白裙」）。这会**直接把模型的文件夹改名**（中文、空格都可以），视频通话里的下拉会一起变，该模型的缩放/水印设置也会自动迁移；重名会自动加 `(2)`、`(3)`。另外，**新上传的模型现在直接用 zip 里的文件夹名做目录名**，不再是一串 `model_xxx` 了。

**Q: 点重命名提示 `Unexpected token 'N', "Not found" is not valid JSON`？**
A: 这是**服务端还在跑旧代码**：`/api/live2d/rename` 这个接口不存在，请求落到静态文件处理器返回了纯文本 `Not found`，前端解析 JSON 就炸了。到 cmd 窗口按 `Ctrl+C`，重新双击「启动.bat」即可（`serve.mjs` 的改动必须重启服务才生效；`index.html` / `live2d-video.js` 刷新浏览器就行）。新版前端已能识别这种情况并直接给出这条提示。

**Q: 背景/大小没生效？**
A: 强刷（Ctrl+F5）清缓存。背景在设置 → Live2D 里选；大小/位置在视频通话底部点「📐 调整」打开调整框，拖手柄缩放、框内拖动移动（角手柄等比，Shift 自由），−/＋ 可等比微调。

**Q: 浏览器语音识别用不了/报错？**
A: 打开 `http://localhost:4173/diag-asr.html` 语音识别诊断页，先点「测试麦克风」再点「开始语音识别测试」，页面会显示：环境检测（Chromium 内核/安全上下文/API）、麦克风权限结果、实时转写、事件日志和诊断结论（含解决建议）。把结果反馈给开发者即可快速定位。

**Q: 模型水印怎么隐藏（悠小喵按 1、泠按 0）？**
A: 这类模型自带"水印开关"表情（exp 目录里的 `水印开关.exp3.json` 等）。打开视频通话后，若模型有水印开关，按键盘 **1 / 0** 即可切换水印隐藏/显示，也可以直接让 AI「把水印隐藏掉」（它会用 `[操作:隐藏水印]` 触发）。**通话界面没有水印按钮**，只能靠键盘或 AI 触发。开关状态按模型记忆。任意模型只要 exp 目录含"水印/watermark"命名的表情都会自动支持，无需改代码；若某个模型水印机制特殊（无开关表情），需要按模型单独适配。

**Q: 换了新模型后鼠标不动了 / 模型不跟随鼠标？**
A: 先确认「设置 → Live2D → 鼠标跟随」是开着的——关闭时通话界面会显示「鼠标跟随已关闭 · 点此开启」提示条，点一下即可开启。如果开关是开的却仍不动，用 `diag-param-bind.html` 扫一下该模型的 `ParamAngleX` / `ParamAngleY` 是否"已绑定"（模型作者可能没把标准名绑到绘制上）。历史上还有一个已修复的 bug：关闭通话再重新打开会丢监听器（见 8.8）。

**Q: 鼠标跟随幅度太小/太大，怎么调？**
A: 设置 → Live2D → 跟随幅度。滑块 `0-10`（`10 = ×1.0` 原始幅度）先在这个范围里找合适档位；数字框可以填更大数字解锁更高倍率（最大 `100 = ×10`）。注意**头部转动幅度受模型自身参数上限限制**（多数模型是 ±30，约 ×1.5 就到顶），再往上由**身体跟随**补足——×1.0 时身体不动，×3.0 时身体转到该模型上限。所以填到 ×3.0 之后通常就不再变化了。面板上的提示会直接告诉你该模型的头部上限（如「头部上限 ×1.5」）。

**Q: AI 回复里的 `[表情:xxx]` 没触发模型表情？**
A: 检查模型 exp 目录是否有对应表情文件；`EXPRESSION_MAP` 是否匹配（悠小喵：happy→星星眼、sad→哭哭、blush→脸红…）。

**Q: 打开页面提示"请求的 Host 不在允许列表内"？**
A: 这是防 DNS rebinding 的白名单校验（见 8.9）。用本机/局域网 IP 或 `localhost` 访问即可；确实要用主机名或域名访问时，设置环境变量 `ALLOWED_HOSTS=你的域名` 后重启服务。

**Q: 端口被占用，服务起不来？**
A: 提示里会写清楚。换端口启动即可：`set PORT=4174 && node web/serve.mjs`。

**Q: 上传模型后提示"已跳过 N 个不允许的文件"？**
A: 那些是 `.html/.js/.svg/.exe` 之类的可执行类型，出于安全不会落盘（否则上传的文件会以同源身份在浏览器里执行）。正常的 Live2D 模型文件（`.model3.json` / `.moc3` / 纹理 / `.exp3.json` / `.motion3.json`）不受影响。

**Q: 用「允许操作电脑」模式时为什么弹确认框？**
A: 该模式允许 AI 写任意路径，之前是完全无声执行的。现在每个新路径写入前会弹一次确认（同一路径只问一次）。想彻底关闭就把权限改回「限制模式」。

## 11. 二次开发入口

- **Live2D 模块**：`web/live2d-video.js`（渲染、驱动、上传、背景、大小）
- **服务端**：`web/serve.mjs`（上传/删除 API、zip 解压）
- **设置界面**：`web/index.html` 的 `tab-live2d` / `tab-vision` 区块
- **AI 回复驱动**：`web/index.html` 的 `handleUserInput`（speakStart/speakEnd/drive）
- **视觉转述**：`web/index.html` 的 `callAI` / `describeImageWithVision`
