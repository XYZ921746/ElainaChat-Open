# 安卓工程（Capacitor + Gradle 打包工具链，不随仓库分发）
#
# 注意：android-app/ 已移到**项目上一级**（D:\222\android-app）—— 构建产物与
# 工具链（含 Android SDK）不放在项目目录里，避免被误提交、也便于复用。
# 这条规则保留有两个作用：
#   ① 兜住升级前遗留在本目录的老副本（删掉它的话，老副本会被 git add . 提交上去）
#   ② 万一将来又把工程移回来，不用再改
android-app/

dist/
*.apk

# Live2D 模型（默认模型随仓库分发，其余一律不入库）
# 注意 1：必须写 models/* 而不是 models/ —— 如果整个目录被排除，git 根本不会进入它，
#         后面针对子目录的 ! 例外就不会生效（gitignore 的已知行为）。
# 注意 2：本文件必须保存为 UTF-8。git 是按字节比较路径的，若用 GBK 保存中文规则会静默失效。
web/live2d/models/*
# 随仓库分发的模型（其余模型仍不入库）
!web/live2d/models/伊蕾娜·默认/
!web/live2d/models/deepseek/

# 本地打包压缩包，不入库
*.zip

# 访问密码。2026-09 起已迁到 data/auth.json（data/ 整目录本就排除），
# 这条规则保留是为了兜住升级前遗留在 web/ 里的老文件 —— 删掉它的话，
# 没跑过新版服务的人执行 git add . 就会把访问密码提交上去。
web/.local-auth.json

# 应用数据目录：跨设备同步的聊天记录 / 设置 / API Key，以及自签 HTTPS 证书。
# 里面全是本机私密数据，绝对不能入库。
data/

# 签名与本地环境
*.jks
*.keystore
local.properties
.env
.env.*

# 沙箱可行性验证（一次性 POC，不参与软件运行）
# 想保留的话可以删掉这条把它一起提交
poc/

# AI 工作记录（不属于项目内容，只是本机的辅助笔记）
.workbuddy-ai/

# 依赖、日志、系统垃圾
node_modules/
npm-debug.log*
yarn-error.log*
*.log
.DS_Store
Thumbs.db
