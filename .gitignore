# 安卓工程（Capacitor + Gradle 打包工具链，不随仓库分发）
#
# 注意：android-app/ 已移到**项目上一级**（D:\222\android-app）—— 构建产物与
# 工具链（含 Android SDK）不放在项目目录里，避免被误提交、也便于复用。
# 这条规则保留有两个作用：
#   ① 兜住升级前遗留在本目录的老副本（删掉它的话，老副本会被 git add . 提交上去）
#   ② 万一将来又把工程移回来，不用再改
android-app/

dist/
*.apk

# ============================================================================
#  二进制资源（mod 图片 / Live2D 模型）—— 不入库，改走 GitHub Releases
#
#  为什么：这两块加起来 24MB，其中 87% 是二进制（PNG/JPG/moc3）。Git 对二进制
#  **无法有效增量压缩** —— 改一张立绘，历史里就多存一整份。主仓库 .git 已经
#  54MB（fork 历史所致），再塞这些会持续膨胀，clone 越来越慢。
#
#  分工：
#    · 仓库里**保留 mod 的源码**（manifest.json / index.js / style.css，共约 60KB）
#      —— 它们要跟主程序 API 对齐、要跑测试，属于开发的一部分。
#    · **资源**（img/ 等）与整个 Live2D 模型走 Releases，用
#      `node scripts/pack-assets.mjs` 打包后上传。
#
#  ⚠️ 注意：只忽略资源子目录，不要忽略 mod 根目录 —— 否则源码也进不来。
# ============================================================================

# mod 的图片资源（源码仍入库）
web/mods/*/img/
# Live2D 模型整体不入库（改为 Releases 分发）
# 注意：必须写 models/* 而不是 models/ —— 整个目录被排除的话，
#       git 不会进入它，后面的 ! 例外也不会生效（gitignore 的已知行为）。
web/live2d/models/*

# 本地打包压缩包，不入库
*.zip

# 访问密码。2026-09 起已迁到 data/auth.json（data/ 整目录本就排除），
# 这条规则保留是为了兜住升级前遗留在 web/ 里的老文件 —— 删掉它的话，
# 没跑过新版服务的人执行 git add . 就会把访问密码提交上去。
web/.local-auth.json

# 应用数据目录：跨设备同步的聊天记录 / 设置 / API Key，以及自签 HTTPS 证书。
# 里面全是本机私密数据，绝对不能入库。
data/

# 签名与本地环境
*.jks
*.keystore
local.properties
.env
.env.*

# 沙箱可行性验证（一次性 POC，不参与软件运行）
# 想保留的话可以删掉这条把它一起提交
poc/

# AI 工作记录（不属于项目内容，只是本机的辅助笔记）
.workbuddy-ai/

# 依赖、日志、系统垃圾
node_modules/
npm-debug.log*
yarn-error.log*
*.log
.DS_Store
Thumbs.db
