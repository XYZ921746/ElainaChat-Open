# 📦 ElainaChat-Open 改造版 — 交付文档

> 基于 MIT 开源的 [ElainaChat-Open](https://github.com/shuixinggangzheng/ElainaChat-Open) 二次开发，新增 **Live2D 视频通话**、**视觉模型图片转述**、**AI 手机操作**、**跨设备同步**、**设置分栏** 等能力。

---

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 项目名 | ElainaChat-Open（改造版） |
| 技术栈 | 纯前端单 HTML + Node 静态服务（零依赖） |
| 定位 | BYOK（自带 Key）AI 角色聊天 + Live2D 视频通话 |
| 运行方式 | `node web/serve.mjs`，端口 4173 |
| 平台 | Web + 安卓（Capacitor） |

## 2. 核心功能

### 2.1 聊天
- OpenAI 兼容 / Anthropic / Gemini / Ollama 多格式（BYOK）
- 角色卡、世界观、分层提示词、记忆整理、图片输入
- TTS 多服务商：MiniMax / 阿里 Qwen-TTS / 豆包 / OpenAI / 浏览器
- ASR：阿里百炼（`qwen3-asr-flash`，DashScope）/ 小米 MiMo；浏览器识别标注「暂不可用」

### 2.2 🎥 Live2D 视频通话（新增）
- 顶部栏「📹 视频通话」按钮 → 全屏通话界面
- **模型外挂**：应用内上传 zip（`web/live2d/models/`，gitignore 不入库）
- 模型选择下拉 + 删除
- **模型大小/位置调节（通话界面调整框）**：底部「📐 调整」按钮开关调整框——
  - 框贴合模型实际绘制范围（非画布留白），默认隐藏、每个模型独立记忆位置/大小
  - 角手柄等比缩放（Shift 自由拉伸）、边手柄单轴、框内拖动移动
  - −/＋ 按钮等比微调 ±10%
- **背景设置**（设置页：预设渐变 / 纯色 / 自定义颜色 / 图片）
- **AI 说话嘴部同步**（LipSync，嘴型幅度随情绪变化：激动张嘴大、平静/伤心小）
- **💧 水印开关（通用自动探测）**：若模型带"水印/watermark"开关表情（如悠小喵「水印开关」→ Param85=1、泠等），打开通话后按键盘 **1 / 0** 切换水印隐藏/显示，也可由 AI 用 `[操作:隐藏水印]` / `[操作:显示水印]` 触发（**通话界面没有水印按钮**），直接驱动参数、比 expression 播放可靠，每个模型记忆开关状态
- **🎤 说话（真实语音输入）**：底部「🎤 说话」按钮，点击开始录音（复用主应用 ASR：阿里百炼 / 小米 MiMo / 浏览器），再点结束发送；聆听时按钮变红脉冲
- **底部栏精简**：只保留 🎤 说话 / 📐 调整 / 📞 挂断（AI 声音静音开关与 −/＋ 大小按钮已移除；静音仍可由 AI `[操作:静音]` 标签触发，大小调整用 📐 调整框拖拽）
- **AI 状态驱动（标签协议，AI 自主决策）**：LLM 回复可用 `[情绪:开心|难过|生气|害羞|惊讶|委屈|思考|平静]`（自动映射表情+嘴型幅度）、`[表情:xxx]`、`[动作:xxx]`、`[位置:left|center|right|上|下]`、`[大小:大|小|特大|特小|65%]`、`[背景:深蓝|星空|…]`、`[操作:打开视频通话|结束通话|整理记忆|静音|取消静音]` 标签驱动模型表现与应用功能；无标签时按触发词/emoji 自动推断情绪；标签均不显示给用户
- **跨模型自适应（AI Agent 决策）**：模型列表接口返回每个模型的 exps/motions（.exp3/.motion3 文件名）；系统提示词**动态注入当前模型可用表情/动作列表**，由 AI 根据情景直接选用真实文件名（`[表情:哭哭]` 等），换任何 Live2D 模型都自动适配；语义键（`[表情:happy]`）与情绪词（`[情绪:开心]`）经 语义映射 → 精确 → 子串/同义词组 三级解析兜底；表情参数用 `setParameterValueById` 直接驱动（比库的 expression() 可靠）
- **⏰ AI 定时任务（未来消息）**：LLM 可用 `[任务:每天 09:00 提醒我喝水]` / `[任务:明天 14:00 …]` / `[任务:每3小时 …]` / `[任务:每周五 18:00 …]` / `[任务:30分钟后 …]` 创建定时任务；调度器每 30s 检查，到点 AI 以角色身份自动发消息（插入当前对话 + 可选浏览器通知），支持周期/一次性；设置 → 高级可查看和取消；系统提示词已注入标签指南引导模型自然使用
- **🤖 AI Agent 权限分级（设置 → 高级）**：`[操作:列出文件 路径]` / `[操作:查看文件 路径]` / `[操作:保存文件 路径|内容]` 文件操作按权限模式受限：**🔒 限制模式**（默认）仅可读写本应用文件夹（web/），**💻 允许操作电脑**模式可读写任意路径（经本地服务执行，风险自担）；操作结果以「【AI 操作结果】」消息回填对话作为上下文；应用内操作（视频通话/记忆/水印/背景/静音）两种模式都可用
- **LLM 表情/动作驱动**：回复含 `[表情:xxx]` / `[动作:xxx]` 标签 → 模型自动做表情/动作（标签不显示）
- 麦克风开关（关 = 模型闭嘴 + AI 静音）

### 2.3 👁️ 视觉模型图片转述（新增）
- 设置 → 视觉：配一个 OpenAI 兼容的视觉模型（如 Qwen-VL）
- 图片对话时：**视觉模型先看图转述成文字 → 交给主模型（如 DeepSeek）回答**
- 未配视觉模型时：图片直接发给当前模型（原行为）

### 2.4 设置分栏（新增）
设置面板改为 6 个 Tab：**角色 / 对话 / 语音 / 视觉 / Live2D / 高级**

### 2.5 📱 AI 手机操作（新增）

让 AI 能**看见并操作手机**：点击、滑动、输入文字、读控件树、截图、启动应用。**仅安卓版 App 可用**（浏览器没有这层能力）。

**四种实现方式，能力与门槛各不相同**（设置 → 高级 → 手机操作 → 「选择实现方式」）：

| 实现方式 | 门槛 | 能力 | 中文输入 |
|---|---|---|---|
| 无障碍 | 免 root，开系统无障碍服务 | 点击/滑动/输入/读界面/截图(API 30+) | **只有它能可靠做到** |
| Shizuku | 免 root，装 Shizuku 并授权 | 同上 + 任意 shell + 启动应用 | 走无障碍 |
| Root | 手机已 root | 同上，不依赖电脑 | 走无障碍 |
| 模块 | 刷 KernelSU 模块 | 最强，**且不抢前台**（后台静默操作） | 走无障碍 |

**默认不预设任何实现方式**（`agentBackend: ''`）。没选时状态文字标黄提示，真正执行时被拒绝并回灌一句能照做的提示 —— **但设置项本身不禁用**，因为总开关与确认策略跟"走哪条路"无关，把设置锁住只会让人以为功能坏了。

**用法**（AI 在回复里发标签，标签不显示给用户）：

```
[操作:手机状态]  [操作:手机查看界面]  [操作:手机截图]  [操作:手机等待 800]
[操作:手机点击 500 800]  [操作:手机滑动 500 1500 500 500 300]
[操作:手机输入 你好世界]  [操作:手机按键 返回]
[操作:手机打开 包名]  [操作:手机命令 <任意 shell>]
```

**运行时可停止**：运行期间屏幕上方固定一条状态条（脉冲点 + 当前步骤与动作名 + 「停止」按钮），点停止会中断在途请求、关掉等待中的确认弹窗，并拦截同一条回复里后续所有步骤。

**敏感操作授权**：风险分三级 —— `safe`（状态/查看界面/截图/等待）直接放行；`sensitive`（点击/滑动/输入/按键/打开）按设置策略；`dangerous`（手机命令）**无论策略如何都单独询问**。策略三档：`always`（默认，每次都问）/ `run`（同一次运行内允许过就不再问）/ `off`。非法值一律回落到 `always`。

**截图交给视觉模型看**：原生把截图以 base64 带回，前端交给「设置 → 视觉」里配的多模态模型转述成文字再回灌主模型。没配视觉模型时如实说明并建议改用「手机查看界面」读控件树。

## 3. 文件结构

```
ElainaChat-Open/
├── web/                          # 唯一编辑入口
│   ├── index.html                # 主应用（单文件，约 704KB）
│   ├── live2d-video.js           # Live2D 视频通话模块（新增）
│   ├── serve.mjs                 # 静态服务 + API + HTTPS + 访问密码 + 数据同步 + 模型上传（增强）
│   ├── diag-live2d.html          # Live2D 库诊断页（调试用）
│   ├── diag-asr.html             # 语音识别诊断页：检测浏览器环境 + 麦克风 + 实际识别测试（调试用）
│   ├── diag-param-bind.html      # 模型参数绑定诊断页：扫描"参数存在但设了没反应"（调试用）
│   ├── vendor/
│   │   ├── tailwind.js
│   │   ├── live2dcubismcore.min.js            # Live2D Cubism3/4 核心
│   │   ├── pixi-6.min.js                      # PIXI 6.5.10
│   │   └── pixi-live2d-display-cubism4.min.js # 纯 Cubism4 版（关键！）
│   └── live2d/models/            # 模型目录（仅内置模型入库，其余 gitignore）
├── 开发文档.md                    # 本文件
├── scripts/sync-web.mjs          # 把 web/ 同步进安卓工程
├── data/                         # 本机私密数据（gitignore，不入库）
│   ├── store.json                # 跨设备同步的聊天记录 / 设置 / API Key
│   └── cert.pem / key.pem        # 自签 HTTPS 证书
├── android-app/                  # 安卓（Capacitor，gitignore 不入库）
│   └── android/app/src/main/java/com/elainachat/opensource/
│       ├── ByokHttpPlugin.java / ByokSecretsPlugin.java     # 原版：直连 + Keystore
│       ├── ElainaShellPlugin.java                           # 新增：手机操作插件入口
│       ├── ShellProcess.java / ShellBackends.java            # 新增：命令执行
│       ├── ShizukuShell.java / UserService.java / IElainaUserService.aidl  # 新增：Shizuku 后端
│       ├── ElainaAccessibilityService.java                  # 新增：无障碍后端
│       ├── PhoneCommand.java / DeviceArtifacts.java          # 新增：命令构造 / 产物处理
│       └── ...
├── .gitignore
└── README.md
```

## 4. 运行

```bash
cd ElainaChat-Open
node web/serve.mjs
# 打开 http://localhost:4173
# 服务器默认监听 0.0.0.0：手机/平板可用 http://<本机局域网IP>:4173 访问
# （可用 HOST 环境变量覆盖，如 HOST=127.0.0.1 仅本机；PORT 可改端口）
```

## 5. 配置说明

### 5.1 对话模型（设置 → 对话）

**API 格式只留三种**（2026-09 收敛，原来 5 种里 gemini / ollama 的原生实现已删除）：

| 格式 | 适用 | 默认地址 | 默认模型 |
|---|---|---|---|
| **OpenAI 兼容** | 任何提供标准 `/v1/chat/completions` 的服务：DeepSeek、硅基流动、OneAPI、Ollama、千问兼容模式、各种自建中转 | `https://api.deepseek.com/v1` | `deepseek-chat` |
| **OpenAI 官方（Responses 接口）** | 只有 OpenAI 官方用的 `/v1/responses` 协议 | `https://api.openai.com/v1` | `gpt-4.1-mini` |
| **Anthropic Claude** | `/v1/messages`，认证头是 `x-api-key` 而不是 `Bearer` | `https://api.anthropic.com` | `claude-sonnet-4-20250514` |

- 其余字段：Base URL + API Key + 模型名（BYOK；安卓 Key 走系统 Keystore，Web 存 sessionStorage + localStorage）
- **「请求模式」下拉已删除**。它只有"直接连接 / 自定义代理"两个选项，而"直接连接"会把 Base URL 锁成只读并强行覆盖，
  用户想用自建中转时反而没地方改 —— 是个纯粹添乱的设计。现在 Base URL 永远可编辑，
  切换格式时只在"输入框为空"或"值恰好等于某个格式的默认地址"时才自动替换。
- **旧配置自动迁移**：`openai` / `dashscope` / `ollama` / `gemini` 四个旧格式名统一映射到 `openai-compatible`；
  Gemini 与 Ollama 的旧地址改写到各自的 OpenAI 兼容端点（`/v1beta/openai`、`/v1`）。
  迁移在配置校验**之前**执行，否则旧值会被判为非法而重置成默认，用户的地址就丢了。
- 切格式时会同步替换模型名（仅当模型框为空或恰好等于某个格式的默认模型），避免切到 Anthropic 后
  框里还留着 `deepseek-chat` 被原样发过去 → 404，而界面上完全看不出原因。

### 5.2 视觉模型（设置 → 视觉）
| 字段 | 说明 |
|---|---|
| API 地址 | OpenAI 兼容，如 `https://api.siliconflow.cn/v1` |
| API Key | 视觉模型 Key |
| 模型名 | 如 `Qwen/Qwen2.5-VL-72B-Instruct` |

### 5.3 Live2D（设置 → Live2D）
- **上传模型**：选 .zip（Live2D Cubism 3 模型，含 `.model3.json` + `.moc3` + 纹理）
- **模型选择**：下拉切换已上传模型（下拉显示友好名，值仍是目录名）
- **重命名模型**：点「✏️ 重命名」直接改模型的**文件夹名**（中文、空格都可以；重名自动加序号），每模型的缩放/水印设置会一起迁移，见 8.11
- **鼠标跟随**：开关 + **跟随幅度**（滑块 0-10 = ×0.0~×1.0；右侧数字框可填到 100 = ×10 解锁更高倍率，×1.0 以上由身体跟随补足），见 8.10
- **背景**：渐变/纯色/自定义/图片
- **大小**：通话界面 −/＋ 调节

### 5.4 语音（设置 → 语音）
- TTS：MiniMax / 阿里 Qwen-TTS / 豆包 / OpenAI
- ASR：阿里百炼（`qwen3-asr-flash`）/ 小米 MiMo（浏览器识别暂不可用）

## 6. 服务端 API（serve.mjs）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/live2d/upload` | POST | 上传模型 zip，解压到 `web/live2d/models/<目录名>/`；**目录名直接取 zip 里的文件夹名**（拿不到才退回 `model_<ts>_<rand>`），重名自动加 ` (2)` |
| `/api/live2d/models` | GET | 列出模型（`name`、`modelJson` 路径、`exps` 表情 / `motions` 动作列表、`vtube.json`）。**`exps`/`motions` 是「相对模型根目录的路径」**（如 `脸红.exp3.json`、`motions/idle.motion3.json`），不是裸文件名 —— 表情/动作并不都在 `exp/` 子目录里，见 8.17 |
| `/api/live2d/models/<name>` | DELETE | 删除模型目录 |
| `/api/live2d/rename` | POST | 重命名模型：`{ name, displayName }` → **真改文件夹**，返回 `{ ok, name, newName }`（见 8.11） |
| `/api/relay` | POST | **本地中转**（见 8.13）。请求体 `{ url, method, headers, body, timeoutMs }`，原样转发并透传状态码与响应体。用于绕开浏览器 CORS：自建中转站 / 内测网关不发 `Access-Control-Allow-Origin` 时，前端改走这条同源通道 |
| `/api/logs/settings` | GET | 读日志设置：当前落盘级别、五个可选级别、对话追踪开关、日志目录与当前文件名、保留份数、单文件上限（见 8.23） |
| `/api/logs/settings` | POST | 改日志设置：`{ level?, trace? }`。**运行时立即生效、无需重启**，并持久化到 `data/logs/log-settings.json`。级别接受 `DBUG`/`WARNING` 等别名，非法值返回 400 且不改动当前值 |
| 静态服务 | GET | 主页面 + vendor + 模型文件。**静态根目录就是 `web/`**，白名单 `[web/, web/live2d/models]` —— `data/` 刻意在静态根之外，所以 `store.json`（含 API Key）与证书不可能被下载 |

> **日志**：所有 `console.*` 输出同时写入 `data/logs/<启动时刻>.log`（每次启动一个新文件、只保留最近 10 份、本地时间、单行化、不记录请求头）。**终端始终显示全部级别，`LOG_LEVEL` / 设置界面只过滤落盘内容**。见 8.14 / 8.16 / 8.23。

> **模型目录名就是显示名**（上传时直接取 zip 里的文件夹名）。URL 与文件名都走 `encodeURIComponent` / `decodeURIComponent`，所以中文、空格、emoji 都能正常访问。整个 `models/` 已在 `.gitignore` 里，不会入库。

### zip 解压实现要点（已修复）
- **中文文件名**：支持 UTF-8 标志位 + GBK 兜底解码（Windows zip 常见 GBK）
- **data descriptor**：支持（PowerShell Compress-Archive 生成的 zip 带此标志）
- **目录名**：直接用 zip 里的文件夹名（中文、空格都可以；拿不到才退回 `model_<ts>_<rand>`），重名自动加 ` (2)` —— 见 8.11
- **防目录穿越**：路径白名单校验

## 7. Live2D 技术要点（踩坑记录）

### 7.1 库组合（关键！）
```
live2dcubismcore.min.js           # Cubism 3/4 核心（必须）
pixi-6.min.js                     # PIXI 6.5.10（必须 6.x）
pixi-live2d-display-cubism4.min.js # 纯 Cubism4 版（必须用这个！）
```

**⚠️ 不要用 `pixi-live2d-display` 的 `index.min.js`（混合版）**——它强制要求 Cubism 2 运行时（`live2d.min.js`），而 Cubism 2 核心很难获取（官方 CDN 404、npm 无），会导致 `PIXI.live2d` 挂载失败。

**正确文件来源**：`pixi-live2d-display@0.4.0` npm 包里的 `dist/cubism4.min.js`（纯 Cubism 4，不需要 Cubism 2）。

### 7.2 模型加载
```js
// 等待 cubism4 就绪
if (PIXI.live2d?.cubism4Ready) await PIXI.live2d.cubism4Ready;
const model = await PIXI.live2d.Live2DModel.from('/live2d/models/<name>/<model3.json>', { autoInteract: false });
```

### 7.3 嘴部同步
```js
model.internalModel?.coreModel?.setParameterValueById?.('ParamMouthOpenY', value);
// 模型 model3.json 需有 LipSync 组（悠小喵有）
```

### 7.4 表情/动作
- 表情：`model.expression('<模型根>/<清单给出的相对路径>.exp3.json')`
- 动作：`model.motion('<模型根>/<清单给出的相对路径>.motion3.json')`
- ⚠️ **不要假定表情/动作在 `exp/` 子目录里**。Cubism 只规定 `*.exp3.json` / `*.motion3.json`
  的文件格式，放哪由模型作者决定：内置的 `deepseek` 把 44 个表情直接堆在模型根目录、
  8 个动作放在 `motions/`。因此 `/api/live2d/models` 返回的 `exps` / `motions` 是
  **相对模型根目录的路径**（如 `脸红.exp3.json`、`motions/idle.motion3.json`），
  客户端用 `modelAssetUrl()` 逐段编码拼 URL（整串 `encodeURIComponent` 会把 `/` 编成 `%2F`）。
- LLM 输出 `[表情:happy]` 等标签 → `drive()` 解析触发 → `stripTags()` 剥离
- 名称解析三级兜底：语义映射（`EXPRESSION_MAP`）→ 整条路径/文件名精确匹配 → 子串+同义词组模糊匹配；
  返回的仍是相对路径，交给 `modelAssetUrl()` 拼地址
- 清单已知却为空（模型确实没有表情文件，如 `伊蕾娜·默认`）→ 直接放弃，不发注定 404 的请求

回归检查：`npm run check` 里的 `scripts/check-live2d-assets.mjs` 会从 `live2d-video.js`
抠出真实函数体，用真实清单验证 URL 拼接与三级解析（19 项）。

### 7.5 诊断页
`http://localhost:4173/diag-live2d.html` —— 检查三库加载、`PIXI.live2d` 是否挂载。

## 8. 已解决的问题（历史）

| 问题 | 解决方案 |
|---|---|
| 模型太大超出屏幕 | 按模型高度自适应（占屏幕 65%），底部居中 |
| 上传 zip 解压乱码/错乱 | 修复 UTF-8/GBK 编码 + data descriptor 解析 |
| 中文模型名路径问题 | 目录用英文 id，展示名保留中文 |
| `PIXI.live2d` 不存在 | 换用纯 Cubism4 版库（不用混合版） |
| 设置页每个 Tab 都显示角色卡 | Tab 用独立类 `active-panel`（避开 `.hidden` 动画类冲突） |
| 视觉模型设置位置突兀 | 移到独立「视觉」Tab |

### 8.1 上游同步（2026-08，origin/main 5888a75，PR#3 + PR#4）

合并上游最新修复到本地定制版（stash → fast-forward → pop，无冲突）：

| 上游修复 | 说明 |
|---|---|
| `replaceChildren` 兼容垫片 | Chromium < 86 无 `Element.prototype.replaceChildren`，发送图片会在 `consumePendingComposerImage` 抛 TypeError。脚本开头仅在缺失时注入垫片（`web/index.html` + `android-app/www/index.html`） |
| `inset` 长属性兜底 | 旧 WebView（Chromium < 87）不支持 `inset` 简写，fixed 浮层会飘到左上角。改为 `top/right/bottom/left` 长属性 + 新增 `.fixed.inset-0 { top:0; right:0; bottom:0; left:0 }` 兜底规则 |
| ASR 防重入修复 | 桌面 Chrome 连续 `start()` 报 "recognition has already started"。新增 `asrStarting` / `asrRecognitionActive` / `asrSessionId` 三态守卫：startListening 防重入、onend 重启校验会话 id、切回浏览器 ASR 前先 abort/stop 旧会话 |

**本地定制额外兜底**：`web/live2d-video.js` 内联样式中的 `#live2d-call-host`（fixed 全屏通话层）与 `.live2d-canvas-wrap` 同样改用长属性 `top/right/bottom/left`，避免旧 WebView 下视频通话界面飘左上角（上游补丁不覆盖此文件）。

> 注意：`android-app/www/index.html` 已同步上游修复，但**未包含** Live2D/视觉/设置分栏定制（维持前开发者"安卓端纯开源版"的约定）。如需同步定制版到安卓，运行 `node scripts/sync-web.mjs` 前请先确认 vendor 里三个 Live2D 库文件已复制到 `android-app/www/vendor/`。

### 8.2 Live2D 模型调整（通话界面调整框，最终方案）

原 −/＋ 按"高度占屏幕比例"缩放存在两个问题：加载后立即读 `model.height`（未就绪时为 0 → scale 异常）且无法调整比例/位置。曾尝试设置页实时预览 + 滑块方案，但用户更偏好通话界面直接拖拽，最终定为**通话界面调整框**（`web/live2d-video.js`）：

- **「📐 调整」按钮开关**：底部栏新增，点击显示/隐藏调整框（默认隐藏，按钮高亮表示开启）
- **调整框贴合模型实际绘制范围**（非画布留白），每帧跟随；角手柄等比缩放（Shift 自由拉伸）、边手柄单轴、框内拖动移动
- 存储为绝对 PIXI scale + 中心位置比例，**每个模型独立记忆**（`live2d.resize.<模型名>`），自动迁移旧 `live2d.resize` / `live2d.scale`；渲染高度钳制在屏幕高的 2%~600%
- −/＋ 按钮等比微调 ±10%（保留）

### 8.3 修复记录（2026-08 二轮）

| 问题 | 根因 | 修复 |
|---|---|---|
| 每个分栏都有角色设定卡 | 上游旧版遗留 JS `settingsContent.appendChild(characterSettingsSection)` 在页面加载时把角色卡**移出** `tab-character`，落到设置内容末尾；它不是 tab panel（无 display:none），任何 Tab 下都可见 | 删除该移动逻辑，角色卡保留在 `tab-character` 内 |
| 调整框与模型对不齐、模型比例变形 | **`live2d-canvas-wrap` 在 HTML 里是 class，代码却用 `getElementById` 获取 → 永远 null**：`resizeTo` 失效、手动 resize 被跳过，canvas 保持默认 800×600 被 CSS 拉伸（764×485），模型变形、框偏移 | ① 给 div 补上 `id="live2d-canvas-wrap"`；② 渲染循环每帧核对 canvas 内部分辨率与容器 CSS 尺寸，不一致立即 `renderer.resize` 并重算布局；③ 框定位乘 `canvas.clientWidth/canvas.width` 校正因子（双保险） |
| 模型水印隐藏（悠小喵/泠 等） | 模型作者在 VTube Studio 里绑定了数字键（悠小喵=1、泠=0）触发"水印开关"表情（`水印开关.exp3.json` → `Param85=1`），但 pixi-live2d-display 不解析 vtube.json，且其 `expression()` 播放 Add 参数不生效 | **通用水印开关**：`serve.mjs` 模型列表接口新增返回 `exps`/`vtube`；前端自动探测 exp 目录含"水印/watermark"的表情 → 读出参数 Id 与隐藏值 → 键盘 1/0 切换（也可由 AI 用 `[操作:隐藏水印]` 触发），**直接 `setParameterValueById` 驱动参数**（比 expression 可靠），按模型记忆开关状态 |
| Live2D 背景换色无反馈 | 设置页点击色块/自定义颜色/图片/清除背景后直接应用，无任何 UI 反馈，用户不确定是否成功 | 新增 toast 提示（"✅ 背景已应用"等）+ 色块选中高亮（`.l2d-bg-opt.selected` 描边），打开设置时同步当前背景的选中状态 |
| 无法局域网访问 | `serve.mjs` 默认监听 `127.0.0.1` | 默认改为监听 `0.0.0.0`，启动时打印本机局域网 IP 地址（如 `http://<你的局域网IP>:4173`），可用 `HOST`/`PORT` 环境变量覆盖 |
| 浏览器语音识别报错难懂（network / not-allowed 等） | 报错直接透出 Chrome 的英文错误码，用户不知道怎么办 | ① `friendlyAsrError()` 把错误码翻译成可操作提示（network→"需连接在线语音服务，建议阿里云"、not-allowed→"权限/环境限制，建议 localhost/https 或阿里云"、audio-capture/no-speech 等）；② `onerror` 降级扩展：network / service-not-allowed 与权限错误一样，配置了阿里云时自动切"录音+云端最终识别"不打断用户；③ `startListening` 检测非安全上下文（局域网 IP/手机 http 访问）直接友好提示 |
| 语音识别浏览器支持不明确 | Firefox 等无 SpeechRecognition API，提示含糊 | **明确仅支持 Chrome**：`isChromiumBrowser()` 检测（UA + userAgentData 双通道），非 Chromium 浏览器点击麦克风直接提示"语音识别仅支持 Chrome（含 Edge 等 Chromium 内核浏览器）"；Chromium 但 API 缺失提示"未启用/版本过旧，请用最新版 Chrome" |
| 语音报"recognition has already started" | Chrome 的竞态：abort/stop 后立即 start()（或 onend 后立即 start()）会抛该错误；更严重的场景是**旧实例卡死**（某次异常退出 onend 未触发，实例永久停留 running，abort 也救不回），之后每次 start 都失败 | 新增 `safeStartRecognition()` + **实例重建**：捕获 "already started" → `abort()` 重置 → **`rebuildBrowserRecognition()` 丢弃旧实例、`createBrowserRecognition()` 创建全新实例并重新绑定事件** → 300ms 后重试（最多 3 次）。新实例状态干净必定可启动；`onstart` 严格置位、`endListening` 提前清标志；重试耗尽才提示"引擎状态冲突，请稍候/刷新页面" |
| 浏览器语音识别反复不可用 | Chrome 语音识别受网络（依赖 Google 服务）与环境（需 https/localhost）双重限制，用户环境无法稳定使用 | **标注"暂不可用"**：设置 → 语音中"浏览器内置识别"加红色「（暂不可用）」标注，选中时显示警告提示改用阿里云 |
| 阿里云 ASR Key 无处配置/难验证 | ASR 的 Key 之前藏在 TTS 配置里（只有 TTS 选阿里千问才显示），且无法快速验证 Key 有效性 | **ASR 复用 TTS 的 DashScope Key**（阿里云百炼 Key 通用 TTS/ASR，一个 Key 即可）+ 语音输入区显示 Key 状态（✓ 已配置/未配置）+ 「测试」按钮（发 0.5s 静音调 `qwen3-asr-flash`，立即区分：✅ Key 有效 / 401 Key 无效 / 403·404 模型未开通）；接口地址与请求格式经实测正确（`multimodal-generation/generation` + `qwen3-asr-flash` + `X-DashScope-DataInspection: enable`） |
| 新增小米 MiMo ASR 语音识别 | 用户提供接口文档 | 语音输入新增「小米 MiMo」服务商：`https://api.xiaomimimo.com/v1/chat/completions`，OpenAI 兼容 `input_audio`（wav/mp3 data URL），模型 `mimo-v2.5-asr`，认证头 `api-key: $MIMO_API_KEY`（或 Bearer）；设置页可配 Key/接口地址/模型名 + 「测试 MiMo 识别」按钮；`canUseCloudFinalAsr`/转写分发支持 aliyun/mimo 双云端，浏览器不可用时自动降级录音+云端识别 |
| 阿里云 ASR 与 MiMo 配置体验不一致 | 阿里云 ASR 接口地址/模型名硬编码，无法像 MiMo 一样调整 | 阿里百炼同样开放**接口地址 + ASR 模型**配置（默认 `dashscope.aliyuncs.com/.../multimodal-generation/generation` + `qwen3-asr-flash`），Key 仍复用 TTS 的 DashScope Key；测试按钮按配置值请求 |
| 接入阿里云千问（Qwen）对话模型 | 用户提供 OpenAI 兼容示例（`compatible-mode/v1`） | API 格式新增**「阿里云千问 Qwen」预设**：默认地址 `https://dashscope.aliyuncs.com/compatible-mode/v1`、默认模型 `qwen3.8-max`（可改）；走 OpenAI 兼容请求；**对话 API Key 留空时自动复用 TTS/ASR 的 DashScope Key**（阿里云公用 Key）；placeholder 提示"留空则复用语音里已填的 DashScope Key" |
| 设置无"恢复默认"入口 | 用户改乱了配置后只能手动逐个改回 | 设置面板底部新增**「↺ 恢复默认设置」**：确认后对话/语音/视觉等恢复 `DEFAULT_SETTINGS` 并立即持久化；**API Key 全部保留**（安全存储不动）、角色卡不变（角色卡有独立恢复按钮）；表单填充逻辑抽取为 `fillSettingsForm()` 供打开设置/恢复后复用 |
| ASR 报错无详细日志（测试能通、实际报错） | `throwProviderResponseError` 只抛错不打日志，弹窗只显示通用文案，无法排查"测试 0.5s 静音成功、实际录音失败" | 完整日志链路：识别开始（录音时长/wav 大小/模型名）→ `[Provider] 接口返回错误 HTTP x`（含状态码+服务商返回原文+解析 message）→ `[ASR] 云端识别失败`（provider/code/status/message）；跳过原因（录音太短/能量过低带 stats）、返回空/无效文本、网络/超时/CORS 异常均有专门日志；弹窗 UPSTREAM_UNAVAILABLE 时附加服务商返回的具体错误 |
| 进入页面强制填 API Key 的引导 | 首次使用弹窗要求当场输入 DeepSeek Key 才能进入，不友好 | 改为**提示弹窗**：进入页面显示"欢迎使用独立开源版，对话/语音等能力需要先在「设置」中配置对应服务商的 API 密钥"，按钮「我知道了」直接关闭（不再校验 Key、不再显示输入框），点击遮罩也可关闭 |

> 均通过 headless Chrome（CDP）实测：canvas 内部尺寸 = CSS 尺寸（scaleFactor=1，无拉伸）、框与 `model.getBounds()` 误差为 0、拖拽等比/移动/开关/关闭清理全部正常、角色卡只存在于 tab-character、设置页已无预览区块；水印开关：探测到「水印开关」→ Param85 0→1、键盘 1/0 来回切换正常。

### 8.4 第三轮：参数"静默失效"专项修复（2026-09）

这一轮的核心发现：**Cubism 运行时对不存在的参数名不报错**。`setParameterValueById('不存在的参数', 1)` 会被静默接受，`getParameterValueById` 甚至能读回刚写入的值（运行时把未知 id 追加到了参数表末尾），但该参数没有任何绘制绑定，画面完全不变。结果是代码"看起来生效、实际零效果"，且不产生任何日志。同理，**参数存在于模型里 ≠ 被绑定到绘制**——作者可能留下未使用的标准参数。

因此新增了一条硬规则：**情绪参数一律先取模型真实参数表，再按语义槽解析成真实参数名；解析不到就跳过并告警，绝不硬写。**

#### 实测数据（悠小喵，168 个参数）

冻结模型（停 motionManager / eyeBlink / breath / physics / pose，漂移基线压到 0）后逐个设值做像素 diff：

| 参数 | 结果 |
| --- | --- |
| `ParamCheekPuff` / `ParamEyeLOpen` / `ParamEyeROpen` / `ParamMouthOpenY` / `ParamMouthForm` / `ParamEyeBallX` | ✅ 有画面效果（已绑定） |
| `ParamSmile` / `ParamEyeLSmile` / `ParamEyeRSmile` / `ParamCheek` | ❌ 存在但未绑定 |
| `ParamBrowL` / `ParamBrowR` / `ParamBrowLY` / `ParamBrowRY` / `ParamBrowLForm` / `ParamBrowRForm` | ❌ 全部未绑定（该模型没绑眉毛） |
| `ParamEyeLOpening` | ❌ 存在但未绑定（实际用的是 `ParamEyeLOpen`） |
| **`ParamAngleX` / `ParamAngleY`** | ❌ **存在但未绑定** |
| **`ParamAngleX1` / `ParamAngleY1` / `ParamAngleY2`** | ✅ **真正绑定的转头参数** |

#### 修复清单

| 问题 | 根因 | 修复 |
| --- | --- | --- |
| 情绪参数组完全无效（P0） | `EMOTION_PARAM_SETS` 写死 `ParamSmile`/`ParamCheek`/`ParamBrowL`/`ParamEyeLOpening` 等，悠小喵里一个都不存在，且静默不报错 | 新增参数解析层：`refreshModelParamIds()` 取模型真实参数表 → `resolveParamId(slot)` 按语义槽 + 候选名/关键字解析；解析不到跳过并 `console.warn`。`EMOTION_PARAM_SETS` 改为语义槽（`smile`/`cheek`/`browL`/`eyeL`/`mouth`…） |
| `surprised` / `calm` 是死代码（P1） | `driveEmotion` 用 `emo.exp`（表情文件键）去查参数组，两边词表不是一套：惊讶 exp 是 `dizzy` → 错套"晕"的参数组（半闭眼，语义相反）；平静 exp 是 `smile` → 查不到 → 什么都不加 | 改为按**情绪键**（`happy`/`sad`/`surprised`/`calm`…）查表；`smile` 槽候选顺序调整为 `ParamSmile → ParamMouthForm → ParamEyeLSmile/RSmile`（嘴型是"笑"最直接的可见线索，且该模型只绑了 `ParamMouthForm`） |
| **鼠标"头部追踪"其实一直没生效** | 只驱动 `ParamAngleX`/`ParamAngleY`，而该模型把转头绑在 `ParamAngleX1`/`ParamAngleY1` 上（标准名留空） | 新增 `headX`/`headY`/`eyeBallX`/`eyeBallY` 语义槽，按 `resolveParamIds()` 解析真实参数；`close()` 复位同样按解析结果来。**候选范围后来在 8.6 收窄**（排除 `ParamAngleY2`，它会推动整个模型） |
| 表情文件解析不到时静默 404 | `resolveExpressionFile` 匹配不到仍返回原名 → fetch 必然 404 → 回退 `model.expression()` 也失败 | 匹配不到时返回空串、复位上一个表情的参数并告警，不再发无效请求 |
| ASR 网络提示看不到 | network 分支先写 `statusText` 再调 `updateUI()`，被 `statusMap[voiceState]` 覆盖回"正在聆听..." | 调整顺序：先 `updateUI()` 再写提示 |
| `[操作:静音]` 等于没静音 | `aiVoiceMuted` 只停了一下嘴型，主应用 TTS 照常播放；且它引用的 `#live2d-mute-btn` 早已移除 | 导出 `isVoiceMuted()`；主应用在 `startVoicePlaybackOnce` 前检查，静音时跳过播放但**照常提交并保存文字回复**；`speakStart()` 也加了静音守卫 |
| 首次打开时自定义背景被盖住 | `applyBackground()` 在 `app` 创建**之前**调用，渲染器还是不透明底色，把 CSS 背景整个盖住 | 把 `applyBackground()` 移到 app 创建之后（重复调用幂等） |
| 关闭通话后残留音量 | `close()` 没有复位 `voiceEnergy`，下次通话嘴型一开始就是张着的 | `close()` 中补 `voiceEnergy = null` |
| 调整框坐标系混用 | `resizeCenter()` 返回 canvas 内部像素，而鼠标事件是 CSS 像素 | 统一乘 `canvasPixelScale()` 换算 |
| 过期文案 | 提示"点右上角 ⬆️ 上传 zip"，但通话界面早已没有上传按钮 | 改为「设置 → Live2D」 |

#### 验证方式（可复现）

> 关键方法论：**验证 Live2D 参数是否真的生效，必须先停掉 motionManager / eyeBlink / breath / physics / pose，把漂移基线压到 0 再做像素 diff**。否则待机动画本身会产生约 15000 像素的噪声，任何结论都不可信（曾据此误判"参数有效"）。

修复后实测：左右移动鼠标 → `ParamAngleX1` 从 −17.9 变到 +17.8，画面变化 5929 像素（修复前为 0）；`[情绪:平静]` → `ParamMouthForm = 0.3`，画面变化 9 像素（修复前为 0）。

### 8.5 局域网访问密码（新增）

服务默认监听 `0.0.0.0`（手机/平板可访问），但 Agent 文件接口没有鉴权、权限模式又由请求方自己传参决定 —— 不设访问控制时，同一局域网内任何设备都能打开应用、甚至读写本机文件。

规则：

- **本机（127.0.0.1）自动视为管理员**：免密进入，且可修改访问密码 —— 这也是忘记密码时的找回入口
- **局域网设备必须登录**：登录后种 HttpOnly Cookie 会话（7 天）
- 首次启动生成 12 位随机密码并在 cmd 窗口醒目打印；用户改过密码后只存 **scrypt 加盐哈希**，控制台不再显示明文
- 登录失败 5 次锁定 60 秒
- **CSRF 防护**：非 GET 请求校验 `Origin` 与 `Host` 同源，否则拒绝（防止任意网页在浏览器里悄悄改本机密码）
- **隐藏文件禁止静态访问**：任何点文件（`.env`、`.git` 等）一律 403。这条规则历史上一并挡住了访问密码文件
- 改密码后清空所有会话，局域网设备需重新登录
- 忘记密码：本机打开 `http://127.0.0.1:4173` → 设置 → 高级 → 访问密码；或 `node web/serve.mjs --reset-password` 重启

凭据文件位于 **`data/auth.json`**，和 `store.json`、证书、日志同处一个目录。
2026-09 之前它在 `web/.local-auth.json` —— 运行时私密数据混在源码目录里，只靠
`.gitignore` 单独排除 + 静态服务的隐藏文件规则兜住，位置本身就不对。
现在 `data/` 整目录既已 gitignore，又刻意位于静态服务根目录**之外**，等于少了两处要靠"规则写对"的地方。
老文件在首次启动时自动迁移（`loadAuthFile` 里做，成功打印一行日志，失败则退回旧位置继续用，
绝不让用户被锁在门外）；`--reset-password` 会同时清除新旧两个位置，否则迁移逻辑会把老文件搬回来。

> `.gitignore` 里那条 `web/.local-auth.json` **刻意保留**：升级前遗留在 `web/` 的老文件如果不排除，
> 没跑过新版服务的人执行 `git add .` 就会把访问密码提交上去。

实测（curl）：局域网访问首页 302→`/login`、未登录 API 401、错误密码 401、正确密码 200+Set-Cookie、局域网改密码 403、本机改密码 200、旧密码失效、改密码后旧会话失效、伪造 Origin 403、点文件 403。
迁移实测：迁移前用原密码可登录 → 启动一次后 `web/.local-auth.json` 消失、`data/auth.json` 内容逐字节一致 → **用原密码仍登录成功**（`{"ok":true}`）→ 再次启动不重复迁移、不报错。

界面侧也做了端到端验证（设置 → 高级 → 访问密码）：面板能识别本机管理员并显示当前初始密码；两次输入不一致、密码少于 6 位都会被拦下并给出提示；正常修改后弹窗确认、状态文案变为"已由你自行设置"、输入框清空；随后用 curl 确认**旧密码失效、界面里刚设的新密码可在局域网登录**，且磁盘上只留下 `salt` + `hash`，没有明文。

> ⚠️ 局域网是明文 HTTP，密码在传输中不加密，建议仅在家庭/可信局域网使用。

**登录页配色**：与应用内**实际渲染值**（pixso 主题，非 `:root` 里那套）保持一致——橄榄绿画布 `#a8b974`、白色面板（`26px` 圆角 + `#d9c7c0` 描边 + `0 26px 70px rgba(58,39,29,.24)` 投影）、米色输入框 `#f8f5f2`、绿色药丸按钮 `#a7bb6c`、棕色文字 `#4c3123`、橙色小标 `#ff7d4d`。样式参数直接对齐应用内的 `.modal-panel` / `.modal-input` / `.btn-primary`，并加了一个和欢迎弹窗同款的橙色 eyebrow 小标。

> 踩坑记录：一开始抄的是 `:root` 里的 `--color-primary: #ec4899` 那套粉紫变量，但应用在运行时会用 pixso 主题覆盖它们，实际渲染出来是暖米色 + 棕 + 绿。**要取配色必须读渲染后的 computed style，不能只看 CSS 变量定义。**

**控制台输出精简**：`启动.bat` 的横幅（Server / Local / LAN / Press Ctrl+C）与 `serve.mjs` 里的接口清单、诊断页地址等提示行全部移除，只保留必要信息：

```
  本机访问:   http://127.0.0.1:4173   （免密码）
  局域网访问: http://<你的局域网IP>:4173   （需要访问密码）

  访问密码: xxxxxxxxxxxx
  可在「设置 → 高级 → 访问密码」改成自己的，改完这里就不再显示。
```

### 8.6 鼠标跟随：修正"整体位移"并加开关（2026-09）

修好头部追踪后暴露出的新问题：鼠标一动，**整个模型会朝鼠标方向下沉前倾**，看着像扑过来，很突兀。

逐参数看变化区域后定位到元凶：

| 参数 | 变化像素 | 变化区域 |
| --- | --- | --- |
| `ParamAngleX1` | 6075 | 宽 128 × 高 417（局部） |
| `ParamAngleY1` | 4896 | 宽 132 × 高 417（局部） |
| **`ParamAngleY2`** | **29343** | **宽 158 × 高 525（连水印层一起动）** |

`ParamAngleY2` 并不是"低头/抬头"，而是把整个模型（含作者加的水印层）一起推移——单独设它就复现了"靠过来"的画面。

修复：`headX` / `headY` 的候选只保留 **标准名** 和 **1 号变体**，不再驱动 `2` 号变体（`ParamAngleX2` / `ParamAngleY2`）。带 `2` 的变体在不同模型里可能是整体位移或整体旋转，语义不可靠，宁可不驱动。

同时新增 **鼠标跟随开关**（设置 → Live2D → 鼠标跟随），可整体关闭；关闭时立即把角度参数复位，模型保持不动。状态存在 `localStorage` 的 `live2d.mouseFollow`。

修复后实测：`ParamAngleY2` 恒为 0；鼠标左右移动时 `ParamAngleX1` 从 −17.9 → +17.9，画面变化 5930 像素、变化区域高度从 519 降到 417（只剩局部）；关闭开关后移动鼠标，所有角度参数保持 0，模型完全不动。

### 8.7 标签解析：情绪标签会吞掉操作标签（重要修复）

`live2d-video.js` 的 `driveText()` 里原本是：

```js
const emo = t.match(/\[情绪[:：]\s*([^\]]+)\]/);
if (emo) {
  driveEmotion(emo[1].trim());
  return;   // ← 问题在这里
}
// 后面的 [位置] / [大小] / [背景] / [操作] 全部在这行下面
```

那个 `return` 的本意只是"有显式情绪就别再做自动推断"，但它把**后面所有标签一起跳过了**。而系统提示词明确要求 AI **每条回复都带一个情绪标签**（`LIVE2D_TAG_GUIDE`：「每条回复根据内容带 1 个情绪/表情标签」）。

后果：`[操作:…]` 标签（文件操作、打开/结束视频通话、整理记忆、静音、隐藏水印）**在实战中几乎永远不会被执行**；`[位置]` / `[大小]` / `[背景]` 同样失效。更糟的是 `stripTags()` 会把标签从显示文本里剥掉，用户看不到标签被忽略，AI 还可能顺着说"已经帮你打开了"——正好踩中提示词里明令禁止的"编造操作结果"。

> 补充：`[任务:…]` 不受影响，它由 `index.html` 的 `extractAndCreateTasks()` 独立解析。

**修复**：把"跳过自动推断"改成只用一个布尔标志控制，不再提前返回；顺带支持**一条回复里出现多个 `[操作:…]`**（例如同时"打开视频通话"+"隐藏水印"）；未知情绪词也不再整条跳过，而是回退到自动推断。

修复后实测（对 `agentActions` 打桩记录调用，7/7 通过）：

| 场景 | 结果 |
| --- | --- |
| `[情绪:开心]…[操作:打开视频通话]` | ✅ 操作不再被吞 |
| `[操作:打开视频通话][操作:整理记忆]` | ✅ 两个操作都执行 |
| `[情绪:难过][操作:列出文件 D:\笔记]` | ✅ 参数完整传递 |
| `[情绪:平静]`（只有情绪） | ✅ 不误触发操作 |
| `[情绪:开心][背景:星空]` | ✅ 背景生效 |
| 无情绪标签时 | ✅ 原路径未改坏 |
| `[情绪:莫名其妙]…`（未知情绪） | ✅ 回退推断且操作照常执行 |

### 8.8 鼠标跟随失效：关闭通话后再打开丢监听（2026-09）

**现象**：换了新模型后"无法跟随鼠标移动"，看起来像是模型不支持头部追踪。

**排查结论（先说结论：模型没问题，代码有 bug）**：

1. 新模型（`model_mtxxm2cp_mii7` / LSS）的 `ParamAngleX` / `ParamAngleY` **是真正绑定的**，且效果比悠小喵更强——左右各 ±20° 的变化量下，LSS 改变 28791 像素、悠小喵只改 4178 像素。
2. 应用驱动参数也正常：鼠标从最左到最右，`ParamAngleX` 从 −17.56 走到 +17.54。
3. 真正的问题是：**鼠标追踪的 `pointermove` 监听器只在 `buildUI()` 里挂载，而 `buildUI()` 只在页面首次打开通话时执行一次**。`close()` 会把监听器移除并把 `mouseHandler` 置空，于是：

   > 打开通话（正常）→ 关闭通话 → 再次打开通话 → 监听器永远不会重新挂载 → **鼠标跟随永久失效**，直到刷新页面。

   这正好命中"关掉通话去「设置 → Live2D」上传新模型，然后重新打开通话"这个最常见的操作路径，所以很容易被误判成"新模型不支持跟随"。

**修复**：把挂载逻辑抽成幂等的 `ensureMouseTracking()`，在每次 `open()` 时调用；顺带做了两处改进——

- 监听目标从 `#live2d-canvas-wrap` 换成整个通话层 `#live2d-stage`：鼠标移到顶栏 / 底栏附近时也能继续跟随；
- 拖拽调整框时（`dragState` 非空）跳过，避免一边调位置一边转头。

**顺带的体验修复**：鼠标跟随关闭时，通话界面会显示一条可点击的提示 **「🖱️ 鼠标跟随已关闭 · 点此开启」**（`#live2d-follow-hint`）。以前这个开关藏在「设置 → Live2D」里，关掉后模型完全不动，很容易被误认为模型坏了。

**实测（headless Chrome + 真实鼠标事件，非合成事件）**：

| 场景 | `ParamAngleX`（鼠标左 → 右） | 结果 |
| --- | --- | --- |
| 第 1 次打开通话（悠小喵） | −17.55 → +17.49 | ✅ |
| 关闭 → 再打开（修复前） | 0 → 0 | ❌ 复现 bug |
| 关闭 → 再打开（修复后） | −17.55 → +17.53 | ✅ |
| 再切换到 LSS（新模型） | −17.50 → +17.52 | ✅ |
| 鼠标跟随 = 关 | 0 → 0 | 预期行为（界面显示提示条） |

> ⚠️ 排查时的一个大坑：**「欢迎使用独立开源版」引导弹窗（`#announcementOverlay`）是全屏浮层，只要它还开着，真实鼠标事件就全被它吃掉**，`pointermove` 永远到不了通话层（合成事件 `dispatchEvent` 会绕过命中测试，所以用合成事件测会得到"正常"的假象）。用自动化测鼠标跟随时，必须先关掉弹窗、并且用真实鼠标事件。

### 附：新增参数绑定诊断页 `web/diag-param-bind.html`

排查上面这类"参数看起来生效、实际没反应"的问题，新增了一个诊断页（与 `diag-live2d.html` / `diag-asr.html` 同一套思路）：

- 逐个参数设值，对比**所有 drawable 的骨骼顶点坐标最大位移**，判定"已绑定 / 存在但未绑定"；
- 自动列出鼠标跟随依赖的语义槽（`headX` / `headY` / `eyeBallX` / `eyeBallY` / `bodyX` / `bodyY`）在该模型上的解析结果；
- 并给出每个参数的**量程**与「幅度 ×N 到顶」标注 —— 参数值会被 Cubism 按模型自身 min/max 静默截断，这列直接告诉你「设置 → Live2D → 跟随幅度」还能放大多少倍（见 8.10）；
- 比像素 diff 更快更稳，且**不需要冻结 motionManager / eyeBlink / breath / physics / pose**——因为它是直接对比顶点、绕过了待机动画的噪声。

用法：`node web/serve.mjs` → 打开 `http://127.0.0.1:4173/diag-param-bind.html` → 选模型 → 「开始扫描」。

> 判定阈值：最大顶点位移 > 0.01 视为已绑定。这个方法是当时定位 `ParamAngleX` / `ParamAngleY` 是否绑定、以及确认 `ParamAngleY2` 会推动整个模型时用的同款手段。

### 8.9 安全与稳定性加固（2026-09，一轮完整代码审计后的修复）

对 `web/serve.mjs`、`web/live2d-video.js`、`web/index.html` 做了一遍逐行审计，按严重程度修复。**每一项都做了回归验证**（服务端 39 项 + 前端 25 项，全部通过）。

#### A. 高危：三个可被外部利用的口子

| 问题 | 触发条件 | 修复 |
| --- | --- | --- |
| **zip 解压没有"解压后体积"上限** | `MAX_UPLOAD(200MB)` 只限制压缩包本身。实测 119.8 KB 的 zip（120MB 全零）→ 落盘 120.0 MB，**放大 1025×**，`inflateRawSync` 还会在内存里整块展开 | 新增 `MAX_EXTRACTED(512MB)` / `MAX_ENTRY_SIZE(256MB)`，`inflateRawSync(..., { maxOutputLength })` + 累加校验，超限直接拒绝 |
| **`/api/agent/read`、`/api/agent/ls` 是 GET，绕过 CSRF** | 第三方网页放 `<img src="…/api/agent/read?path=…&permission=computer">`，请求由**本机浏览器**发出 → 被判为"本机管理员" → `permission=computer`（**由请求方自己传参**）生效 → 任意本地文件被读取 | ① 对 `/api/*` **不区分方法**做同站校验：优先看 `Sec-Fetch-Site`，缺失时退回比对 `Origin` 与 `Host`；② 非浏览器客户端（curl / 原生插件）不带这些头，照旧放行 |
| **Host 头未校验 → DNS rebinding 可冒充本机管理员** | 攻击者域名解析到 `127.0.0.1`，浏览器请求同时满足 `remoteAddress=127.0.0.1`（管理员）与 `Origin.host === Host`（旧 CSRF 校验用 Host 做基准）→ 拿到任意文件读写 | 新增 **Host 白名单**：只放行 `localhost` / `127.0.0.1` / `::1` / 本机网卡地址（v4+v6）/ 主机名；可用 `ALLOWED_HOSTS=你的域名` 追加。启动时打印提示 |

> 审计时靠"看代码像漏洞"的两条，实测后**排除了**：zip 目录穿越（`../` 会被跳过）、客户端中途 RST 断连（6 次实测服务不崩）。已实测确认的问题才写进来。

#### B. 中危：可靠性与副作用

| 问题 | 说明 | 修复 |
| --- | --- | --- |
| **data descriptor 靠扫签名定位 → 静默损坏数据** | `flags & 0x8` 时用 `for (…) if (字节 === 50 4B 07 08)` 找描述符。实测：内容含该 4 字节的文件，54 字节只解出 5 字节；更糟的是偏移错位后**后续条目被整体丢弃**（第二个条目 `.model3.json` 直接消失，接口却仍返回 `ok`）。而 PowerShell `Compress-Archive` 生成的 zip 正是这种 | **改为以中央目录（EOCD + CD）为权威元数据**读取名称/压缩大小/数据偏移，不再猜。没有可用中央目录且条目带 bit3 时，直接报"请用标准工具重新打包"，而不是猜 |
| **危险类型可落盘并被同源执行** | 上传的 zip 里若含 `.html/.svg/.js`，会被静态服务按 `text/html` 返回 → 同源 XSS → 可读 `sessionStorage` 里的 API Key、调用 Agent 接口 | ① 解压时**跳过** `.html/.js/.svg/.exe` 等扩展名并在响应里回报 `blocked`；② `live2d/models/` 下**一律以 `application/octet-stream`** 返回；③ 全站加 `X-Content-Type-Options: nosniff`；④ 上传界面提示"已跳过 N 个不允许的文件" |
| **`computer` 模式下 AI 可无声覆盖任意文件** | 提示词被图片文字/长期记忆/第三方模型输出污染后，一次模型输出就能写任意路径 | 客户端在 `permission === 'computer'` 写文件前弹确认，列出目标路径（同一路径只问一次）；把权限改回"限制模式"可彻底关闭 |
| **`agentWrite` 请求体无上限** | `readJsonBody` 有 64KB 上限，`agentWrite` 自己读全部 chunk | 新增 `AGENT_WRITE_LIMIT = 1MB`，超限 413 |
| **密码校验用同步 `scryptSync`** | 每次登录尝试阻塞事件循环数十毫秒，可被并发请求拖死服务 | 改用 `promisify(scrypt)` 异步；`sessions` / `loginFails` 增加 10 分钟定期清理（`unref`），登录失败计数在锁定期过后重新计数 |
| **静态文件流没有 error 处理** | `createReadStream(...).pipe(response)` 不转发错误 | 加 `stream.on('error')` + `response.on('close')` 释放句柄；`stat` 改 `lstat` 拒绝符号链接；响应头已发送时不再二次 `writeHead` |

#### C. 前端（`live2d-video.js` / `index.html`）

| 问题 | 修复 |
| --- | --- |
| 同类标签只有第一个生效（`[表情:a][表情:b]` → 只有 a；`[操作:]` 在 8.7 已改全局，其它标签没跟上） | 统一抽出 `lastTagValue()`，**同类标签最后一个生效**，位置/大小/背景同步统一 |
| `setExpression` 是 async 却被"即发即忘"调用 → 连续回复时 `currentExpParams` 互相覆盖（表情切了脸还停在上一张） | 加自增 `expressionSeq`，每次 await 后校验"我是不是最后一次"，不是就直接丢弃结果 |
| `loadModel()` 无并发保护 → 快速切换模型会创建多个实例，旧的没 `destroy()`（实测创建 7 个） | 加 `modelLoadSeq`，过期结果直接 `destroy()` 丢弃。回归实测：创建 7 个、舞台上始终只有 1 个 |
| `autoStart:true` 的 PIXI 已渲染，自建 rAF 里又 `app.ticker.update()` → 每帧重复渲染一遍（注：不是 2 倍速，模型动画由 PIXI 自己的 ticker 驱动，只多了一次无效渲染） | 去掉手动 `ticker.update()`；`close()` 里 `app.ticker.stop()`、`open()` 里 `start()`，隐藏时不再空转渲染 |
| `[背景:url(https://evil/…)]` 由 AI 可控 → 写进 CSS 就是外链信标 | 新增 `sanitizeBackground()`：只放行 data: 图片 / 本服务内相对路径 / 渐变 / 纯色，禁止 `;` `{}` `expression(` `javascript:` 等 |
| 静音完全没有界面反馈（操作的是早已移除的 `#live2d-mute-btn`） | 新增 `#live2d-mute-hint` 提示条 |
| 表情/动作名直接拼进 URL（`encodeURIComponent` 不编码 `.`，Android 走本地文件系统时 `[表情:../../x]` 可穿越） | 新增 `sanitizeAssetName()`：含 `/` `\` `..` 或 `.-` 开头一律拒绝（实测 `setExpression('../../serve.mjs')` 直接返回 false，不发请求） |
| `fuzzyMatchFileName` 双向 `includes`：单字符 target 会命中任意文件 | 要求 target 与文件名长度均 ≥ 2 |
| **模型完全没有 exp 目录时，表情标签必然 404**（8.4 只修了"列表非空但匹配不到"的情况；回归测试抓到 1 次真实的 404 请求） | 新增 `modelAssetListKnown` 区分"清单未知"与"确定为空"：确定为空时直接忽略并告警，不再发注定 404 的请求。修复后 404 归零 |
| `escapeHtml` 不转义单引号，多处把 id 拼进 `onclick="…'${id}'"` | `escapeHtml` 补 `'` 与反引号；新增 `safeAttrId()`（白名单剔除）用于内联 `onclick` 里的 id —— 注意**不能**靠 `&#39;`，HTML 解析器会先解码实体再交给 JS |

#### D. 启动体验

`server.on('error')` 现在会给出可读提示（端口占用 → 提示换端口，退出码 1），不再抛裸栈。

#### E. 回归验证

- **服务端 39 项**：Host 白名单（伪造 Host 403 / 本机与网卡地址放行）、跨站 GET 与 Origin 拦截、静态服务（nosniff、符号链接、隐藏文件、models 下强制二进制）、zip 正常包/描述符包/危险类型/穿越/炸弹、请求体上限、鉴权（401/429）、中途断连稳定性。
- **前端 25 项**：模型加载、渲染与待机动画、鼠标跟随（含关闭再打开回归）、标签驱动（情绪不吞操作、多个操作标签）、背景消毒（恶意外链被丢弃、分号注入被拒）、静音提示、同类标签、表情名穿越被拒、调整框、并发切换模型无泄漏、无 404、无 JS 异常。

> 测试方法备忘：**验证 WebGL 画布内容不能用 2D canvas `drawImage` 读取**（PIXI 默认 `preserveDrawingBuffer: false`，读到的永远是空白），必须用页面截图比对。

### 8.10 鼠标跟随幅度可调（跟随幅度滑块 + 解锁更高倍率，2026-09）

**需求**：同一个"转头"参数在不同模型上的视觉幅度差别很大 —— 同样的设置有的模型几乎不动、有的一下转到底。用户需要在「设置 → Live2D」里手动拉平。

**UI**：滑块 `0-10`（标准范围，`10 = ×1.0` 原始幅度）+ 右侧数字框可填更大数字解锁更高倍率（上限 `100 = ×10`）。数字框失焦/回车会把实际生效值（已截断）回写。倍率即时写入 `localStorage['live2d.mouseFollowScale']`，默认 `10`（老用户无此配置时保持原幅度）。

**关键发现（决定了实现方式）**：一开始只做了"倍率乘数"，实测发现**倍率超过约 ×1.5 就完全失效**。原因是参数值会被 Cubism 按模型自身量程截断：

| 参数 | 量程 | 是否绑定到绘制 |
|---|---|---|
| LSS `ParamAngleX` | ±30 | ✅ 已绑定 |
| LSS `ParamBodyAngleX/Y/Z` | ±10 | ✅ 全部绑定 |
| LSS `ParamAngleZ` | ±30 | ✅ 已绑定 |
| LSS `ParamEyeBallX/Y` | ±1 | ❌ **未绑定（设了没反应）** |
| 悠小喵 `ParamAngleX` | ±30 | ❌ 未绑定 |
| 悠小喵 `ParamAngleX1` | ±30 | ✅ 已绑定 |
| 悠小喵 `ParamBodyAngle*` | ±10 | 存在但（单参数扫描时）位移为 0 |
| 悠小喵 `ParamEyeBallX/Y` | ±1 | ❌ **未绑定** |

头部基础驱动值是 `20`，而量程上限是 `30` → **×1.0 就用掉了三分之二，×1.5 到顶**。所以单纯放大倍率在头部上是空转。

**实现**：把"解锁更高倍率"接到**身体跟随**上，让高倍率真的产生变化：

- 头部：`x * 20 * k`、`y * 16 * k`（`k = 倍率/10`）—— ×1.0 与旧行为完全一致，之后到量程上限为止
- 眼球：基准值从 `1.3` 降到 `0.8`。原来 `1.3` 超出量程 `±1`，导致眼睛一动就顶到边界、永远贴着边；现在全偏转时到 `0.8`，不再截断。倍率按 `√k` 缓动
- **身体**：`bodyK = clamp((k-1)/2, 0, 1)`，驱动 `ParamBodyAngleX/Y` 到各自量程上限。**×1.0 时身体完全不动（保证向后兼容），×3.0 时身体转到该模型的上限**
- 新增 `getFollowAmpInfo()`：返回 `{ loaded, headSaturateAt, bodyParamCount }`。设置面板在倍率 > ×1.0 时直接显示 **「头部上限 ×1.5，更高倍率由身体跟随补足」**，免得用户以为"数字填了没用"是 bug

**实测（headless Chrome + 页面截图比对，设置弹窗必须先关闭）**：

| 倍率 | LSS `ParamAngleX` | LSS 身体 | LSS 像素变化 | 悠小喵 像素变化 |
|---|---|---|---|---|
| ×0.0 | 0 | 0 | — | — |
| ×1.0 | 19.14 | 0 | 44 845 px | 5 899 px |
| ×2.0 | **30（到顶）** | 4.79 | 58 058 px | 16 319 px |
| ×3.0 | 30 | 9.57（满） | **63 271 px** | **17 296 px** |
| ×6.0 | 30 | 9.57 | 63 271（平台） | 17 296（平台） |

两个模型的像素变化都随倍率单调增大，×3.0 起进入平台（身体已满量程）；变化区域始终是局部（悠小喵 128×417 → 153×417），**没有出现 8.6 那种"整个模型位移"的回归**。UI 侧 9 项 + 实效 6 项，共 10/10 通过。

> ⚠️ 测这类功能的坑：**设置弹窗（`#settingsOverlay`）盖住画布时，像素对比会全部得到 0**，看起来像"参数完全没生效"。必须 `document.getElementById('closeSettings').click()` 之后再截图。第一轮就是这么误判的。

**注意**：`ParamEyeBallX/Y` 在两个内置模型上都未绑定，所以"视线跟随"实际不生效 —— 这是模型作者没绑，不是代码问题。`diag-param-bind.html` 可以扫任意模型的绑定情况。

### 8.11 模型重命名 + 修复"鼠标静止时改倍率不生效"（2026-09）

**需求**：上传后的模型在列表里只显示 `model_mty0qqda_eyr8` 这种服务端生成的 id，用户分不清哪个是哪个。

**做法：目录名即显示名，重命名就真改文件夹。**

一开始担心"目录名是 URL 的一部分，改了会出事"，所以先做了一版"目录名保持 id + 另存 `.names.json` 显示名映射表"。但那多了一层状态，用户看到的文件夹名和界面里的名字对不上，反而更难排查。实测确认了那三个担心其实都能解决：

| 担心 | 实际情况 |
|---|---|
| 每模型设置失联（`localStorage['live2d.resize.<name>']`、`live2d.wm.<name>`） | 改名后**客户端主动迁移**这两个 key，设置不丢 |
| 模型正在通话中加载会 404 | 已加载的模型资源在内存里，画面不受影响；切换/重开走新路径，正常 |
| 中文/空格进路径会出问题 | `serve.mjs` 第 804 行本来就 `decodeURIComponent(url.pathname)`，客户端也用 `encodeURIComponent(name)` 拼 URL；而且模型内部文件名（`悠小喵.moc3`、`悠小喵.8192/texture_00.png`）一直是中文，早就跑通了 |

于是删掉 `.names.json`，改成：

- **上传时目录名直接取 zip 里的文件夹名**（拿不到才退回 `model_<ts>_<rand>`）—— 这样新传的模型一进来就不是 `model_xxx` 了
- `POST /api/live2d/rename { name, displayName }` → `fs.rename()` 真改目录，返回 `{ ok, name, newName }`
- **重名自动加序号**（`伊蕾娜 · 默认装 (2)`）—— 否则又回到"两个都叫同一个名字"
- 名字清洗 `sanitizeModelDirName()`：去控制字符、`\ / : * ? " < > |` → `_`、压缩空白、剥掉首尾的点与空格（Windows 不允许尾点/尾空格）、拒绝 `.`/`..`/Windows 保留设备名（`CON`/`NUL`/`COM1`…）、限长 60
- 客户端 `applyRenamedModel(old, new)`：迁移 `live2d.resize.*` 与 `live2d.wm.*`、把 `selectedModel` 指到新名字、刷新通话内下拉
- 删除模型时同时清掉这两个 key（原来只清了 resize，水印状态会残留）

UI：设置 → Live2D 的模型下拉旁新增「✏️ 重命名」，用 `showCustomModal({ input: true })` 弹输入框并预填当前名字。

**顺带修了用户实际遇到的报错**：服务端是旧版时 `/api/live2d/rename` 会落到静态处理器返回纯文本 `Not found`，前端 `res.json()` 抛出 `Unexpected token 'N', "Not found" is not valid JSON` —— 完全看不懂。新增 `readJsonSafe()`，非 JSON 响应给出**「服务端没有这个接口（HTTP 404）。多半是 serve.mjs 改了但服务没重启 —— 到 cmd 窗口按 Ctrl+C，再重新双击「启动.bat」」**。上传/删除/列表也统一走它。

**实测（curl 15 项 + UI 16 项全过）**：

- 服务端：中文名改名成功且**磁盘上真的改名** ✅、静态访问中文目录下的 model3.json 返回 200 且内容正确 ✅、重名加 `(2)` ✅、非法字符 `a/b\c:d*e?f"g<h>i|j` → `a_b_c_d_e_f_g_h_i_j` ✅、只有点/空格 → 400 ✅、空名 → 400 ✅、`CON` → 400 ✅、首尾点被剥离 ✅、超长截断到 60 ✅、同名 → `unchanged:true` ✅、路径穿越 → 404 ✅、不存在 → 404 ✅
- 上传：zip 内文件夹名 `测试用模型` → 目录名就是 `测试用模型` ✅、再传一次 → `测试用模型 (2)` ✅
- UI：下拉更新 ✅、选中项指向新名 ✅、`live2d.resize.*` / `live2d.wm.*` 双双迁移且旧 key 清空 ✅、服务端列表同步 ✅、**改名后的模型在通话里真的加载出来**（标签=新名字、尺寸 4359×8500、168 个参数）✅、能改回原名 ✅

> 踩坑：第一版 UI 测试只断言了"有模型加载出来"，但默认加载的是列表里第一个模型 —— 改名后的那个根本没被加载，测试给了假阳性。改成**断言通话内名字标签等于新名字**才真正验到。测"改名/换模型"这类功能时，一定要断言"加载的是哪一个"，不能只断言"加载成功了"。

#### 顺带修掉一个 bug：鼠标静止时改倍率不生效

写完 8.10 后复查发现：`mouseTrackTick` 是**插值到稳定就 `clearInterval`** 的，而 `setMouseFollowScale` 只在**归零**时才主动复位参数。所以「鼠标不动、只调倍率」时模型会一直停在旧倍率的姿态上。

实测确认：倍率 ×3.0 且鼠标静止 → 头部 30 / 身体 9.58；改回 ×1.0 **不动鼠标** → 仍然是 30 / 9.58（应该变成 19.14 / 0）。

修复：把驱动部分抽成 `applyFollowDrive()`，`setMouseFollowScale` 在非零时立即调用一次重算。修后同样场景 → 头部 19.16 / 身体 0 ✅。

### 8.12 AI 手机操作（2026-09）

**需求**：让 AI 能看见并操作手机界面 —— 点击、滑动、输入、读控件树、截图。这是本改版跨度最大的一轮，横跨 Web 前端与安卓原生。

本节只记录交付结论与踩坑（原独立的手机 Agent 设计文档已并入本文档）。

#### 一、结论先行：成本比预期低很多

上游 1.3.x 的 `android-app` 里**已经把最难的两块地基打好了**，而且是同项目血缘（MIT），可以合法直接取用：

| 上游文件 | 规模 | 对我们的价值 |
|---|---|---|
| `ElainaPetService.java` | 384 行 | 悬浮窗 + 前台服务 —— Agent 浮层与保活的基础 |
| `ElainaPetPlugin.java` | 219 行 | 能力开关 + 全套引导（无障碍/悬浮窗/电池/厂商白名单） |
| `ElainaAccessibilityService.java` | 146 行 | 读屏骨架（只读，需扩展成可操作） |
| `ElainaPetBridge.java` | 233 行 | 浮层 WebView 的 JS 桥 |
| `FileBridgePlugin.java` | 409 行 | SAF 文件选择/导出/导入 |

**但有一条必须澄清**：上游对 Shizuku 的用法**很浅**。`requestShizuku()` 授权后**只是打开无障碍设置页**，`enableKeepAlive()` 是**一个字都没有的空方法**（只留注释）。依赖、Provider、状态查询、授权请求是现成的；**真正"用 Shizuku 执行命令"必须自己写**。别被方法名骗了。

#### 二、架构：一套工具名 + 多个可切换执行器

```
                    ┌── 命令族（执行 shell 命令）
后端实现 ───────────┤     ├── KSU 模块
                    │     ├── su（root）
                    │     └── Shizuku（UID 2000）
                    │
                    └── 无障碍（调原生 API，不执行命令）
                          └── dumpUiTree / dispatchGesture / ACTION_SET_TEXT
```

命令族的三个执行器**共用同一套命令字符串**，只是换个地方执行 —— 所以命令构造层只写一份（`PhoneCommand.java`）：

| 动作 | 命令（三个后端通用） |
|---|---|
| 点击 | `input tap x y` |
| 滑动 | `input swipe x1 y1 x2 y2 300` |
| 按键 | `input keyevent 4` |
| 控件树 | `uiautomator dump <文件>` |
| 截图 | `screencap -p <文件>` |
| 启动应用 | `monkey -p 包名 -c android.intent.category.LAUNCHER 1` |

**探测优先级**：模块 → Root → Shizuku → 无障碍。**但用户手选的那一个优先**，且它不可用时**明确报错，不偷偷换一个** —— 否则用户以为在走 root、实际走的是无障碍，出了问题无从排查。

**唯一的 per-tool 例外是中文输入**：命令族的 `input text` 只认 ASCII，只要无障碍服务开着，中文输入一律改走无障碍的 `ACTION_SET_TEXT`，并在结果里说明换了路。

#### 三、砍掉 ADB 后端（一个明确的取舍）

早期方案里还有第五个后端「ADB（电脑侧）」，**已经去掉**。理由：Shizuku 本来就是"借 `adb` 那套权限"，只是不需要电脑一直连着。单独再做一个 ADB 后端，对用户来说就是多一个"要插数据线 / 要开无线调试"的选项，而它能做的事 Shizuku 全都做得到 —— 属于自己给自己加门槛。没 root 的机器上，用 Shizuku 也只需要电脑**启动那一次**，之后手机自己就能跑。

#### 四、自己写的部分（三块）

| 文件 | 职责 |
|---|---|
| `ElainaShellPlugin.java` | 插件入口（`@CapacitorPlugin(name="ElainaDevice")`）、四个后端分发、授权跳转 |
| `ShellProcess.java` | 进程执行与输出回收（Root / Shizuku 共用） |
| `ShellBackends.java` | Root 与模块两个执行器 |
| `ShizukuShell.java` + `UserService.java` + `IElainaUserService.aidl` | Shizuku 后端（UserService 模式，AIDL 在 Shizuku 进程里以 UID 2000 跑） |
| `ElainaAccessibilityService.java` | 无障碍后端（点击/滑动/输入/读界面/截图） |
| `PhoneCommand.java` | 动作 → 命令（三个命令族后端共用） |
| `DeviceArtifacts.java` | 控件树 XML 压平、截图缩放与缩放比 |

前端侧：`window.ElainaDevice` 桥接（`deviceBridge()` 从 `window.Capacitor.Plugins.ElainaDevice` 取）、四个后端卡片、真实状态说明、截图转述、运行状态条、风险分级与授权。

#### 五、关键工程细节（都是会踩的坑）

**① 弹窗层级不能用内联 `style.zIndex`。**
`.modal-overlay` 上写了 `z-index: 100001 !important`，会把所有弹窗压成同一层 —— 于是"谁在上面"变成由 DOM 顺序决定，改一下 HTML 里元素的位置就可能把对话框埋到别的弹窗后面。而且 `!important` 会让内联值**完全失效**。最终用 ID 选择器（优先级高于类选择器）+ `!important` 显式分层：

```
100001 普通弹窗（设置、分类管理…）
100002 实现方式面板（从设置里打开，必须盖住设置）
100003 提示 / 确认对话框（要用户立刻做决定，永远在最上面）
```

**② 卡片用 `div` 而不是 `button`。** 卡片里还要放「去设置」按钮，而 HTML **不允许 button 嵌套 button**（浏览器会把结构解析错乱）。所以卡片是 `div[role=button][tabindex=0]`，点击时用 `closest('[data-backend-setup]')` 判断是否点在设置按钮上。

**③ 后端卡片必须显示状态说明原文**（不只是「已就绪 / 不可用」两个词）。手机上没法 hover，只给一个"不可用"，用户不知道该做什么。现在显示的是原生返回的原话，比如"还没授权，点「请求 root 授权」"。

**④ Root 不做主动探测。** `su -c id` 每跑一次，KernelSU / Magisk 就会弹一次授权框，而 `status()` 在每次打开面板时都会被调用 —— 主动探测等于每开一次设置就弹一次框。改成授权结果缓存在 SharedPreferences，只在用户点「请求 root 授权」时真跑一次。反过来，某次执行明确拿到 `Permission denied` 时缓存会被立刻清掉，免得界面一直显示「已授权」而实际全在失败。

**⑤ `agentBackend` 白名单漏一个 id，会让用户选了那种方式后一刷新就被重置成"没选过"**（加 Shizuku 时就踩过一次）。白名单列表必须与 `AGENT_BACKENDS` 保持一致。

**⑥ AIDL 必须给所有方法写显式 id。** `destroy()` 的事务号 `16777114` 是 Shizuku 服务端约定的固定值，而 aidl 编译器要求"要么全指定、要么全不指定"。少了 `destroy()`，Shizuku 解绑时关不掉服务，**下次绑定会失败**。

**⑦ 其他原生侧注意点**：`buildFeatures { aidl true }` 必须开（AGP 8 起默认关闭）；`ShizukuProvider` 要自己在 Manifest 声明（`provider` 这个 aar 里只带了权限和元数据，**没有** provider 节点）；只对回环地址放行明文 HTTP（`network_security_config.xml`），其余域名仍强制 HTTPS；截图必须**连缩放比一起返回**（长边缩到 1120 像素省 token，不传比例会导致点击坐标整体偏移）；截图/控件树的中间文件落在**应用的外部私有目录**（shell 的 UID 2000 写不进应用内部目录），命令里带 `chmod 666`，每次执行前先删旧文件**避免命令失败时读到上一次的旧界面**；Binder 有 1 MB 事务上限，命令输出要截断。

**⑧ `ShellProcess` 有两个必须注意的点**：stdout/stderr 要**并发读**，只读一个流的话另一个写满管道会把子进程卡死；输出超限后**仍要继续读**（只是不存），否则"输出太长"会变成"命令永远不结束"。

#### 六、厂商适配：先别自己吓自己

**Android 是 CTS 认证的，标准 API 的行为在全平台一致。** `dispatchGesture`、`ACTION_SET_TEXT`、`AccessibilityNodeInfo`、`uiautomator dump`、`screencap`、`input tap` 都是 AOSP 标准接口，厂商**改的是"允不允许你用"，不是"API 行为不一样"**。所以"同一个点击代码在小米上点得到、在华为上点歪了"这种事**不会发生**。

**这条推论很重要**：它意味着适配工作的性质是**做引导 + 做保活 + 做降级**，而不是"每个厂商写一套实现"。

真正的问题集中在三处：**① 权限授予路径**（各家设置页层级不同）、**② 后台保活**（厂商会杀前台服务、掐通知、限制自启 —— 严重度最高）、**③ root / 模块**（模块写死机型框架包，换机型要改）。

应对策略五条：能力探测不做机型猜测（不写 `if (小米) {...}`）、**多后端本身就是最大的适配策略**（某条路不通就换一条）、引导分厂商做（按包名逐个试，都没命中就打开应用详情页兜底）、保活分三层逐层降级、**失败必须能诊断**。

最后一条我们已经有基础设施了：之前做的**启动窗口日志转发**在这里价值极大 —— 手机上的失败原因会被推到电脑的 cmd 窗口。没有这个，适配问题只能靠猜。

#### 七、验证到了什么，以及做不到什么

**在电脑上可复现的**：

- 原生代码编译通过（Gradle 8.11.1 + AGP 8.7.2 + JDK 21），APK 产出成功
- 包内逐项核对：7 个 dex 里能找到全部新类（含 AIDL 生成的 `IElainaUserService$Stub` 与 `$Proxy`）、`rikka.shizuku.Shizuku`、`ShizukuProvider`；`AndroidManifest` 里无障碍服务声明 + `BIND_ACCESSIBILITY_SERVICE`、ShizukuProvider + `INTERACT_ACROSS_USERS_FULL`、`API_V23` 权限、`networkSecurityConfig` 引用、无障碍元数据全部到位
- `assets/public/index.html` 与仓库 `web/index.html` 逐字节一致（排除行尾符）
- 前端 **56 项回归全通过** —— 状态条显隐与文案、停止（含**打断等待中的确认弹窗**）、三种授权策略、风险分级、脏数据回落、端到端链路（`[操作:手机点击 …]` → 授权 → 拒绝/允许 → 回灌）、停止拦截同批次后续动作、解析容错、无 JS 异常

**只能在真机上确认的（电脑上做不到）**：

- `su -c id` 在具体 KernelSU / Magisk 版本上能不能拿到 `uid=0`
- 无障碍的 `dispatchGesture` 会不会被某个 ROM 拦；`ACTION_SET_TEXT` 在具体输入框里的表现
- **Shizuku 的 UserService 绑定在真机上是否稳定**（四个后端里最没法验证的一个）
- 模块网关（3070）的真实接口路径与字段名 —— 代码对 `stdout/out/output`、`stderr/err/error` 都做了兼容，但没对着真模块跑过
- 各厂商后台管理下长任务会不会被掐断
- 中文输入的实际表现

**一条铁律**：任何会打开页面的自动化测试，**必须先拦掉 `/api/store`**，否则测试数据会推到服务端污染真实数据（这个坑已经踩过一次）。

#### 八、进度与下一步

| 片 | 内容 | 状态 |
|---|---|---|
| 1 | 无障碍操作能力 + 四个执行器 | 已完成 |
| 2 | 截图链路（含缩放比回报 + 视觉模型转述） | 已完成 |
| 3 | 模块后端 + su 执行器 + 手机等待 | 已完成 |
| 4 | **可靠性：Agent 循环搬到前台服务；标签协议升级为正规 Tool Calling** | **未开始** |

**下一步最该做的**是第 4 片的前半：现在 Agent 循环跑在 WebView 里，应用切到后台时被系统节流会导致长任务走一半停住 —— 这是真机上最容易遇到的坑。

### 8.13 对话接口连不上：CORS 是真因，加本地中转（2026-09）

**现象**：`获取模型` 报 `获取模型列表失败：Failed to fetch`，对话也发不出去。用户反馈"我用其他软件同样配置都是正常的"。

#### 一、定位过程（关键是别猜，去测）

用户实际配置（`data/store.json`）是：

```json
{ "apiFormat": "openai-compatible",
  "baseUrl": "https://developer.amd.com.cn/radeon/api/v1",
  "apiKey":  "rc-7958fd33...",
  "model":   "deepseek-chat" }
```

不是 DeepSeek 官方，是第三方中转。直接拿 curl 打这个地址：

```
GET /v1/models              → 200 OK，但响应头只有
                              Content-Type / Server: nginx / vary: Cookie, Accept-Encoding
                              ★ 完全没有 Access-Control-Allow-Origin
OPTIONS /v1/chat/completions → 405 Method Not Allowed，同样没有 CORS 头
POST /v1/chat/completions    → 404 {"detail":{"error":{"message":"Model deepseek-chat is not available"}}}
```

**结论：两个独立的问题叠在一起。**

1. **CORS**：服务商一个 CORS 头都不发，预检还是 405。浏览器在预检阶段就把请求毙掉了，
   前端拿到的只有 `TypeError: Failed to fetch` —— 这个字符串是 Chromium 对网络层失败的兜底文案，
   没有状态码也没有原因码，所以看起来像"网络断了"，实际是浏览器主动拦截。
   ChatBox / Cherry Studio 用系统 HTTP 栈，不受 CORS 约束，所以同一套配置在它们那儿正常。
2. **模型名不存在**：`deepseek-chat` 不在这个服务上。它提供的是 `DeepSeek-V4.1-Flash` /
   `DeepSeek-V4-Flash` / `GLM-5.3-Flash` / `Qwen3.8-27B` 等。就算 CORS 通了，这一步也是 404。

#### 二、修复

**服务端**（`web/serve.mjs`）新增 `/api/relay`：

- 前端只跟**同源**的 `/api/relay` 说话，不触发预检；由 Node 侧 `fetch` 转发到真正的服务商；
- 请求体 `{ url, method, headers, body, timeoutMs }`，透传状态码与响应体（`for await` 逐块写，将来上 SSE 不用改）；
- 剥离 `host` / `content-length` / `cookie` / `origin` / `referer` / `connection`，`accept-encoding` 固定 `identity`
  （避免上游压缩，SSE 要能逐块透传）；
- 请求体上限 32 MB —— 对话历史、语音 base64、图片 base64 都走这条通道。

**安全**（接口挂在鉴权之后，另加三层）：

- 云元数据地址（`169.254.169.254` / `metadata.google.internal` / `metadata.tencent.internal`）直接 403；
- 局域网设备不能把中转当跳板打本机端口（`isLocalRequest` 判断），本机自己发起的放行 —— Ollama 就住在 `127.0.0.1`；
- 中转自身失败时返回 `{ ok:false, relayError:true, message }`，与上游返回的错误区分开，
  否则用户看到 502 会去怪服务商，而实际是 DNS / TLS / 超时。

**前端**（`web/index.html`）：

- `detectLocalRelay()` —— 探测同源 `/api/server-info`，结果缓存成 Promise（首个请求可能比探测先到，用 `await` 接住）；
  安卓原生环境、`file://` 直接返回 false；
- `relayRequest()` —— 统一的中转调用；
- `postJsonFromDevice` / `getJsonFromDevice` 里各加一个分支：**原生插件 → 本机中转 → 裸 fetch**。
  所有对话（OpenAI 兼容 / Responses / Anthropic）、获取模型、语音、视觉都走这两个函数，改一处全通。

#### 三、顺带修掉的两个错误信息问题

- **FastAPI 系网关的错误套在 `detail.error.message` 里**。原来的 `throwProviderResponseError` 只认
  `payload.error.message` / `payload.message`，抓不到这层 —— 用户只会看到兜底的"对话服务请求失败"，
  而真实原因是"模型名不在这个服务上"。新增 `extractProviderErrorMessage()` 统一挖文案，
  `getApiErrorText()` 也补上了 `detail` 分支。
- **新增 `MODEL_NOT_FOUND` 错误码**：地址和 Key 都通、只是模型名不对时，直接告诉用户去点「获取模型」；
  `NETWORK_ERROR` 的文案也改成能落地的排查步骤（先说 CORS，再说用 `启动.bat`）。

#### 四、验证（可复现）

```
A. 正确模型名  → HTTP 200，真实回复「你好！👋 很高兴见到你！」
B. 错误模型名  → HTTP 404，detail.error.message 完整透出
C. 域名不存在  → HTTP 502 { relayError:true, message:"本地中转无法连接目标：getaddrinfo ENOTFOUND ..." }
D. 云元数据    → HTTP 403 { relayError:true, message:"该地址不允许通过中转访问" }
```

#### 五、踩坑：Node 的 fetch 不读 `http_proxy`

本机环境有系统代理（`http_proxy=http://127.0.0.1:65508`），但 **Node 的 `fetch`（undici）默认不读这几个环境变量**，
中转是直连出去的。实测直连该服务商 355 ms 可达，所以没影响；但如果服务商只有走代理才通，
中转会失败并报出 `getaddrinfo` / `ECONNREFUSED` 之类的具体原因（这比 `Failed to fetch` 好排查得多）。
真要在 Node 侧走代理，得显式 `setGlobalDispatcher(new ProxyAgent(...))`，目前没做。

#### 六、顺手加的两个静态校验脚本

单文件应用没有构建期检查，改错一个字符就是白屏或 `Cannot read properties of null`，所以补了：

- `scripts/check-inline-scripts.mjs` —— 抽出每个内联 `<script>` 块丢给 `new Function()` 验语法，
  报错时换算回 HTML 行号；`type="module"` 的走 `vm.SourceTextModule`。
- `scripts/check-dom-refs.mjs` —— 校验 `getElementById('x')` 的 x 在 HTML 里真实存在、
  `elements.yyy` 在映射表里有定义。**首次运行就抓出 3 处遗留死代码**
  （`announcementKeySection` / `announcementKeyInput` / `announcementKeyError`，元素早已不存在，只靠 `if` 兜底），已一并清除。

两个都挂在 `npm run check`，`test` 与 `build:apk` 会先跑它。
（后续又加了七个专项检查：`check-live2d-assets.mjs`（19 项）、`check-live2d-native-list.mjs`（9 项，
见 8.17）、`check-chat-message-shape.mjs`（17 项，见 8.18）、`check-agent-paths.mjs`（26 项，见 8.19）、
`check-log-format.mjs`（35 项，见 8.20）、`check-anthropic-request.mjs`（56 项，见 8.21）、
`check-model-list.mjs`（43 项，见 8.22）。
九条串成 `npm run check`，共 205 项断言。）

### 8.14 日志落盘 + 两个"时好时坏"的成因（2026-09）

用户反馈"还是不太稳定"，但要查日志时发现**根本没地方查** —— 所有输出只打在 `启动.bat` 的窗口里，
关掉就没了，滚几屏之后也找不回。偶发问题最需要的那一次请求，恰恰早就滚出屏幕了。

#### 一、日志落盘（`data/logs/`）

在 `serve.mjs` 顶部包装 `console.log/info/warn/error`，各镜像一份到文件：

- **每次启动生成一个新文件**，文件名即启动时刻 `YYYY-MM-DD_HH-mm-ss.log`（本地时间）
- **只保留最近 10 份**，超出的从最老的开始删（详见 8.16）
- 每行带完整 ISO 时间戳；多行内容压成一行（换行替换为 `⏎`），方便 `grep`
- **绝不记录请求体和请求头** —— `Authorization` 就在请求头里，落盘等于把 API Key 写到磁盘上
- **写文件前统一脱敏**（`redactSecrets()`）。启动横幅里有 `访问密码: xxxxxxxx`，它会跟着落盘；
  而排查问题时这些日志是要发出去的（贴到聊天里、发给别人看），明文密码就一起漏了。
  现在只保留标签：`访问密码: ******（仅打印在控制台，不写入日志）`。
  **控制台输出保持明文** —— 用户本来就靠终端里那行密码登录，而 `启动.bat` 没有重定向控制台，
  不存在"换个地方又漏出去"的口子。另外两条兜底规则顺带盖住 `Bearer xxx` 与
  `apiKey/token/password/secret=...` 形式的凭据（万一将来有别的代码路径把它们带进日志）
- 启动与收到 `SIGINT`/`SIGTERM` 时各写一行 `[boot]`，能看出这次会话跑了多久
- 写日志失败被 `try/catch` 吞掉，绝不影响主流程

中转请求各留一行结果：

```
[2026-09-21T09:33:07.773Z] LOG   [relay] GET developer.amd.com.cn/radeon/api/v1/models -> 200 上游 296ms
[2026-09-21T09:33:08.064Z] ERROR [relay] 请求失败 GET https://nope.invalid/v1/models -> getaddrinfo ENOTFOUND nope.invalid [ENOTFOUND] (48ms)
```

失败行带上 `cause.code`（`ENOTFOUND` / `ECONNREFUSED` / `UND_ERR_CONNECT_TIMEOUT` …），
能一眼区分"没连上""上游慢""被客户端中断"三种完全不同的情况。

#### 二、顺带定位到两个真会导致"时好时坏"的缺陷

**缺陷 1：中转探测失败被永久缓存（上一轮引入的）**

`detectLocalRelay()` 把探测结果缓存成 Promise 以避免重复探测，但**失败也一起缓存了**。
探测请求有 5 秒超时，如果服务端当时正在保存大文件 / 瞬时卡顿导致探测超时，
`false` 就被缓存住，**整个页面会话此后永远退回裸 fetch** → 撞 CORS → 报 `Failed to fetch`。
表现正是用户描述的"时好时坏、刷新一下又好了"。

修法：只缓存成功。失败时清掉缓存，下次请求重新探测。

```js
})().then((ok) => {
    if (!ok) localRelayProbe = null;   // 失败不缓存，否则整个会话永久退回裸 fetch
    return ok;
});
```

**缺陷 2：登录会话过期被误报成"API Key 无效"**

`/api/relay` 挂在鉴权之后。局域网访问时访问密码的会话有 TTL，过期后服务端返回
`401 { ok:false, needLogin:true }`。原来的 `relayRequest` 不识别这个，直接交给
`throwProviderResponseError` → `inferApiErrorCode(401, …)` → 判成 `APP_KEY_INVALID`，
用户看到"Key 无效"就会去反复换 Key，而真正该做的是刷新页面重新登录。

修法：`relayRequest` 里先识别 `response.status === 401 || payload?.needLogin`，
抛独立的 `LOCAL_SERVER_UNAUTHORIZED`，并在 `getClientErrorPresentation` 里给出
"刷新页面重新输入访问密码"的提示。

### 8.15 运行时数据统一收进 `data/`（2026-09）

起因：用户问"`web/` 能不能也并进 `data/`"。直接合并不可行（源码脱离版本控制、`data/` 会变成静态根、
路径推导会变成 `data/data`），但这个问题指向了一个真实的不一致 ——
**运行时私密数据当时分散在两处**：

- `data/`：`store.json`（含 API Key 与聊天记录）、`cert.pem` / `key.pem`
- `web/.local-auth.json`：局域网访问密码

后者是"位置不对、靠规则补救"的典型：它待在静态服务的根目录里，安全只靠两条规则同时成立 ——
`.gitignore` 里那条单独排除规则，加上静态处理器"禁止访问点文件"的检查。任何一条被改动都会漏。

**改动**：`AUTH_FILE` 从 `path.join(root, '.local-auth.json')` 改为 `path.join(DATA_DIR, 'auth.json')`。
`data/` 整目录既已 gitignore，又刻意位于静态根之外，这两处风险同时消失。

**迁移**：`loadAuthFile()` 读不到新文件时回看老位置，读到就 `rename` 过去并打印一行日志；
搬不动（权限等）则退回用老内容继续跑 —— **不能让用户因为一次重构被锁在门外**。
`--reset-password` 同时清新旧两个位置，否则迁移逻辑会把老文件搬回来，"重置"等于没重置。
`saveAuthFile` 补了 `mkdir`，保证目录不存在时也能写入。

**`.gitignore` 里那条 `web/.local-auth.json` 刻意保留**：升级前遗留在 `web/` 的老文件若不排除，
没跑过新版服务的人执行 `git add .` 就会把访问密码提交上去。

**验证**：迁移前用原密码可登录 → 启动一次后 `web/.local-auth.json` 消失、`data/auth.json` 内容逐字节一致
→ **用原密码仍登录成功**（`{"ok":true}`）→ 再次启动不重复迁移、不报错（幂等）。

### 8.16 日志改为"每次启动一个文件 + 只留最近 10 份"（2026-09）

> 本节讲的是**文件切分与清理**。行内格式、级别、来源行号、对话追踪后来在 **8.20** 里重写过一遍，
> 下面提到 `_2` / `_old` 命名与正则的地方以 8.20 为准（同秒多实例现在是 `-2`，大小拆分才是 `_2`）。

起因：8.14 把日志落到固定的 `data/logs/server.log`（超 4 MB 滚成 `.1`），
所有会话混在同一个文件里，翻历史很费劲 —— 要找"刚才那次"得先在几百行里定位时间点。

**改成按"启动"切分**：每次启动新建 `YYYY-MM-DD_HH-mm-ss.log`，文件名就是启动时刻（本地时间）。

为什么按启动切而不是继续按大小滚：**一次启动 = 一次会话**，出问题时直接看对应那次启动的文件，
不用在历史里翻找"哪一段是刚才那次"。而且文件名天然可排序（字典序 == 时间序），
清理逻辑只要排个序、切掉前面多出来的即可，不需要读每个文件的元数据。

**自动清理**：`LOG_KEEP = 10`。建好本次的新文件**之后**再清理，保证"保留 10 份"含本次。
8.20 起按**启动时刻前缀分组**计数（一次启动产出主日志 + 追踪日志两个文件，
按文件数算会让"留 10 份"缩水成 5 次启动）。

实现要点（都是踩过的坑）：

- **同一秒起两个实例**：文件名会撞。加 `-2` / `-3` 序号（`existsSync` 探测）。
- **清理用的正则必须放宽容错**。第一版写成 `(?:_\d+)?`，只认数字后缀，
  结果旧格式 `server.log.1` 改名后带的 `_old` 后缀不匹配 → 那种文件**永远不会被清理**，
  会一直堆着。现在用 `(?:[-._][A-Za-z0-9]+)*`，同时容得下 `_old`（接管旧文件）、
  `_2`（大小拆分）、`-2`（同秒多实例）。
- **旧格式文件要接管**：`server.log` / `server.log.1` 是固定名，放着不管就成了"不会再更新的孤儿文件"，
  用户分不清哪个是新的。启动时按它们的修改时间改名成新格式，之后正常参与轮转。
- **先建新文件再清理**，否则本次日志不算进保留数，会多留一份。
- 目录不可写时退化为"只打控制台"，不让启动崩掉。`LOG_TO_FILE=0` 时连兜底路径也不写。

**验证**（构造 11 份新格式 + 1 份 `_old` + 1 份旧 `server.log`，共 13 份）：
输出里的标签现在是 AstrBot 风格的 `[Core]` + 四位级别（8.20）：

```
[Core] [INFO] [serve:?]: 旧日志 server.log 已改名为 2026-09-21_17-47-31.log
[Core] [INFO] [serve:?]: 清理旧日志 2020-01-01_00-00-01.log（只保留最近 10 次启动）
[Core] [INFO] [serve:?]: 清理旧日志 2020-01-01_00-00-02.log（只保留最近 10 次启动）
[Core] [INFO] [serve:?]: 清理旧日志 2020-01-01_00-00-03.log（只保留最近 10 次启动）
[Core] [INFO] [serve:?]: 清理旧日志 2020-01-01_00-00-04.log（只保留最近 10 次启动）
[Core] [INFO] [serve:367]: serve.mjs 启动 pid=… 版本 v1.0.0
→ 13 份 → 正好剩 10 份（5 次启动），且 `_old` 也在轮转范围内
```

> `SIGTERM` 在 Windows 上无法可靠送达（`启动.bat` 里按 Ctrl+C 走的是 `SIGINT`，那条是有效的）。
> 用外部工具强杀进程时不会有收尾行 —— 这时以文件里最后一个时间戳为准。

### 8.17 内置模型"没有表情/动作"：`exp/` 目录是个错误假设（2026-09）

**现象**：APK 里打包了两个内置 Live2D 模型，用户反馈"没看到"。

**先排除掉的**（都不是问题，别在这里浪费时间）：

- 打包本身没坏。用 Python 直接读 APK 的 zip：`assets/public/live2d/models/` 下 70 个条目，
  两个模型齐全（`deepseek` 61 个文件、`伊蕾娜·默认` 8 个文件），2 个 `.moc3`，
  `manifest.json` 内容正确，UTF-8 文件名（含中文、`·`）也没被破坏。
  > 教训：`jar tf | grep` 的输出会被 grep 判成二进制而**截断**，看起来像"只有几个文件"。
  > 要核对 APK 内容就用 `unzip -l`，或者用 Python 读 zip（本仓库选了后者）。
- 插件没缺。`@capacitor/filesystem@7.1.8` 已装且已注册进 `capacitor.build.gradle` /
  `capacitor.settings.gradle`。
- 模型能加载。两个 `model3.json` 的引用（`Moc` / `Textures` / `Physics`）全部存在；
  库里 `Motions` / `Expressions` 缺失都有 `?? []` 兜底，`isValidJSON()` 只要求
  `Moc` + 非空 `Textures`，所以"没有 Motions 段"不会导致加载失败。
- 播种逻辑没问题。`nativeWriteFile()` 不传 `encoding` 时 Capacitor 按 **base64 解码**写入
  （官方类型定义原话："If not provided, data is written as base64 encoded"），
  分块大小 1 MB 能被 4 整除，base64 不会在中间断出填充错误。

**真正的原因**：**表情/动作不在 `exp/` 子目录里**。

Cubism 只规定 `*.exp3.json` / `*.motion3.json` 的**文件格式**，放在哪个目录由模型作者自己决定。
而旧实现把 `exp/` 写死在了两处：

1. `serve.mjs` 的 `listModels()`：只在模型目录下找名为 `exp` 的子目录，找不到就返回空列表；
2. `live2d-video.js`：`setExpression()` / `playMotion()` / `detectWatermark()` /
   `applyWatermark()` 一律拼 `modelBaseUrl(name) + 'exp/' + 文件名`。

而内置模型的真实结构是：

| 模型 | 表情 | 动作 |
|---|---|---|
| `deepseek` | 44 个 `*.exp3.json` **直接在模型根目录** | 8 个，7 个在 `motions/`、1 个 `aidale.motion3.json` 在根目录 |
| `伊蕾娜·默认` | 无 | 无（模型本身就没带，属正常） |

所以 `exps: []` / `motions: []`，任何表情标签都会去 fetch 一个必然 404 的
`/live2d/models/deepseek/exp/脸红.exp3.json` —— 表现就是"模型在，但没表情"。

**修复**：把 `exps` / `motions` 的语义从"裸文件名"改成**"相对模型根目录的路径"**，
并去掉写死的 `exp/`。

- `serve.mjs`：新增 `collectModelFiles()` 递归收集（深度上限 3），
  `model3.json` / `vtube.json` **优先取根目录下的**，避免纹理等子目录里的同名文件抢走。
- `index.html`：新增 `nativeCollectModelFiles()`（Capacitor 版），语义与服务端一致；
  顺带补上原先漏掉的 `vtube` 字段，并跳过 `live2d/models/` 下的非目录项。
- `live2d-video.js`：新增 `modelAssetUrl(name, relPath)` 统一拼 URL ——
  **逐段 `encodeURIComponent`**，不能整串编码（否则 `/` 变成 `%2F`，服务端找不到文件）。
  4 处写死的 `'exp/'` 全部替换掉。
- 名称解析改成三级兜底：整条路径精确匹配 → **文件名**精确匹配 → 子串/同义词组模糊匹配；
  返回的仍是相对路径。`fuzzyMatchFileName()` 比较时只看文件名部分，返回时把完整路径带回去。
- `getAvailableExpressions()` 注入给 AI 的清单剥掉目录与扩展名并去重
  （AI 该看到"脸红"，不是"exp/脸红.exp3.json"）。

**验证**：

```
$ curl -s localhost:4173/api/live2d/models
模型: deepseek
  modelJson : c_0120.model3.json
  vtube     : c_0120.vtube.json
  exps  (44): ['love.exp3.json', 'ħצ.exp3.json', '兔兔贴纸.exp3.json', ...]
  motions( 8): ['aidale.motion3.json', 'motions/chuipaopao.motion3.json', 'motions/idle.motion3.json', ...]
模型: 伊蕾娜·默认
  exps  ( 0): []      motions( 0): []      ← 模型本身没带，正常

脸红.exp3.json            -> HTTP 200
motions/idle.motion3.json -> HTTP 200
伊蕾娜 LSS.model3.json    -> HTTP 200
```

修复前 `exps=0 / motions=0`，修复后 44 / 8，资源全部可达。

回归检查 `scripts/check-live2d-assets.mjs`（已并入 `npm run check`）：
它用字符串切片把 `live2d-video.js` 里的**真实函数体**抠出来，在 Node 里喂真实清单跑一遍
（19 项：URL 拼接含中文与子目录、注入防护、三级解析、空清单放弃）。
不复制逻辑，所以测的就是真代码；函数被重命名时它会直接报错提醒同步更新。

另有一个**双实现对拍** `scripts/check-live2d-native-list.mjs`（同样在 `npm run check` 里）：

模型列表有**两份实现**——Web 版走 `serve.mjs` 的 `listModels()`，APK 走 `index.html` 的
`nativeLive2dFetch()`（Capacitor Filesystem）。两份代码要遵守同一套语义（都递归扫描、都优先取
根目录的 `model3.json`/`vtube.json`、都把 `exps`/`motions` 表达成相对路径），极容易各改各的跑偏，
而原生那份**只有真机才能验证**。

这个检查把两边的真实函数体抠出来，喂**同一个真实模型目录**（原生侧的 Capacitor Filesystem 用
Node `fs` 做等价桩），深比对输出：

```
服务端 listModels()      : [["伊蕾娜·默认",0,0],["deepseek",44,8]]
原生 nativeLive2dFetch() : [["伊蕾娜·默认",0,0],["deepseek",44,8]]
  ok   两边模型数一致
  ok   两边结果逐字段深比对一致
  ok   deepseek 的 modelJson 取的是根目录下的
  ok   deepseek 表情数 > 40（根目录下的 exp3 被扫到）
  ok   exps 是相对路径而非裸文件名
  ok   清单里的每个表情/动作文件都真实存在
模型列表双实现对拍：9 通过, 0 失败
```

> 写这类"抠函数体"的检查时有个必踩的坑：`indexOf('function x(')` 会从 `async function x(` 的
> **中间**开始切，抠出来就成了同步函数，里面所有 `await` 立刻语法错误。切片前必须把 `async ` 前缀补回去。

> 两个顺带发现、**未修**（不属本次范围）：
> 1. `伊蕾娜·默认` 既没有表情也没有动作文件，渲染出来是完全静止的 —— 这是模型自身内容缺失，
>    不是代码问题。要动只能补模型文件。
> 2. `deepseek` 里有个表情文件名本身是乱码（`ħצ.exp3.json`，原始下载时编码就坏了）。
>    它会出现在给 AI 的表情清单里，功能不受影响。

### 8.18 `System message must be at the beginning.`：文案在骗人（2026-09）

用户发来一句：「你看看报错日志我没有发送图片为什么会出现这种情况」。

#### 一、先看日志，别猜

`data/logs/2026-09-21_19-31-04.log`：

```
[relay] POST developer.amd.com.cn/radeon/api/v1/chat/completions -> 400 上游 938ms
上游响应：{"status":400,"message":"System message must be at the beginning.",
           "rawText":"{\"error\":{\"message\":\"System message must be at the beginning.\",…
                      \"usedProvider\":\"self-dploy\",\"requestedModel\":\"Qwen3.8-…
```

配置侧（`data/store.json`）：`baseUrl = https://developer.amd.com.cn/radeon/api/v1`、
`model = Qwen3.8-27B`、`apiFormat = openai-compatible`、`visionModel` 为空。
会话「你好」里只有 `user: 你好` / `ai: …` 两条，**确实一张图都没发**。

所以有两件事要分开解释：**为什么 400**，以及**为什么提示词在讲图片**。

#### 二、400 的真因：这个网关只接受「一条、且在位置 0」的 system

报错字面意思是"system 消息必须在开头"，读起来像是"你把 system 放到中间了"。但本应用的
layered-v2 提示词分层一次会发 4~12 条 system，**全都在开头、位置 0 起连续排列** —— 按字面
意思完全合法，却照样 400。于是做了一组**消息形状对照实验**，逐条改形状打同一个端点：

| 发送形状 | 结果 |
|---|---|
| `[system, user]` | **200** |
| `[system, system, user]` | **400** `System message must be at the beginning.` |
| `[user]`（无 system） | 200 |
| `[system, user, system]`（system 夹在中间） | 400（这条才是文案字面说的那种） |
| 用 `developer` 角色替代 `system` | 400 `Unexpected message role.` |

结论：该服务商（`usedProvider: self-dploy`）**只接受至多一条 system，且必须在位置 0**。
多条连续 system 尽管都在开头，仍被拒。报错文案描述的是它自己的约束，不是你的错误 —— 属于典型的
**误导性上游文案**。

#### 三、修复：发送前合并开头的连续 system

`web/index.html` 新增 `mergeLeadingSystemMessages()`，在 `callChatAPI` 里**分发到三种格式之前**
统一执行：

```js
function mergeLeadingSystemMessages(messages) {
    const list = Array.isArray(messages) ? messages : [];
    let end = 0;
    while (end < list.length && list[end] && list[end].role === 'system') end++;
    if (end <= 1) return list;   // 0 条或 1 条，不用动
    const merged = list.slice(0, end)
        .map(message => extractTextContent(message.content))
        .filter(text => String(text || '').trim())
        .join('\n\n');
    return [{ role: 'system', content: merged }, ...list.slice(end)];
}
```

语义等价性（为什么合并是安全的，而不是"阉割提示词"）：

- **只动开头连续的 system**。夹在中间的 system 一律不碰 —— 那种情况是真错，合并会掩盖别的 bug。
- 合并只是**拼接**，内容一条不少，用空行分隔；合并后的文本对模型而言与多条 system 拼接同义。
- 空白的 system 被过滤，避免留下 `content: ""` 这种怪东西；全空白时仍占一条，不产生"0 条 system"的状态。
- `end <= 1` 直接返回**原数组引用**，不产生无谓拷贝。

同时对**所有**格式生效（OpenAI 兼容 / Responses / Anthropic）—— 这是客户端侧的兼容性收敛，
不是给某一家打补丁。Anthropic 官方本来只允许一条 system（走顶层 `system` 字段），合并后更顺。

顺手在 `callChatAPI` 加了一行诊断日志，下次同类问题一眼可辨：

```
[Chat] openai-compatible 发送 4 条：system,user,assistant,user
```

#### 四、顺带修掉那个"图片"误导

用户之所以问"我没发图片"，是因为错误提示里自己提到了图片。
`getClientErrorPresentation()` 的 `BAD_REQUEST` 分支**无条件**拼了一段
「图片转述常见原因：视觉模型名不对、图片过大、或该模型不支持图片…」—— 任何 400 都会带上它，
与图片毫无关系。改成条件提示：

```js
const imageRelated = apiError.imageHint === true || /image|vision|multimodal|多模态|图片/i.test(detail);
```

配套给 `ClientApiError` 加 `imageHint` 字段，并在 `callAI` 捕获时按"本次是否真的带了图"打标：

```js
try { return await callChatAPI(messages, {...}); }
catch (err) { if (hasImage && err instanceof ClientApiError) err.imageHint = true; throw err; }
```

没带图时改为给通用原因：**"服务商对请求格式有额外要求（例如只接受一条 system 消息）、
模型名与接口不匹配、或该模型不支持当前参数"** —— 正好覆盖本次这个真实场景。

#### 五、验证

端到端实证（真实端点，同一个 Key、同一个模型名）：

```
400 system×8 合并前 → 200 system×1 合并后  ✓ 复现成功且修复有效
```

回归检查 `scripts/check-chat-message-shape.mjs`（已并入 `npm run check`，17 项）：
同样用字符串切片抠出 `extractTextContent` + `mergeLeadingSystemMessages` 的**真实函数体**在
Node 里跑，覆盖空数组 / 单条 / 无 system / 非数组入参 / 8 条合 1 / 位置 0 / 内容一条不少 /
历史未被改动 / 空白过滤 / 数组内容拍平 / **中间的 system 不动** / 真实分层 12 条 → 1 条。

> 这个检查特意留了一条"system 在中间时不动它"，防止将来有人把合并写成"全局收集所有 system"，
> 那样会把真正的位置错误一并吞掉，变成更难查的问题。

### 8.19 「在桌面建个 txt」为什么失败：桌面被移动过（2026-09）

用户测 Agent 功能：「请你简单的在我电脑桌面上创建一个txt文件……提示一下桌面默认位置**我移动了**需要你找一下」。
结果 AI 列了一遍 `C:\Users`、又列了一遍 `C:\Users\Administrator`，然后操作失败、停住。
用户的两句反馈是：「这不对啊和我电脑文件不符」「ai没创建文件就停止了」。

#### 一、日志与对话都指向同一个结论

`data/logs/2026-09-21_20-04-58.log`：

```
[relay] POST developer.amd.com.cn/... -> 200 上游 21144ms
GET /api/agent/ls?permission=computer&path=C%3A%5CUsers              200    14ms
[relay] POST developer.amd.com.cn/... -> 200 上游 27892ms
GET /api/agent/ls?permission=computer&path=C%3A%5CUse...             200     2ms
GET /api/agent/ls?permission=computer&path=C%3A%5CUse...             404     0ms   << 请求失败
```

`data/store.json` 里那次会话的最后一条：

```
【AI 操作结果】
操作失败：目录不存在（AI 解释失败：模型只返回了思考内容，没有生成可见回复，请重试或缩短输出。）
```

再去看那两份目录列表：`C:\Users` 是 6 项、`C:\Users\Administrator` 是 56 项，**里面根本没有 `Desktop`**。
直接查注册表：

```
UserShellFolders.Desktop = D:\桌面
```

**桌面被移到了 `D:\桌面`**。而 AI 只会按惯例猜 `C:\Users\<用户名>\Desktop` —— 猜错就是一句
「目录不存在」，然后它就没招了。

#### 二、两个真 bug

**bug 1：没有任何途径知道外壳文件夹的真实位置。**
服务端只认绝对路径，AI 只能靠常识猜。桌面被移动（用户手动移 / OneDrive 重定向）就必然失败。
而且失败信息是干巴巴的「目录不存在」，不含任何纠正线索 —— 连"下一轮自己改"的机会都没有。

**bug 2：Windows 的 junction 被当成文件。**
`C:\Users` 里的 `All Users`、`Default User`，以及 `<用户>` 下的
`My Documents`、`Application Data`、`Cookies`、`Local Settings`、`NetHood`、`PrintHood`、
`Recent`、`SendTo`、`Templates`、`「开始」菜单` —— 这 12 个兼容性链接在 readdir 的 `Dirent` 里
**既不是目录也不是文件**：

```
非dir非file -> "All Users" | isDirectory= false isSymbolicLink= true | stat后= DIR(stat)
```

旧实现只看 `e.isDirectory()`，于是这 12 项全被打上 `📄`。用户打开资源管理器根本看不到这些项
（它们是隐藏的系统链接），却在 AI 的列表里以"文件"的样子冒出来 —— 这就是「和我电脑文件不符」。

#### 三、修复

**服务端（`serve.mjs`）**

1. 新增 `SHELL_FOLDER_SPECS` + `readUserShellFolders()` + `resolveShellFolders()`：
   从注册表 `HKCU\...\Explorer\User Shell Folders` 读出桌面/文档/下载/图片/音乐/视频的真实位置。
   一次运行只解析一次（起进程不便宜，且这些路径在一次运行内不会变）。
2. 新增 `GET /api/agent/roots`：把真实位置给前端。**只在「本机 + permission=computer」时才返回 path**，
   其余情况返回一份没有 path 的列表 —— 局域网访客拿不到宿主机的目录结构。
3. 新增 `expandPathAliases()`：支持 `%DESKTOP%` / `~desktop` / `%USERPROFILE%` / `~/…` 等别名，
   大小写不敏感。**只认这两种显式写法** —— 不去猜「桌面\a.txt」这种裸词，
   否则一个恰好叫「桌面」的目录会被劫持。
4. `agentLs` / `agentRead` 的 404 文案带上真实位置（`shellFolderHint()`），
   让 AI 下一轮能自己纠正，而不是只能道歉。
5. `listAgentEntries()`：`isDirectory()` 为假时再 `stat` 一次跟随链接，按真实类型归类，
   并加 `link: true` 标记。排序逻辑不变。

**前端（`index.html`）**

6. `detectAgentRoots()`：页面启动就预热一次（缓存成 Promise，**只缓存成功** —— 理由同
   `detectLocalRelay`：失败也缓存会让整个会话都拿不到真实路径）。一律按 `permission=computer` 询问，
   因为服务端自己会判断是不是本机。
7. `agentRootsText()`：把真实位置拼进「Agent Skill：文件操作」提示词，并明确写
   **"不要按惯例猜 `C:\Users\<用户名>\Desktop`"** + 别名用法。**这是真正的闭环** ——
   服务端解析得再对，没进提示词 AI 还是照旧猜。只在「允许操作电脑」模式下注入。
8. `buildLayeredRoleplayMessages` 前 `await detectAgentRoots()`，**整段包 try/catch**：
   拿不到真实路径顶多是 AI 多问一次，但这里抛出去整个对话就发不出去了。
9. 目录列表渲染加「（链接）」后缀，把 junction 和普通文件区分开。
10. `explainAgentFailure()` 重做：
    - 原因截断 300 → **600 字**（真实位置提示比 300 字长，原来会被切掉一半，AI 拿不到纠正线索）
    - 允许 AI **自动重试一次**：解释里带了新的 `[操作:…]` 就真的执行（走同一条标签管线，停止/授权照常）。
      **必须限次**（`AGENT_FAILURE_MAX_RETRY = 1`，成功即清零）—— 否则「失败 → 解释 → 重试 → 再失败」
      会变成无限循环，一次误判就能把 API 额度刷光。
    - `EMPTY_MODEL_OUTPUT`（思考型模型在"系统内部通知"这种元提示上尤其容易只回思考内容）
      原样重试一次；再失败就只把原因显示出来，不再甩
      「（AI 解释失败：模型只返回了思考内容…）」这种嵌套文案给用户。

#### 四、一个差点写错的地方：命令行输出的编码

`reg.exe` / `powershell.exe` 的 stdout 走的是**系统 ANSI 代码页**（中文系统 GBK），
按 `encoding: 'utf8'` 读会把 `D:\桌面` 变成 `D:\????`：

```
[powershell.exe] OK -> "{\"Desktop\":\"D:\\\\????\", ...}"
```

路径看起来"读到了"，实际是错的 —— **比读不到更危险**（别名会静默指向一个不存在的目录）。
修法：PowerShell 那边让脚本自己 `[Convert]::ToBase64String(...)` 输出，纯 ASCII 过线，
Node 侧 `Buffer.from(raw, 'base64').toString('utf8')` 还原；
`reg.exe` 那边用 `decodeCliText()`（先按 UTF-8 解，出现替换字符就按 GBK 重解）。
顺便把 PowerShell 提到 `reg.exe` 前面 —— reg 没法控制输出编码，中文路径有被弄坏的风险。

#### 五、验证

新增 `scripts/check-agent-paths.mjs`（已并入 `npm run check`，26 项）。它是**真的起一次
`serve.mjs` 打真实 HTTP 接口**（`LOG_TO_FILE=0`，不污染 `data/logs/`），比抠函数体更贴近实际：

```
--   这台机器上的桌面: D:\桌面
--   识别到的链接（junction）: 「开始」菜单、Application Data、Cookies、Local Settings、
                              My Documents、NetHood、PrintHood、Recent、SendTo、Templates
ok   %DESKTOP% 解析到真实桌面
ok   别名后接文件名时也展开（404 里能看到展开后的完整路径）
ok   猜错的桌面路径返回 404 / 404 的文案里带着真实桌面路径
ok   被标成 file 的条目里没有实际是目录的
ok   限制模式下 %DESKTOP% 被拒绝（别名不能绕过权限）
ok   限制模式下 web/../data 穿越被拒绝
ok   允许操作电脑时把真实桌面写进提示词
ok   没有 path 的条目不会被写进提示词（局域网访客拿到的就是这种）
Agent 真实位置与目录归类检查：26 通过, 0 失败
```

最后 6 项是**提示词闭环**的断言：用字符串切片抠出 `agentRootsText()` 的真实函数体跑一遍，
确认「真实位置真的进了提示词」以及「限制模式 / 局域网拿不到 path 时不会注入」。
前 8 项修好了 `%DESKTOP%` 却忘了注入提示词的话，AI 还是照旧猜 —— 那才是最容易漏的一环。

顺手给 `serve.mjs` 加了 `LOG_TO_FILE=0`：自动化场景（回归检查、CI）会频繁起停服务，
每次启动都生成一份日志会把用户真正的排查日志挤出「最近 10 份」。关闭时连兜底路径也不写，
否则会在 `data/logs/` 里凭空冒出一个不符合轮转命名、永远不会被清理的 `server.log`。

### 8.20 日志系统重写：照 AstrBot 的格式，把「对话内容 + 报错原因」记全（2026-09）

用户原话：

> astrbot 几乎所有的报错、用户的对话、ai 聊天的内容和报错的原因都写出来。
> 我们现在的日志只有什么地址在什么时间调用了什么 api，不全面。

格式参照用户机器上的 AstrBot 源码（`astrbot/core/log.py`，loguru 配置），
两套 format 字符串、四位级别名、级别色、` [v版本]` 的附加规则都是从那里面直接抄的。

#### 一、旧日志到底缺什么

| # | 现象 | 根因 | 后果 |
|---|---|---|---|
| 1 | 文件名写 `20-04-58`，行内写 `[12:04:58.464Z]` | 行内时间用 `toISOString()`（UTC） | 差 8 小时，对不上号 |
| 2 | 同一个文件里两套时间格式 | 消息体里还嵌着前端传来的本地 `[20:04:59]` | 一个文件里两个"现在" |
| 3 | 只有时间，没有来源 | 设计上就没记 | 同一句报错可能来自好几处，不知道是哪处 |
| 4 | 上游返回的多行 JSON 报错被压成一行 | 落盘前统一 `\s+` → 空格 | 反而更难读 |
| 5 | 级别字段恒为 `LOG` | 服务端自己拼的字符串，不是真级别 | 不能按级别过滤 |
| 6 | 空行也是一条记录 | `console.log('')` 照样落盘 | grep 结果里全是噪音 |
| 7 | 客户端上报截断到 300 字符 | `toText()` 里的 `.slice(0, 300)` | 完整上游报错被砍在半截（`requestedModel":"Qwen3.8-` 就断了） |
| 8 | **只有"调了什么"，没有"问了什么 / 答了什么 / 为什么失败"** | 设计上就没记 | 排查时还原不出场景 —— 这是最要命的一条 |

第 8 条是这次改造的核心。前 7 条是顺手一起修掉的。

#### 二、格式（照 AstrBot）

```
控制台  [HH:mm:ss.SSS] [标签] [级别] [来源:行号]: 消息            （TTY 时带 ANSI 颜色）
文件    [YYYY-MM-DD HH:mm:ss.SSS] [标签] [级别] [来源:行号]: 消息
```

- 级别固定四位：`DBUG` / `INFO` / `WARN` / `ERRO` / `CRIT`（同 AstrBot 的 `short_levelname`）
- `WARN` 及以上附 ` [v版本]`，`INFO` 不附 —— 正常信息里塞版本号纯属噪音
- 级别色（DEBUG 亮蓝 / INFO 亮青 / WARN 亮黄 / ERROR 红 / CRIT 亮红）、时间绿，全部同 AstrBot
- 版本号从 `package.json` 读。这个项目频繁重打包，出问题时能一眼对上"这份日志是哪一版产生的"
- **时间一律用本地时间**。这是日志最基础的可用性，旧实现（UTC）连这一条都没满足
- 标签由消息开头的 `[xxx]` 抽出，并收敛成一套固定词汇
  （`Core / Store / Relay / Live2D / Agent / Chat / HTTP / Page / Auth / Cert / TTS / ASR / Vision`），
  所以调用点照旧写 `console.log('[relay] …')` 就行，输出却是结构化的
- 来源行号从 `new Error().stack` 里取第一条**不属于日志模块自己**的帧

#### 三、两个文件，各管一件事

| 文件 | 内容 | 给谁看 |
|---|---|---|
| `<启动时刻>.log` | 一行一条记录，换行压成 `⏎` | 机器（grep / tail） |
| `<启动时刻>.trace.log` | LLM 的完整请求消息 + 完整回复 + 完整上游报错，多行**原样保留** | 人 |

为什么分成两个：对话内容又长又私密，混在主日志里会把 grep 结果淹掉；而且排查问题时
主日志常常是要**直接发出去**的 —— 分开之后"要不要发对话内容"就变成一个明确的动作。
启动横幅里会明确提醒追踪日志含完整对话内容、对外分享前先看一眼。

#### 四、埋点为什么选在 `/api/relay`

`/api/relay` 是**所有对外 AI 请求的唯一出口**（三种对话格式、获取模型、语音、视觉全走它）。
在这一层埋点就能覆盖全部，前端一行都不用改。

追踪日志里记什么：

- **请求**：模型名、消息条数、请求体字节数，然后逐条消息（序号 / 角色 / 字数 / 正文）
- **成功**：回复全文 —— 非流式直接读 JSON；流式把 `data:` 分片**拼回完整正文**
  （否则日志里只有一坨 `data:` 行，等于没记）
- **失败**：完整响应体 + 从 `detail.error.message` 这类嵌套结构里挖出真正的原因

主日志则各留一行摘要：`提问：…`（只留最后一条用户消息）与 `回复：…`（各截 200 字），
一眼看出"这次聊了什么"。失败时那一行直接带上原因，不用翻文件。

#### 五、五种失败形态，必须分得清

实测输出（`[标签] [级别] [来源]` 前缀省略）：

```
[Chat] 提问：那我们去哪儿玩？
[Relay] POST 127.0.0.1:14300/v1/chat/normal -> 200 上游 26ms
[Chat] 回复：你好呀，我是伊蕾娜。 今天想去哪儿？

[Relay] ERRO POST 127.0.0.1:14300/v1/chat/bad -> 400 上游 4ms << 上游报错：模型 test-model 不存在，请改用 DeepSeek-V4-Flash
[Relay] ERRO POST 127.0.0.1:14300/v1/chat/sse-err -> 200 上游 3ms << 响应内报错：当前分组上游负载已饱和，请稍后再试
[Relay] ERRO 请求失败 POST http://127.0.0.1:14399/v1/chat/completions -> connect ECONNREFUSED 127.0.0.1:14399 [ECONNREFUSED] (4ms)
[Relay] WARN 目标地址无效：not-a-url
```

1. **上游 4xx/5xx** —— 状态码能看出来，原因靠挖嵌套字段
2. **状态码 200，报错藏在 SSE 的 `data:` 行里** —— 最容易漏的一种。不特意挖一下，
   用户只看到"AI 不回话"，而日志里一片祥和，完全无从下手
3. **连不上**（DNS / TLS / 连接被拒）—— 带上 `cause.code`，比浏览器那句 `Failed to fetch` 有用得多。
   并且会把**请求内容**也写进追踪日志：只看到"连接被拒"却不知道当时问的是什么、
   模型名有没有写错，而那恰恰是最常见的原因
4. **本地校验拒绝**（地址非法 / 协议不符 / 云元数据地址 / SSRF / 方法不支持）——
   这些以前是**完全静默**的，前端只拿到一句中文提示，日志里连"发生过这件事"都没有
5. **客户端主动断开** —— 不记。它不是错误，记下来只会污染日志

#### 六、踩过的坑

- **来源行号全指向包装函数自己**。把函数赋给 `console.log` 之后，V8 把帧名渲染成
  `console.patchedConsoleLog [as log]`；正则不剥掉 ` [as log]` 后缀的话，这个名字既不等于
  `patchedConsoleLog`、也过不了 `^.*\.` 那一刀，于是包装层被当成"真正的调用点" ——
  结果是**每一条**日志都指向同一行，来源定位完全失效。第一版就是这个症状（全是 `serve:351`）。
- **控制台每条日志打两遍**。包装时若再调一次原始 `console.log`，控制台上会出现
  "未格式化原文 + AstrBot 格式行"两行，而落盘的只有后者 —— 两边对不上号。
- **按文件数清理会让「保留 10 份」缩水一半**。一次启动产出两个文件（主 + 追踪），
  按文件数算的话"留 10 份"只等于 5 次启动。改成按文件名前 19 字符（启动时刻）分组，
  同一次启动的两个文件一起留、一起删。
- **`_2` 与 `-2` 要能共存**。`_2` 是单文件超 8 MB 后的大小拆分，`-2` 是同一秒起第二个实例的
  序号 —— 两者都会出现在文件名里，清理用的正则必须同时容得下。
- **图片 base64 绝不能进日志**。视觉请求的 base64 动辄几 MB，写进去日志瞬间膨胀。
  只记字节数。
- **超大请求体不解析**。`JSON.parse` 一个几十 MB 的对象要几百毫秒，为了日志给每次对话
  平白加延迟不划算 —— 超过 `RELAY_TRACE_PARSE_MAX`（16 MB）时只记大小。

#### 七、开关与脱敏

| 环境变量 | 默认 | 作用 |
|---|---|---|
| `LOG_TO_FILE` | `1` | 设 `0` 完全不落盘（自动化场景） |
| `LOG_CHAT` | `1` | 设 `0` 不写追踪日志，只留访问与错误摘要 |
| `LOG_DIR` | `data/logs` | 换日志目录（检查脚本写临时目录，不碰用户日志） |

脱敏只在**写文件**这一层做，控制台保持原样（用户本来就靠终端里那行密码登录）：

- 启动横幅的 `访问密码: xxx`
- `Bearer xxx` / `apiKey` / `api_key` / `api-key` / `accessKey` / `token` / `password` / `secret` / `authorization`
- 常见厂商的裸 key 形态：`sk-` / `rc-` / `hk-` / `ak-` 后接 16 位以上

**不记录请求头** —— `Authorization` 就在里面，落盘等于把 API Key 写到磁盘上。

#### 八、验证：35 项（`scripts/check-log-format.mjs`）

真的起**三次**服务（正常 / `LOG_TO_FILE=0` / `LOG_CHAT=0`）+ 一个 mock 上游，跑七种请求。
日志写进系统临时目录（`LOG_DIR` 覆盖），不碰用户的 `data/logs/`，也不会触发旧日志轮转删除。

```
日志系统检查：35 项通过，0 项失败
```

其中几条是专门防"悄悄坏掉"的：

```
ok  每一行主日志都符合 AstrBot 格式且不是空记录
ok  主日志一条记录一行（多行已被压平）
ok  日志时间戳是本地时间（不是 UTC）
ok  文件名时刻与日志内时间戳一致
ok  WARN 及以上带版本号 / INFO 不带版本号（不制造噪音）
ok  来源行号能区分不同调用点（不是全指向包装层）
ok  日志文件里没有明文访问密码
ok  控制台每条日志只输出一次（没有"原文 + 格式化"两份）
ok  非 TTY 控制台不带 ANSI 颜色码
ok  提问摘要是最后一条用户消息
ok  上游 4xx 的原因被挖出来写进主日志
ok  200 + SSE 内报错也能识别
ok  连不上时记录 ECONNREFUSED（能区分 DNS/TLS/拒绝）
ok  追踪日志含完整 system 提示 / 完整历史消息 / 完整回复原文（换行保留）
ok  追踪日志含完整上游报错响应体
ok  流式回复被拼回完整正文
ok  追踪日志多行原样保留（不压行）
ok  连不上时也记下当时问的是什么
ok  非对话接口（获取模型）的响应体不灌进追踪日志
ok  LOG_TO_FILE=0 时不产生新日志文件
ok  LOG_CHAT=0 时不生成追踪日志
```

「来源行号能区分不同调用点」这一条是这次的关键回归项：它专门盯着上面第六节那个
` [as log]` 的坑 —— 一旦有人改动 console 包装的写法，这条会立刻红。

### 8.21 Anthropic 格式必然 400：模型名里的 `-thinking` 后缀（2026-09）

用户反馈「Anthropic 有问题无法使用」。刚做好的日志系统立刻派上用场 —— 主日志里直接有：

```
[Relay] [ERRO] POST newapi.gay/v1/messages -> 400 上游 1659ms << 上游报错
[Page]  [ERRO] [Provider] 接口返回错误 HTTP 400
  {"message":"max_tokens must be greater than 1024 for manual Claude thinking (request id: …)"}
```

#### 一、别猜，去读上游源码

报错文案很怪：用户**从没开过思考模式**（`thinkingMode: false`），为什么服务商说 "manual Claude thinking"？

先把我们发出去的请求体确认清楚 —— `callAnthropicChat` 里只有 `model / system / messages /
max_tokens / temperature`，**没有任何 thinking 字段**。所以"要开 thinking"不是我们要求的。

于是去读中转站（new-api）的源码。搜索报错文案定位到
`relaykit/relayconvert/reasoning/claude.go`，再顺着找到 `suffix.go`：

```go
// ParseClaudeModelSuffix → parseProviderModelSuffix
if before, ok := strings.CutSuffix(modelName, "-thinking"); ok {
    intent := Intent{Mode: ModeEnabled, Source: SourceSuffix}      // ← 模型名后缀直接开思考
    ...
}
if marker := strings.LastIndex(modelName, "-thinking-"); marker >= 0 {
    budget, _ := strconv.Atoi(modelName[marker+len("-thinking-"):]) // -thinking-2000 指定预算
    intent := Intent{BudgetTokens: &budget, ...}
    ...
}
if before, ok := strings.CutSuffix(modelName, "-nothinking"); ok { ... }   // 显式关闭
```

#### 二、完整的因果链

```
用户按中转站说明填了模型名 claude-sonnet-4-5-thinking
  → new-api 解析模型名后缀，认定"要开 manual thinking"
  → manual thinking 要求 1024 <= budget_tokens < max_tokens（Anthropic 的硬约束）
  → 旧版 new-api 直接校验并拒绝：max_tokens 必须 > 1024
  → 我们发的是 ROLEPLAY_OUTPUT_TOKEN_LIMITS.text = 900  →  400
```

**900 这个值本身没错**（OpenAI 兼容格式下完全合法），错在它撞上了 Claude 思考链路的硬下限。

顺带发现**同一个 bug 的第二个受害者**：「设置 → 测试对话连接」按钮发的是 `maxTokens: 8`，
在 Anthropic 格式下**必然失败**。用户多半就是点它发现"用不了"的。

#### 三、修复

**① 给 max_tokens 一个下限**（`ANTHROPIC_MIN_MAX_TOKENS = 4096`）。

第一版把这段写在 `callAnthropicChat` 里；后来发现这条约束**跨格式**（见第六节），
就提到了 `callChatAPI` —— 判据只有一处，三种格式共用：

```js
// callChatAPI()：分发到三种格式之前算一次
const resolvedOpts = Object.assign({}, opts, {
    maxTokens: resolveOutputTokenLimit(settings.model, opts.maxTokens, format === 'anthropic')
});

// 判据只看模型名后缀；`-nothinking` 里没有 "-thinking" 这个子串，天然不会命中
function thinkingSuffixTokenFloor(modelName) {
    const name = String(modelName || '').trim();
    if (!/-thinking(?:$|-)/i.test(name)) return 0;          // 0 = 这个模型名没提任何要求
    const explicit = /-thinking-(\d+)\s*$/.exec(name);
    if (explicit) return Math.max(ANTHROPIC_MIN_MAX_TOKENS, Number(explicit[1]) + 1);
    return ANTHROPIC_MIN_MAX_TOKENS;
}

function resolveOutputTokenLimit(model, requested, required = false) {
    const floor = required
        ? Math.max(ANTHROPIC_MIN_MAX_TOKENS, thinkingSuffixTokenFloor(model))
        : thinkingSuffixTokenFloor(model);
    const wanted = Number.isFinite(requested) ? Math.max(1, Math.floor(requested)) : null;
    if (floor <= 0) return wanted;      // 普通模型名：原样透传（null = 不发这个字段）
    return Math.max(floor, wanted || 0);
}
```

`required = true` **只给 Anthropic 格式**：它的 `max_tokens` 是必填字段，而且那条链路
**永远**要按"可能开思考"保底（用户可以在设置里直接指定 thinking）。
OpenAI 系两种格式则**只在模型名带后缀时才抬** —— 普通模型名的 `max_tokens: 900`
不许被悄悄改大，那是用户的输出预算。

顺带把决策结果打进主日志，下次遇到这类 400 一眼能对上号：

```
[Chat] anthropic 发送 4 条：system,user,assistant,user，输出上限 4096
```

**② 为什么是 4096，而不是刚好越过阈值的 1025**

预算和正文**吃的是同一个 `max_tokens` 额度**（`budget = max_tokens × 80%`）：

| max_tokens | 思考预算 | 正文还剩 |
|---|---|---|
| 1025 | 1024 | **1** |
| 1280（新版 new-api 自己抬的值） | 1024 | 256 |
| **4096** | 3276 | **820** |

只抬到 1025 或 1280 的话，正文空间会被思考吃掉 —— 表现成 `EMPTY_MODEL_OUTPUT`
（"模型只返回了思考内容"），比直接报错更难查。

抬高**没有副作用**：`max_tokens` 只是**上限**，没开思考时模型该多短还是多短。
4096 也是 Claude 各代模型都接受的安全值（`claude-3-haiku` 的输出上限正好是 4096）。

**③ `-thinking-<数字>` 后缀要一起处理**：这种写法会**指定**预算，而中转站会把预算压到
`max_tokens - 1`（预算 ≥ max_tokens 时）。所以 max_tokens 必须严格大于它，否则正文一个 token 都不剩。

**④ 400 的提示要点破后缀的含义**。用户看到 "manual Claude thinking" 只会更迷惑（他压根没开过），
所以在 `getClientErrorPresentation()` 的 `BAD_REQUEST` 分支加了一条**条件**提示：

> 这条报错来自模型名里的 -thinking 后缀：中转站看到它就会开启 Claude 的扩展思考……
> · 不想要思考：把模型名里的 -thinking 去掉，例如 claude-sonnet-4-5-thinking → claude-sonnet-4-5；
> · 想要思考：可以用 -thinking-<数字> 指定预算，但数字越大越慢、也越贵。

触发条件是 `max_tokens` 和 `thinking` **同时**出现在上游文案里 —— 不是无条件拼上去的
（8.18 那次"图片转述"提示被写成无条件的教训）。

#### 四、验证：56 项（`scripts/check-anthropic-request.mjs`）

抠出 `callChatAPI` / `callAnthropicChat` / `convertOpenAIToAnthropicMessages` /
`convertOpenAIToResponsesInput` / `normalizeChatReply` / `getClientErrorPresentation`
的**真实函数体**，只把网络出口（`postJsonFromDevice`）换成假的，检查**真正会发出去的 body**。
不联网、不复制逻辑。

驱动的是 `callChatAPI` 而不是 `callAnthropicChat` —— 这样"下限在哪一层算"本身也被验证，
跨格式那几条断言才有意义。

```
— Anthropic 格式的 max_tokens 下限（无条件）—
ok   主对话（900）发出的 max_tokens > 1024
ok   测试连接（8）发出的 max_tokens > 1024
ok   不传 maxTokens 时 > 1024
ok   传了更大的值时不被压低（32000 不被压低）
ok   带 -thinking 后缀时给正文留出空间（>= 2048）
ok   -thinking-3000 / -thinking-8000 时 max_tokens 必须大于该预算

— 跨格式：模型名带 -thinking 后缀时同样保底 —
ok   openai-compatible + -thinking → max_tokens > 1024
ok     并且模型名原样传给上游（判据只读名字，不改名字）
ok   openai-compatible + -thinking-3000 → 大于该预算
ok   openai-compatible + -thinking + 不传 maxTokens → 也补上下限
ok   openai-responses + -thinking → max_output_tokens > 1024

— 反向：普通模型名不许被动 —
ok   openai-compatible / openai-responses + 普通模型名 → 原样 900
ok   …… + 不传 maxTokens → 干脆不发这个字段
ok   -nothinking 不触发下限（它不是 -thinking）
ok   后缀大小写混写也认（宁可多抬，不能漏）
ok   DeepSeek 官方 + 深度思考 → 换成 deepseek-reasoner，且 max_tokens 不被抬

— 端点 / 请求头 / 请求体 / 响应解析 / 错误路径 —
ok   三种格式各自的端点拼接（/v1/messages、/chat/completions、/responses）
ok   Anthropic 用 x-api-key + anthropic-version，OpenAI 系用 Bearer
ok   system 合并成一条（Anthropic 提到顶层 / Responses 走 instructions）
ok   不带 thinking 字段（应用没有主动开思考）
ok   thinking block 归到 reasoning 而不是正文
ok   只返回思考内容时抛 EMPTY_MODEL_OUTPUT（不给用户看空回复）

— 400 提示 —
ok   max_tokens + thinking 的报错 → 点明 -thinking 后缀，并给出下一步（去掉后缀）
ok   普通 400 不提 -thinking / 不提图片转述 / 走通用提示
ok   只提 max_tokens、不提 thinking 时不误判

对话请求形状检查：56 通过, 0 失败
```

「反向：普通模型名不许被动」这一组是**防过度修复**的：把下限提到公共层之后，
最容易犯的错就是让所有 OpenAI 请求的 `max_tokens` 都被抬高 —— 那会静默改变用户的输出预算，
而且不会有任何报错提示。末尾 5 项则是提示的边界回归，保证这条新提示不会像当年"图片转述"
那样变成无条件的噪音。

#### 五、注意

- **`-thinking` 是 new-api 系的扩展，不是 Anthropic 官方约定**。所以这条提示只在报错时给，
  没有写进设置界面的说明 —— 直连 `api.anthropic.com` 的用户看到会莫名其妙。
- 模型名带 `-thinking` 会让**每一次**对话都走思考（更慢、更贵）。如果用户只是照抄了中转站的
  示例模型名而并不想要思考，去掉后缀即可。
- 我们**没有**主动传 `thinking` 参数：应用内的「思考模式」开关在 Anthropic 格式下依然是空操作
  （它只在 DeepSeek 官方端点下用于换成 `deepseek-reasoner`）。这一层留给"要不要支持 Anthropic
  原生 thinking"单独决定，不混在这次修复里。
- 检查脚本里的 `extractFn()` 本身踩过一个坑：它拿**第一个 `{`** 当函数体开头，而
  `callChatAPI(messages, opts = {}, settings = …)` 的**默认参数里就有 `{}`** —— 抠出来是半截函数，
  `new Function` 会报一个和被测函数毫无关系的 `SyntaxError: Unexpected token 'class'`。
  现在抠取前先跳过参数表；另外三个脚本有同样写法，一并加固了。

#### 六、这个坑是**跨格式**的：所以下限必须提到 `callChatAPI`

第一版只修了 `callAnthropicChat`。查上游源码时发现 `RenderClaude` 只有一个调用点，
在 `relaykit/relayconvert/internal/shared/claude/reasoning.go` 的 `ApplyReasoning()`：

```go
// 原生 Claude 请求、且没有 host modifier 时直接跳过渲染
if !crossProtocol && source.IsEmpty() && suffix.IsEmpty() { ...; return nil }
...
rendered, err := reasoning.RenderClaude(...)   // 跨协议（OpenAI → Claude）一定走到这里
```

即 **用 OpenAI 兼容格式调 Claude，经中转站转协议时同样会经过这段校验**（`crossProtocol = true`）。
所以"改成 OpenAI 格式"并不能绕开这个坑 —— 只要模型名里还带着 `-thinking`，400 照样来。

**判据：报错里的 `type` 说明是谁产生的。** 那条 400 的响应体是 `type: "new_api_error"` ——
**中转站自己**产生的（不是上游返回的），说明请求已经进到中转站的处理逻辑里。
所以这不是"这个通道不可用"，而是"参数被卡住"，没必要为了它切格式或换站点。

于是下限判据被提到 `callChatAPI`（见第三节 ①）：Anthropic 格式**无条件**保底，
OpenAI 系两种格式在**模型名带后缀**时保底。
**别为了"只在 Anthropic 生效"把它挪回格式函数里** —— 那正是这次补修要修掉的问题。

### 8.22 「获取模型」在 Anthropic 格式下不可用：其实只是地址拼错了（2026-09）

用户问「为什么 anthropic 不支持获取模型功能，你做兼容给我做好了呀」。查完发现**前提就是错的**。

#### 一、Anthropic 有 Models API

界面、README、交付文档里一路写着"它的模型列表接口与 OpenAI 不同，所以不可用"。官方文档：

```
GET /v1/models
Headers: x-api-key、anthropic-version: 2023-06-01
Query:   limit（默认 20，最大 1000）、after_id / before_id（分页游标）
返回:    { data: [{ type: "model", id, display_name, created_at, max_tokens, … }],
          has_more, first_id, last_id }
```

`data[].id` 和 OpenAI 的 `{ data: [{ id }] }` **同构** —— 解析逻辑本来就能共用，
`fetchOpenAIModelList()` 里甚至已经写好了 Anthropic 的认证头分支（`x-api-key` + `anthropic-version`）。
所以"不支持"这三个字从来就不成立。

#### 二、真因：Base URL 的版本段

```js
// 旧代码
const endpoint = /\/models$/i.test(base) ? base : base + '/models';
```

而 `CHAT_API_FORMATS.anthropic.defaultBaseUrl = 'https://api.anthropic.com'`（**不带** `/v1`）——
请求打到 `https://api.anthropic.com/models` → **404**，再被上层统一翻译成
"接口未返回模型列表（data 为空），可能该服务不支持 /models"。

对比 `callAnthropicChat` 拼对话地址的规则：

```js
/\/v\d+$/i.test(baseUrl) ? `${baseUrl}/messages` : `${baseUrl}/v1/messages`
```

**同一个 Base URL：对话走 `/v1/messages`（对），模型列表走 `/models`（错）。**
这就是"为什么它不支持"的全部答案 —— 不是协议不同，是两处拼地址的规则不一致。

#### 三、修复

抽出 `modelsEndpointFor(baseUrl, apiFormat)`，让两条路径共用同一条版本段规则：

| Base URL | 对话地址 | 模型列表地址 |
|---|---|---|
| `https://api.anthropic.com` | `/v1/messages` | `/v1/models` |
| `https://api.anthropic.com/v1` | `/v1/messages` | `/v1/models` |
| `https://newapi.gay`（中转） | `/v1/messages` | `/v1/models` |
| `https://api.deepseek.com`（OpenAI 兼容） | `/chat/completions` | `/models`（**不加** `/v1`） |
| `https://api.openai.com/v1`（OpenAI 兼容 / Responses） | `/chat/completions`、`/responses` | `/v1/models` |

OpenAI 系**保持原样**（`base + '/models'`）—— 它的 Base URL 惯例上就带版本段，
而且必须和 `callOpenAICompatibleChat` 拼 `/chat/completions` 的规则一致。
给它补 `/v1` 等于把同一个 bug 换个方向再犯一次。

另外三处：

**① Anthropic 的认证降级。** `x-api-key` 是官方写法，但第三方中转（new-api 系）的 `/v1/models`
常常只认 `Authorization: Bearer`，而它只回 `authentication_error` —— 看不出是认证头用错了。
所以第一次用 Anthropic 风格，**401 / 403 / 404 时换 Bearer 再试一次**。
5xx 与"连不上"不重试（换一条路也一样失败，白打一次请求）；两条都失败时报**第一次**的原因
（Anthropic 风格那条更接近真相）。200 + `data` 为空也不重试 —— 那说明服务真的没给。

**② 补 `limit=1000`。** Anthropic 默认只回 20 条，不指定会**静默截断**（Claude 家当接近这个量级）。
OpenAI 系不加这个参数（不是所有兼容实现都认）。

**③ 把"不可用"的说法全部清掉**（设置界面 hint、README、交付文档各一处）。
一条错误结论被写进三份文档，会一直误导后来人 —— 这次就是被用户问出来的。

#### 四、验证：43 项（`scripts/check-model-list.mjs`）

抠出 `modelsEndpointFor` / `parseModelList` / `fetchModelList` / `callAnthropicChat` 的真实函数体，
只把两个网络出口（GET / POST）换成假的。

```
— 地址拼接 —
ok   Anthropic 官方裸域名 → /v1/models（原来拼成 /models，404）
ok   已带 /v1 / 结尾多余斜杠 / 粘了完整 /v1/messages / 已指向 /v1/models
ok   Anthropic 中转站裸域名 → /v1/models（和对话的 /v1/messages 对齐）
ok   OpenAI 兼容 + 裸域名 → /models（**不加** /v1，与对话地址规则一致）
ok   OpenAI 兼容 粘了 /chat/completions → 剥掉 / Responses 粘了 /responses → 剥掉
ok   没填 Base URL → 明确报"请先填写"

— 不变量：与对话地址同源 —
ok   4 组 Base URL：模型地址 == 对话地址换掉末段

— 认证头 —
ok   Anthropic 用 x-api-key + anthropic-version，不混进 Authorization
ok   Anthropic 带 limit=1000 / OpenAI 兼容用 Bearer 且不加 limit

— 响应解析 —
ok   Anthropic 的 data[].id（display_name 不参与取值）/ OpenAI 的 data[].id
ok   混着裸字符串也认 / 没有 id 的条目被过滤 / 没有 data 字段与 null → 空数组不抛错
ok   结果按字母序（下拉框里好找）

— Anthropic 的认证降级 —
ok   401 后换 Bearer 再试一次并成功（第二次确实是 Bearer，总共 2 次）
ok   404 也会退一步重试
ok   5xx 不重试，并把上游的原话透出来
ok   两条路都失败时发了 2 次，报错保留第一次的原因
ok   网络层直接抛错时不重试，原样把错误抛出去
ok   OpenAI 系 401 只发 1 次（没有"换认证头"这条路）

— 空列表 —
ok   200 + data 为空 → 说清"可能该服务不支持 /models"，且不去重试

获取模型列表检查：43 通过, 0 失败
```

「不变量：与对话地址同源」这一组是本检查最有价值的部分：它直接驱动真实的 `callAnthropicChat`
拿到对话 URL，再断言 `模型地址 == 对话地址.replace('/messages', '/models')`。
**未来任何一次地址拼接重构只要破坏这条关系，这里立刻会红** ——
而这条关系一旦破坏，症状正是"对话能通、获取模型 404"这种最费解的现象。

#### 五、注意

- **OpenAI 兼容格式不要"顺手"给裸域名补 `/v1`**。它的 Base URL 惯例上就带版本段
  （设置界面的占位符写的是 `https://api.siliconflow.cn/v1 等`），补了反而会让对话地址错。
  要改就两条路径一起改，并靠上面那条不变量守住。
- 第三方中转**是否**提供 Anthropic 风格的 `/v1/models` 取决于它自己（有的只按 OpenAI 协议实现
  `/v1/models`，靠 Bearer 认证）—— 所以才有那条认证降级。降级只在 401/403/404 时发生，
  不会把正常的错误掩盖掉。
- 视觉模型那一路（`settingVisionBaseUrl`）**没有格式选择器**，永远走 OpenAI 风格 ——
  传 `apiFormat` 为 `undefined` 时 `modelsEndpointFor` 走的就是 OpenAI 分支，符合预期。

### 8.23 日志级别可调 + 补齐 DEBUG/CRITICAL：终端全量、文件按级别收窄（2026-09）

**背景**：8.20 已经把日志格式对齐了 AstrBot，但级别这一层是**半成品**：

- `LEVEL_SHORT` / `LEVEL_NO` / `LEVEL_COLOR` 里都定义了 `CRITICAL`，**全项目零调用** —— 死配置。
- `console.debug` 没有被包装，所以 `DEBUG` 同样**没有任何产生路径**。
- 没有级别过滤，落盘永远全量。

结果是"五个级别"这件事只存在于常量表里，实际能用的只有 INFO / WARN / ERROR 三个。

**改法**（与 AstrBot 的分工一致，见 `astrbot/core/log.py` 的 `_setup_loguru` 与 `_add_file_sink`）：

> AstrBot 的 console sink 是 `level="DEBUG"` 写死的，file sink 用 `logger.level`（可配置）。
> 也就是**终端永远全量，级别只用来过滤文件**。这里照抄这个分工。

1. **终端全量、文件过滤**：`emitLog` 里加一道 `shouldLogToFile(level)`，
   低于当前级别的记录**只进控制台**。这样"排查时终端不丢东西"和"日志文件不被刷爆"同时成立。
2. **补齐产生路径**：包装 `console.debug` → DBUG；新增 `logCritical()` → CRIT。
   端口被占用等"服务根本没起来"的启动失败改用 CRITICAL —— 它不该和普通报错同一个档次。
   同时把 `logCritical` 加进 `LOG_INTERNAL_FNS`，否则来源行号会全部指向包装函数自己。
3. **浏览器上报的 debug 不再被压成 INFO**：`/api/client-log` 原来把 `debug` 映射成 `console.log`，
   于是"把级别调到 DEBUG"也看不到前端调试信息。
4. **级别可运行时修改，且持久化**：
   - 新接口 `GET/POST /api/logs/settings`（在鉴权之后 —— 它会改服务端行为）。
   - 存在 `data/logs/log-settings.json`。**跟着 `LOG_DIR` 走**而不是写死 `DATA_DIR`：
     自动化检查会用临时目录覆盖 `LOG_DIR`，写死的话跑一次检查就把用户真实的级别改掉了。
   - 优先级：`LOG_LEVEL` 环境变量 > 配置文件 > `INFO`。环境变量优先是刻意的 ——
     自动化要能强制指定级别，不受本机已保存的值干扰。
   - 非法级别返回 **400 且不改变当前值**（不静默接受）。
5. **对话追踪也能运行时开关**（原来只有 `LOG_CHAT=0` 这个启动期环境变量）。
   打开时若追踪文件还没建过会补建一个 —— 否则用户打开开关去目录里找却什么都没有。

**设置入口**：设置 → 高级 → 日志。改完**立即生效**，不需要点「保存设置」、
也不需要重启服务（这点和 Live2D 那类"改 serve.mjs 必须重启"的改动不同，必须说清楚）。

**安卓 App 上整块隐藏**（不是显示一句"不适用"）：APK 里没有 `serve.mjs`，
日志落盘这件事根本不存在。留着可点击的级别下拉框、点了却弹"修改失败"，比没有更糟 ——
所以 `refreshLogSettings()` 探测失败时给 `logSettingsSection` 加 `hidden`，
`applyLogSettings()` 也在区块隐藏时直接 return（避免陈旧点击弹出误导性的失败提示）。
这与「电脑操作权限 / 手机操作」按设备形态隐藏是同一种处理。

**为什么级别不写死成环境变量**：这个应用是双击 `启动.bat` 跑的，用户改不了环境变量。

**验证**（`check-log-format.mjs`，35 → 47 项）：

```
ok  LOG_LEVEL=WARN 时 INFO 记录不落盘（级别真的在过滤文件）
ok  LOG_LEVEL=WARN 时 WARN 仍然落盘
ok  LOG_LEVEL=WARN 时控制台仍有 INFO（终端始终全量）      ← 三条必须一起验
ok  GET /api/logs/settings 返回当前级别
ok  接口给出五个级别（DEBUG/INFO/WARN/ERROR/CRITICAL）
ok  POST 接受 AstrBot 短名 DBUG 并归一成 DEBUG
ok  POST 能关闭对话追踪
ok  非法级别返回 400（而不是静默接受）
ok  非法级别被拒后当前级别保持不变
ok  改级别后新产生的 INFO 才落盘（改之前那条没有）        ← 证明无需重启
ok  端口被占用时产生 CRITICAL 记录（级别不是死配置）
```

#### 踩坑：同一秒起两个实例时，日志文件名排序不可靠

`<时刻>.log` 与 `<时刻>-2.log` 里，`-`(0x2D) 排在 `.`(0x2E) **前面**，
按文件名排序会把**旧的**那个当成"最新"。检查脚本里必须按 `mtime` 取最新日志，
否则断言全部落在错误的对象上 —— 用例会变成假阳性（这次就先踩了一次）。

### 8.24 「获取模型」能拉到列表，却不会填进模型名输入框（2026-09）

**症状**：Anthropic 格式下点「🔄 获取模型」——列表**正常拉到了**、下拉框也出现了，
但**模型名输入框里还是原来那个名字**，一对话就 404 `model not found`。

**根因**：`populateModelSelect` 只填了 `<select>`，**从头到尾没给 `<input>` 赋过值**：

```js
select.innerHTML = '';
for (const m of models) { … select.appendChild(o); }
select.classList.remove('hidden');
select.onchange = () => { input.value = select.value; };        // 只有手动点下拉才回填
if (input.value && models.includes(input.value)) select.value = input.value;   // 只读，没写
```

而真正发给服务商的是 **input** 的值（`readProviderSettingsForm` 读 `elements.settingChatModel.value`）。
两者脱节 → 这个功能等于没生效。

**为什么在 Anthropic 上特别明显**：切换 API 格式时会自动把输入框填成该格式的预设模型名
（Anthropic 是 `claude-sonnet-4-20250514`），而它**通常不在**服务端真实返回的列表里。
于是"列表是新的、发出去的是旧的"，界面上完全看不出问题。

**改法**（`populateModelSelect` 现在按四种情况处理，**顺序很重要**）：

| 输入框当前值 | 处理 | 理由 |
|---|---|---|
| 在列表里 | 原样保留，下拉框选中它 | 尊重用户已填对的选择 |
| 为空 | 填列表第一项 | 否则"拉到了但没选"，功能等于没用 |
| 是**预设默认值**且不在列表里 | 覆盖成列表第一项 | 那是切格式时应用自动填的，留着必然 404 |
| 其它手填值且不在列表里 | **不覆盖**，但下拉框选中第一项 | 可能是中转站的私有别名，用户是故意填的 |

第一条必须放在最前：预设名**恰好有效**时也该保留，否则会把用户当前用的模型
悄悄换成列表里字母序第一个（如 `claude-haiku`）—— 那是越权改动。

**为什么以前没被发现**：`check-model-list.mjs` 只覆盖了 `fetchModelList` / `parseModelList`
（"能不能拉到"），**没有覆盖"拉到之后有没有回填"**。这次补上 12 项，覆盖上面四种分支
以及"失败时不改动输入框""按钮文案要恢复"。

**验证**（`check-model-list.mjs`，43 → 58 项）：

```
ok  下拉框被填上了
ok  下拉框显示出来（不再 hidden）
ok  输入框被覆盖成列表里的真实模型
ok    不能还是那个不在列表里的预设名
ok  用户已填且存在于列表 → 保留用户的选择
ok  用户手填的别名不在列表里 → 不覆盖输入框
ok    下拉框选中列表第一项（点一下就能换过去）
ok  输入框为空 → 填上列表第一项
ok  手动选下拉框 → 同步进输入框
ok  预设名恰好存在于列表 → 保留它（不越权换成第一项）
ok  失败时输入框原样不动
ok  失败时按钮文案恢复
```

另用 mock 上游跑了一遍真实中转链路（`/api/relay` → mock 的 `/v1/models`），
确认响应体与 `parseModelList` 的期望形状一致。

### 8.25 「data 为空」这条提示会把人带偏：地址少 `/v1` 时拿回的是网页（2026-09）

**症状**：点「获取模型」报

```
接口未返回模型列表（data 为空），可能该服务不支持 /models
```

但**服务是支持 `/models` 的**，模型只是暂时被下掉了 —— 或者更常见：**Base URL 少了版本段**。

**实测证据**（对一个 new-api 系中转站）：

```
[200] https://newapi.gay/models      content-type: text/html   ← 站点首页！
[401] https://newapi.gay/v1/models   content-type: application/json  ← 真正的接口
```

少了 `/v1` 时，请求打到了**站点首页**，拿回一整页 HTML。旧代码 `JSON.parse` 失败 →
`payload` 为 `null` → 解析出空数组 → 报"data 为空，可能该服务不支持 /models"。
**这个结论是错的**，用户会去怀疑服务商，而真正该做的是在地址末尾补 `/v1`。

**注意**：`data 为空` 本身也可能是真的（上游把模型下掉了、账号下没有可用模型），
所以不能简单删掉这句话 —— 必须**区分**下面几种情况，各自给出不同的下一步：

| 上游实际返回 | 现在的提示 | 用户该做什么 |
|---|---|---|
| `200` + HTML | 「返回的是网页（HTML），不是 API 响应」+ 实际请求地址 + 建议补 `/v1` | 地址末尾加 `/v1` |
| `200` + 非 JSON | 「不是有效 JSON」+ 原文开头 120 字 | 看原文判断拿到了什么 |
| `200` + JSON 但 `data` 为空 | 「JSON 里没有模型列表」+ 实际请求地址 | 确认该站是否真有模型 / 路径是否不同 |
| `4xx/5xx` | 上游原话（原有行为） | 按上游报错处理 |
| 网络层失败 | 保留 CORS / 中转那段说明 | 用 `启动.bat` 打开 |

关键是**每条都带上「实际请求的地址」** —— 这类问题十有八九是地址拼错，
而用户看不到代码拼出来的最终 URL，只能靠猜。

**排查这类问题的手法**（本次用到，值得记下）：日志里那条
`[Relay] GET newapi.gay/models -> 200 上游 1324ms` 就是入口 ——
它直接显示了**拼出来的路径**。`isChat` 为假时响应体不进追踪日志（刻意的，
免得模型列表把对话内容淹掉），所以要看响应体得自己用 `node` 打一次那个 URL。

**验证**（`check-model-list.mjs`，58 → 64 项）：

```
ok  200 + HTML → 点明"返回的是网页，不是 API 响应"
ok    并建议补上 /v1
ok    且写出实际请求的地址
ok  200 + 非 JSON → 报"不是有效 JSON"并附原文开头
ok    原文开头被带出来（便于判断到底拿到了什么）
```

并起了一个 mock 上游（`/models` 返回 HTML、`/v1/models` 返回 401）走真实 `/api/relay`
链路复现，确认两条路径的实际响应与上面判断一致。

### 8.26 AI「不能操作手机」：执行链路是通的，但提示词从没告诉模型它有能力（2026-09）

**症状**：安卓 App 里让 AI 操作手机，AI 好像完全不会 —— 既不点击也不读界面，
只是用角色口吻回应，或者干脆说做不到。

**排查结论：链路每一环都是好的**，逐段验证过：

```
live2d-video.js:1448  handleAgentOperation 认「手机/设备」前缀
        ↓ window.agentActions.agentPhoneOperation
index.html:6981       agentPhoneOperation → runAgentPhoneOperation
        ↓ 停止检查 + AGENT_RISK 授权
        ↓ ElainaDevice.exec
原生                   @CapacitorPlugin(name="ElainaDevice")，MainActivity 已注册
        ↓
        【AI 操作结果】回灌对话
```

**真因：系统提示词里从来没有告诉模型"你能操作手机"。**

- `LIVE2D_TAG_GUIDE` 的【操作标签】只列了 7 个应用内操作（打开视频通话 / 结束通话 /
  整理记忆 / 静音 / 取消静音 / 隐藏水印 / 显示水印），**一个 `手机*` 都没有**。
- 文件操作有专门的 `# Agent Skill：文件操作` 动态注入，**手机操作零注入**。
- 已删除的 `Agent能力说明.md` 里也**没写手机操作**，而且它**根本没被注入**
  （`index.html` 里搜不到 `agent-skill` 这个引用）—— 它只是一份给人看的设计文档。

模型不知道标签存在，就永远不会输出它。所以现象是"AI 不能操作手机"，
而实际上只要它输出 `[操作:手机查看界面]`，整条链路立刻就能跑起来。

**这类 bug 的特征值得记住：没有任何报错。** 每一环单独看都是好的，
只有"提示词里有没有这句话"这一条断了；而它断了不影响任何其它功能，
所以既不会被现有检查发现，也不会在手动点界面时暴露。

**改法**：新增 `agentPhoneSkillText()`，按**可用性**分三档注入
（"告诉了却做不到"比不告诉更糟 —— 模型会空谈甚至编造）：

| 场景 | 注入内容 |
|---|---|
| 网页版（无原生设备层） | **不注入**。界面上本来也没这块设置，提了只会让模型空谈 |
| APK 但未选实现方式 / 总开关关着 | 「当前未启用」+ **明确禁止输出标签**（会被直接拒绝）+ 引导用户去开 |
| 可用 | 10 个标签的完整清单 + 工作方式（先读控件树再点、中文输入先聚焦…）+ 反幻觉约束 |

两条提示词构建路径（`layered-v2` 与回滚用的 `legacy-v1`）**都要注入** ——
只补一边会让切回 legacy 后出现"AI 又不能操作手机了"的诡异差异。

同时把静态 `LIVE2D_TAG_GUIDE` 里补一句说明：手机/文件操作由系统单独告知，
不要凭那里猜（避免它被当成"总是可用"）。

**验证**（新增 `scripts/check-agent-phone.mjs`，已纳入 `npm run check`，23 项）：

```
ok  agentPhoneSkillText() 存在且不是空壳
ok  没有原生设备层（网页版）时不注入能力说明
ok  未启用时明确禁止模型输出手机标签（否则它会空谈/编造）
ok  未启用时引导用户去正确的位置开启
ok  可用档列出了全部 10 个手机操作标签
ok  分层版（layered-v2）注入了手机操作能力
ok  回滚版（legacy-v1）也注入了 —— 只补一边会让切回 legacy 后"又不能操作手机了"
ok  提示词里承诺的每个标签，解析器都认识
ok  解析器认识的每个标签，提示词里都告知了模型
ok  每个手机操作都有风险分级
ok  live2d-video.js 会把 [操作:手机…] 转发给主应用
ok  原生插件名与前端查找的名字一致（ElainaDevice）
ok  MainActivity 注册了原生插件（没注册就永远拿不到设备层）
ok  静态指南没有把手机标签写成"总是可用"
```

其中**「提示词承诺的标签 ↔ 解析器认识的标签」双向比对**是最有价值的一条：
这两份清单在代码里是分开维护的，任何一边加/改标签而另一边没跟上，
就会变成"AI 照做了却没人接"（或"能接却没人做"），而两者都不会报错。

另把 `agentPhoneSkillText()` 抠出来跑了四种场景，确认注入内容随设备与设置正确切换。

### 8.27 Agent 操作完成后不会自己往下走：成功路径从不回调模型（2026-09）

**症状**：AI 读取 / 写入 / 点击之后就不动了，**必须用户手动打一句"继续"**才走下一步。

**根因是一个不对称**（`web/index.html`）：

| 路径 | 行为 |
|---|---|
| **失败** | `explainAgentFailure()` → 调 `callAI()`，AI 能自己解释并重试 ✅ |
| **成功** | `insertAgentResult()` → 只把结果塞进对话，**从不调 `callAI()`** ❌ |

AI 看不到"我刚做完的事"，自然不会有下一步。

**日志铁证**（`data/logs/*.trace.log`）：执行完 `[操作:保存文件 D:\桌面\测试.txt|测试文件]`
之后，下一次请求的 `[11/11]` 是 **user（244 字）** —— 正是用户手动打的那句"继续"，
而不是由操作结果触发的续跑。对比失败路径，它自己就会再发一次请求。

**改法**：在 `insertAgentResult()`（所有操作结果的唯一收口）里接上自动续跑。
链路是自持的 —— 续跑 → AI 输出新标签 → 执行 → `insertAgentResult` → 再续跑；
AI 不再输出标签就说明任务结束，链路自然终止。

三条硬约束（每条都对应一个容易写错的地方）：

1. **有预算**：`AGENT_CONTINUE_MAX_STEPS = 8`。否则"读文件 → 续跑 → 再读 → 再续跑"会烧光额度。
   失败路径早就因为同样的理由加了 `AGENT_FAILURE_MAX_RETRY`。
2. **停止优先**：续跑入口与渲染前都检查 `isHalted()`，用户按了停止绝不能再自动往下走。
3. **合并触发**：一条回复可能有多个操作标签，结果会连着插进来 —— 用 400ms 防抖合成一次续跑，
   否则会并发打出好几个请求。

**实现时踩到的三个坑**（都已加回归检查锁住）：

- **死循环**：达到上限时插的提示本身又触发了续跑 → 再到上限……无限循环。
  修法是让 `insertAgentResult` **默认不续跑**（见下条），系统说明性文字自然就不会递归。
- **预算不重置**：`resetAgentContinueBudget()` 一开始**没有任何调用点**，
  等于"整个页面会话共享 8 步"，用完后永远不再续跑 —— 表现为"第一次能用、后面又不动了"，
  比原 bug 更难查。已挂到 `handleUserInput`（每条新的用户消息重置）。
- **提前收尾**：`drive()` 里的操作是异步的（要先弹授权、再发请求），
  若靠"等一下再看 `agentContinueTimer`"判断有没有后续，会误判成"AI 已收尾"而提前 `endRun`，
  把多步任务腰斩。改成**同步**看回复里有没有 `[操作:…]` 标签。

#### 默认必须"不续跑"（选择加入，而非选择退出）

`insertAgentResult()` 是 **18 处调用**的公共收口，其中大多数是
「无权限 / 未启用 / 用户取消 / 未识别」这类**被拒绝**的结果。若它们也触发续跑，
就变成"被拒绝 → 续跑 → 又试一次 → 又被拒绝"的无效循环 ——
用户会看到 AI 对着一个做不到的事反复尝试（这正是实现中途出现过的现象）。

所以签名是 `insertAgentResult(text, { continueLoop })`，**只有显式 `true` 才续跑**，
且只在两处真的"做完了某件事"的地方开启：

| 位置 | 结果 |
|---|---|
| `doAgentFile()` 成功分支 | `{ continueLoop: true }` |
| `runAgentPhoneOperation()` 执行成功 | `{ continueLoop: true }` |

失败说明、被拒绝、已停止、已达上限 —— 一律**不续跑**。
这也顺带解决了"失败路径的 `AGENT_FAILURE_MAX_RETRY = 1` 与续跑上限 8 互相打架"的问题：
失败走它自己的重试预算，续跑只管成功后的推进。

另：`beginRun()` 会清空 `approvedThisRun`，所以只在"未运行"时才调用 ——
无条件调用会让「同一次运行内允许过就不再问」在每次续跑时失效（用户点了允许，下一步又问）。

**验证**（新增 `scripts/check-agent-continue.mjs`，29 项，已纳入 `npm run check`）：

```
ok  insertAgentResult 默认**不**续跑，只有显式 continueLoop:true 才续跑
ok  insertAgentResult 里确实会触发续跑（这是原 bug 的修复点）
ok  只有"真的做完了"的两处开启续跑（文件成功 + 手机成功）
ok  文件操作成功处开启续跑
ok  手机操作成功处开启续跑
ok  「已跳过」类结果不续跑
ok  「已取消写入」不续跑
ok  失败说明不续跑
ok  达到上限时的提示不开启续跑（否则无限循环）
ok  达到上限后立即 return（不再发起请求）
ok  每条新的用户消息都重置预算（否则整个会话共享 8 步，用完永远不再续跑）
ok  用**同步**判断回复里有没有操作标签（drive() 是异步的，靠定时器判断会误判）
ok  有后续操作时 return，交给下一轮 insertAgentResult 接力
ok  续跑前后都检查 isHalted（用户按停止后绝不能再自动往下走）
ok  只在未运行时 beginRun（无条件调用会清空"本次已授权"，导致反复弹确认）
ok  提示词要求"任务完成就别再发标签"（否则会一直撞上限）
ok  失败路径仍然会调 callAI（AI 自己解释+纠正）
```

并写了**端到端仿真**（用假的 `callAI` + 假 `Live2DCall.drive` 串起真实函数体）：

- **成功场景**：`callAI` 恰好 2 次（续跑 + 收尾）、运行状态正常结束；
- **失败场景**：`callAI` **0 次**（失败不触发续跑，避免对着做不到的事反复试）；
- **恶意场景**（AI 每次回同一个失败操作、永不收尾）：恰好 8 次后停下，
  出现"先停在这里"提示，等待后次数不再增长 —— 闸门有效。

### 8.28 上一条修完后暴露的两个问题：写文件永远失败 + 续跑回复不显示（2026-09）

装上 8.27 的版本后现象依旧，而且多了一个新症状。查日志发现是**两个独立的 bug**，
8.27 的续跑只是把第一个 bug 的"症状"放大了。

#### 一、写文件**从来没成功过**：把同源接口交给了中转

日志里这一行是决定性的：

```
[Relay] [WARN] [serve:1777]: 目标地址无效：/api/agent/write
```

`doAgentFile()` 的 write 分支用的是 `postJsonFromDevice('/api/agent/write', …)`，
而这个函数是给**外部服务商**用的 —— 它会走本机中转 `/api/relay`（用来绕 CORS）。
把**相对路径**交给中转，中转的 `new URL('/api/agent/write')` 直接解析失败。

而 `ls` / `read` 分支走的是同源 `fetch`，所以一直正常 ——
**表现为"读取能用、写入不行"**，非常有迷惑性（用户会以为是权限或路径问题）。

修法：write 分支改用同源 `fetch('/api/agent/write')`，与 read/ls 一致。

> 为什么 8.27 之后"看起来更糟"：写入一直失败 → 走的是 `explainAgentFailure` 失败路径
> → 它本来就会调 `callAI` → 但那次请求又失败了（见下条），于是什么都不显示。
> 续跑（只在成功时触发）自然一次都没跑起来。

#### 二、续跑产出的回复不显示，得再发一句话才冒出来

8.27 的 `continueAgentLoop()` 自己拼了一套"存消息 + 渲染"，但漏了
`handleUserInput` 的 `commitAiMessageOnce()` 做过的几件事，其中**最关键的是
`removeThinkingMessage()`**：

「伊蕾娜正在想…」气泡一直挂在对话末尾，新回复被压在它下面 ——
消息其实已经进了 `conv.messages` 并 `saveConversations()` 落盘，
所以"再发一句话"触发了重新渲染，上次的回复就冒出来了。
这正是用户描述的"AI 完成后没出现回复内容，得再发一句才能看见"。

修法：续跑渲染与 `commitAiMessageOnce` 对齐 ——
`removeThinkingMessage()` → 判当前会话与 notes/diary 模式 → 防重复插入 →
`state.voiceState = 'idle'` → `updateUI()`。

**教训**：同一个"提交一条 AI 消息"的动作在两处各写了一遍，就一定会漏。
这类重复实现要么抽成一个函数，要么用回归检查把两边必须一致的点锁住（本次选了后者）。

**验证**（`check-agent-continue.mjs`，29 → 38 项）：

```
ok  续跑渲染前移除"正在想"气泡（不移除会一直挂着，回复看起来没出现）
ok  渲染前判当前会话与 notes/diary 模式（与 commitAiMessageOnce 一致）
ok  渲染前防重复插入同一节点（幂等）
ok  续跑后把语音状态复位
ok  续跑后刷新 UI
ok  doAgentFile 不用 postJsonFromDevice 打同源 /api/ 接口（会当中转目标 → 地址无效）
ok  写文件走同源 fetch
ok  读/列目录也走同源 fetch（两边保持一致）
ok  全项目没有"用设备层出口打同源 /api/ 接口"的写法
```

最后一条是**全局扫描**：不止修这一处，而是禁止整类写法再出现
（`postJsonFromDevice` / `getJsonFromDevice` 的参数不该是 `/api/` 开头的相对路径）。

另起真实服务验证了写入链路：`POST /api/agent/write` 返回 200，
文件确实落盘，UTF-8 中文与 emoji 内容正确。

### 8.29 数据分类落盘 + 导入导出：所有数据挤在一个文件里（2026-09）

**问题**：跨设备数据全在 `data/store.json` 一个文件里（32.3 KB）。实测占比：

| 键 | 大小 | 占比 |
|---|---|---|
| `elaina_open_conversations` | 11.1 KB | 34% |
| `elaina_open_character_cards` | 9.6 KB | 30% |
| `elaina_open_character_card` | 8.0 KB | 25% |
| 其余 9 个键 | ~3.6 KB | 11% |

三个问题：

1. **一个文件写坏 = 全部数据一起丢**；
2. **人设卡没法单独分享**（混在聊天记录里，导出一份就是全部）；
3. 用户看不出"哪些文件是我的数据"，升级前想备份也不知道该存什么。

**做法**：新增 `server/store.mjs` 作为存储层，按用途分文件：

```
data/
├─ store.json           设置 / API Key / UI 偏好（迁移后 1.5 KB）
├─ characters/          人设卡 —— 一卡一文件
│  ├─ index.json          卡片顺序 + 当前选中
│  └─ <卡片id>.json
├─ conversations.json   聊天记录
└─ memory.json          记忆
```

**关键设计：前端一行都没改。** 前端仍走"键值同步"（`localStorage` 包装 → `POST /api/store`），
存储层负责把整块键值**拆成文件**（写）和**拼回键值**（读）。
所以 `data-sync.js`、APK 侧、所有前端调用点全部不动 —— 这是把风险压到最低的关键。

**自动迁移**：启动时检测 `store.json` 里是否还留着分类键，有就搬出去。
搬完这些键会从 `store.json` 消失（只写 misc），所以**天然幂等** —— 重复启动不会重复迁移。
迁移前会检查目标文件是否已存在，**存在就不覆盖**（避免第二次启动把新数据盖回旧的）。

**导入导出**：`GET /api/data/export`、`POST /api/data/import`、`GET /api/data/info`。
导出是**单个 JSON**（不是 zip）—— 不引依赖，用户能直接看懂、能编辑。
导入默认 **merge 模式**：只覆盖备份里有的键，本机其它数据保留（比 replace 安全）。

**UI**：设置 → 高级 → 我的数据。与日志设置同样处理：APK 没有本地服务，整块隐藏而不是留一堆点了报错的按钮。

**验证**（两个新脚本，共 94 项断言）：

- `check-store-layout.mjs`（47 项）：分类落盘、读回、删卡清残留、坏数据不覆盖、
  非法 id 不写到目录外、迁移、迁移幂等、导入导出往返、导入边界
- `check-data-api.mjs`（47 项）：起真实服务走真实 HTTP，验证迁移日志、
  `/api/store` 读回、`/api/data/*` 全部行为、写入后导出仍完整、重启后落盘

**踩坑记录**：

1. **`DATA_DIR` 原本不可覆盖** —— 检查脚本会写到用户的真实 `data/`。
   加了环境变量支持（`DATA_DIR`），测试用临时目录，绝不碰真实数据。
2. **前端首次启动会种入一张内置角色卡**（`builtin_deepseek_whale`），
   所以"造 1 张卡"的测试看到的是 2 张。这是设计行为（"删掉也会自动补回"），不是 bug ——
   但一开始误判成了"页面覆盖了服务端数据"，专门写了个探针才查清。
3. **数据区在「高级」Tab 里**，打开设置时默认停在别的 Tab，按钮是 `display:none`。
   浏览器测试一开始用 `evaluate` 强制点击，**那是个假通过** ——
   改成真实的 `switchSettingsTab('tab-advanced')` 才发现问题。
4. **自定义确认框的确认按钮**要用 `#customModalConfirmBtn`，
   不能按文字找（弹窗标题也叫"导入备份"，会点到标题上）。

### 8.30 五个 Playwright 测试无限挂起：硬编码了 Edge 路径（2026-09）

**问题**：跑 `npm test` 时 `android-app/test/` 下 5 个测试全部卡死到超时，看起来像"代码被改坏了"。

**原因**：每个测试各自硬编码了同一个路径：

```js
const edgePath = 'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe';
await chromium.launch({ executablePath: edgePath, headless: true });
```

本机只装了 Chrome、没有 Edge。`chromium.launch()` 拿到不存在的路径时**不是报错，而是挂住** ——
所以表现是"无限超时"而不是"启动失败"，排查方向容易被带偏。

**做法**：新增 `android-app/test/browser.mjs` 统一探测（环境变量 `BROWSER_PATH` → Edge → Chrome →
Linux/macOS 常见路径 → 抛出带指引的错误）。5 个测试改成共用它。

**效果**：从"无限挂起"变成 1.8 秒跑完。修完才看到真实状态：

| 测试 | 结果 |
|---|---|
| `voice-race` | pass=1 fail=0 |
| `byok-provider` | pass=1 fail=0 |
| `tts-providers` | pass=1 fail=0 |
| `chat-adapters` | fail=1（`Failed to fetch`） |
| `context-structure` | fail=1（提示词断言） |

后两个**用拆分前的备份跑同样失败**，确认是原有问题、与本次改动无关。

### 8.31 导出备份在装了迅雷的机器上失败：机器级扩展抢走了下载（2026-09）

**问题**：浏览器里点「导出备份」，拿到的文件是 **0 字节**（有时干脆什么都不下）。

**排查过程**（这一步走了弯路，记下来避免重复）：

1. 后端直连验证 → **HTTP 200 + 13580 字节 + PK 头**，完全正常
2. 同一请求带浏览器 UA + Referer → 同样 200 / 13580 字节
3. 但浏览器里 fetch 拿到的是 **HTTP 204 + 0 字节**

`204` 是"成功但无内容"。搜遍 `serve.mjs`，**只有 `/favicon.ico` 会返回 204** ——
导出路径根本不可能产生它。**所以 204 不是后端发的，是中间有人改写了响应。**

**根因**：迅雷往 Chrome 里装了**机器级扩展**：

```
HKLM\SOFTWARE\WOW6432Node\Google\Chrome\Extensions\ncennffkjdiamlpmcbajkmaiiiddgioo
    path    = %LOCALAPPDATA%\ChromeExtensionCache\xl_ext_chrome.crx     ← "xl" = 迅雷
    version = 3.52.14
HKCU\SOFTWARE\Google\Chrome\NativeMessagingHosts
    org.freedownloadmanager.fdm5.cnh                                     ← FDM 的原生消息宿主
```

它看到响应带 `Content-Disposition: attachment` 就把这个 URL 抢走交给自己的下载引擎，
浏览器侧只留一个被改写的空响应。用户既拿不到文件、也不知道为什么，
**还会被弹一个下载器窗口**（如果装了迅雷/FDM，正在用电脑时突然弹窗非常打扰）。

**为什么"用独立 profile 就没事"是错的**：`HKLM` 是**机器级**设置，
任何 Chrome 实例启动时都会读它 —— 包括自动化测试用的临时 profile。
只有用户手动装的扩展才随 profile 走。

**做法**（两处一起改，缺一不可）：

| 位置 | 改法 |
|---|---|
| 后端 `serve.mjs` | **不发** `Content-Disposition: attachment`，文件名改走自定义头 `X-Backup-Filename` |
| 前端 `index.html` | fetch 成 blob → `URL.createObjectURL` → `<a download>` 保存 |

`blob:` 是内存地址，外部下载器无从接管，也不会弹它的窗口。
另外前端**必须检测 `blob.size === 0`** 并明确报错 —— 否则会静默存下一个 0 字节的坏文件，
用户以为备份成功了，真要用的时候才发现是空的（这比报错危险得多）。

**守护**：新增 `scripts/check-export-download.mjs`（15 项），
既查源码（用没用 blob 下载、有没有检测空响应），也查真实响应头
（确认没有 `Content-Disposition`、有 `X-Backup-Filename`）。
**它不触发任何真实下载** —— 避免测试本身又弹出下载器。

**教训**：涉及下载的功能**不要在浏览器里做自动化测试**。
这台机器上任何 Chrome 实例都会加载迅雷扩展，一点下载就弹窗打扰用户。
验证下载正确性用 `Invoke-WebRequest`（纯 HTTP，不经浏览器）。

### 8.32 设置新增「能力」分栏：把"给 AI 开多大权限"从「高级」里分出来（2026-09）

**问题**：记忆、定时任务、电脑操作权限、手机操作这四组都塞在「高级」里，
而「高级」同时还有日志、我的数据、访问密码 —— 前者是"AI 能做什么"，
后者是"程序怎么跑"，两类混在一起，找设置得从头翻。

**做法**：新增「能力」Tab（排在「高级」前），把前四组移过去：

| Tab | 放什么 |
|---|---|
| **能力** | 记忆（自动整理）、定时任务、电脑操作权限、手机操作 |
| **高级** | 日志、我的数据、访问密码 |

**关键：移的时候 id 与 DOM 结构一字未改**，所以所有 JS 引用
（`elements.settingAutoMemory`、`#scheduledTaskList`、`#agentPermissionSection`、
`radio[name=agentPermission]`、`syncAgentSectionsByDevice()`）全都不用动。
`check-dom-refs.mjs` 一次通过（142 个引用 0 处问题）。

**踩坑记录**（这次编辑失误，值得记）：

第一次移动时用 `edit` 替换，**把三个区块删掉了却没插入新位置** ——
`每 N 轮对话后自动整理记忆` 和 `定时任务（AI 未来消息）` 在文件里变成 0 次。
而且新面板的开头 id 写错成了 `dataSettingsSection`（和「我的数据」撞车）。
幸好 `check-dom-refs.mjs` 和 id 唯一性检查立刻暴露了问题。

**从 git 恢复时又踩一个坑**：用 PowerShell 的 `>` 重定向导出
`git show HEAD:web/index.html`，**文件被写成 GBK**，Node 读出来匹配不到中文。
改用 `cmd /c "git show ... > file"`（二进制安全）才拿到正确内容。
**教训：导出 git 内容给 Node 处理时，别用 PowerShell 重定向。**

**验证**：
- 结构检查（Tab 配套 / id 唯一 / 元素归属 / div 与 section 逐行平衡）29 项全过
- 浏览器实测 13 项：Tab 栏顺序正确、点「能力」三组都可见、
  桌面端手机操作区自动隐藏、记忆开关能读到值、权限单选项正常、
  切回「高级」日志与我的数据仍在
- **测试全程没碰任何下载按钮**（见 8.31 的教训）

### 8.33 三种对话协议拆到独立模块：表驱动分发 + 依赖注入（2026-09）

**背景**：`index.html` 里混着三套对话协议实现（openai-compatible / anthropic /
openai-responses）外加两个消息转换器，共 176 行。它们与界面毫无关系，
但混在 13,000 行里，导致"改对话协议"和"改界面"互相干扰。

**做法**：新建 `web/js/chat-providers.js`（246 行），把这 6 个函数整体搬过去，
`index.html` 里只留三处接线。

| 位置 | 改动 |
|---|---|
| `<head>` | 加 `<script src="/js/chat-providers.js">`（必须在主脚本前） |
| `callChatAPI` 之前 | 挂 `window.ChatDeps = { 10 个依赖 }` |
| `callChatAPI` 里 | 两行 `if` 分支 → `window.ChatProviders.forFormat(format)(...)` |
| 原位置 | 删掉 6 个函数（176 行） |

**两个设计决定**：

1. **依赖注入用 `window.ChatDeps`，而不是 ES module**
   普通 `<script>` 之间作用域独立，拆出去后直接调 `getChatBaseUrl()` 会拿到 undefined。
   挂到 window 的好处是**调用方一行都不用改**。
   而且 `deps()` 是**每次进函数取一次**、不在模块顶层缓存 ——
   顶层缓存在"本文件先加载、ChatDeps 后挂上"时会拿到 undefined，
   且那种失败是**静默的**（函数存在，调用时才炸）。

2. **用表分发，不用 if 链**
   `if` 链要改两处（加分支 + 记得位置），表只要加一行，且表能枚举出来给测试断言。

**兼容性验证（这是重点）**：

| 检查 | 结果 |
|---|---|
| 19 项关键逻辑逐字一致（端点拼接 / 认证头 / 字段名 / 默认模型…） | 全过 |
| 三个函数体行数对比 | 每个只多 1 行（`deps()` 解构） |
| `check-chat-providers.mjs`（新增，50 项） | 50/50 |
| `check-anthropic-request.mjs` | **43 → 56 项全过** |
| `check-chat-message-shape.mjs` | 17/17 |
| 真实浏览器验证（20 项） | 20/20 |
| `npm run check` | EXIT=0 |

**意外收获：一个测试变得更好了。**
`check-anthropic-request.mjs` 原来是从 `index.html` 里**正则抠函数**再 `new Function` 执行 ——
拆完之后抠不到了，于是改成**直接加载真模块**（`vm.runInContext` 执行
`chat-providers.js`，喂给它 `window.ChatDeps`）。
**测的从"源码文本"变成了"线上真正跑的那份代码"**，而且断言从 43 项涨到 56 项。

**踩坑记录**：

1. **删除时的安全检查拦住了自己**。写了个脚本按行区间删函数，删前扫描"这些函数有没有被区间外引用"。
   它报 `callAnthropicChat 在区间外被引用: L12377` —— 查下来是**注释里提到**
   （"这条规则必须和 callAnthropicChat 拼 /messages 的规则保持一致"）。
   我的注释过滤只处理了 `//` 和行首 `*`，没跟踪多行注释状态，于是误报。
   改成逐行跟踪 `/* */` 状态后通过。**这个拦截是有价值的** —— 它逼我确认了那处不是真调用。
2. **`Measure-Object -Line` 与 `split('\n')` 口径不同**，一度以为文件被删坏了
   （PowerShell 报 12,336 行，Node 实测 13,048）。核对了 `<div>`/`<section>` 开闭
   与关键函数是否都在，确认文件完好。**核对行数要用同一种口径，或直接查结构。**
3. **`<script>` 计数 12 开 11 关** 是虚惊：多出来的那个在**注释文字**里
   （"那是普通 `<script>`（不是 ES module）…"）。`check-inline-scripts.mjs`
   的正则只认真标签，所以没受影响。
4. **拆完之后漏改了两个检查脚本**。`check-anthropic-request.mjs` 和
   `check-model-list.mjs` 都在用 `extractFn('callAnthropicChat')` 之类从 index.html 抠函数，
   函数搬走后它们直接抛"找不到函数"。**`npm run check` 当场就红了** —— 这说明
   "抠源码"这种测试方式本身就是脆的（改结构就断）。
   两个脚本都改成**加载真模块**（`vm.runInContext` 执行 chat-providers.js），
   顺带把断言从 43 项涨到 56 项（anthropic）。
   **教训：搬迁函数后要全项目扫一遍 `extractFn('旧名')`**，别只看新调用点。

### 8.34 APK 侧数据备份：安卓版没有后端，「我的数据」原本整块是隐藏的（2026-09）

**问题**：`/api/data/*` 依赖 `serve.mjs`，而 APK 里没有 Node 后端 ——
所以「我的数据」在 APK 里**整块被隐藏**（`fetch` 失败 → `classList.add('hidden')`）。

**但 APK 恰恰是最需要备份的地方**：数据存在应用私有目录（WebView 的 localStorage），
**卸载即丢失**，用户连手动拷出来都做不到。Web 版至少能直接去 `data/` 目录拷文件。

**做法**：新增 `web/js/chat-backup.js`（约 330 行，零依赖），在 APK 侧直接读写 localStorage。

| 设计点 | 说明 |
|---|---|
| **格式与 Web 版同构** | 同一套 zip 结构（manifest / settings / characters/ / conversations/ / memory.json） |
| **两边可互相导入** | APK 导出 → 电脑 Web 版导入 ✅；Web 版导出 → 手机导入 ✅ |
| **自己实现 zip** | 浏览器没有 `node:zlib`，而引第三方库会破坏零依赖。用 **store 模式（不压缩）** —— 备份本来就不大，不值得为压缩引依赖 |
| **读 deflate** | Web 版导出的 zip 会用 deflate（后端 `createZip` 对大文件压缩）。APK 侧用浏览器内置的 `DecompressionStream('deflate-raw')` 解 —— **不引依赖**，现代 WebView 都有 |
| **只认白名单键** | 导入时只写 `BACKUP_KEYS` 里的键，防止备份文件里的奇怪键污染 localStorage |
| **`elaina_open_character_card` 不单独存** | 它只是"列表里当前选中那张"的副本。导出时只存 `characters/` + `currentCardId`，导入时由 `currentCardId` 重建 —— 避免两处不一致 |

**`refreshDataSettings` / `exportDataBackup` / `importDataBackup` 都加了 APK 分支**：
`IS_NATIVE_APP && window.ChatBackup` 时走本地路径，否则走 `/api/data/*`。

**导出/导入具体走什么（这里有个必须避开的坑）**：

| 动作 | 做法 | 为什么不能图省事 |
|---|---|---|
| **导出** | `Capacitor Filesystem.writeFile` 写进 **Documents 目录** | **安卓 WebView 不处理 `blob:` 下载** —— 桌面浏览器会把 blob 交给下载器，WebView 里点 `<a download>` 通常**毫无反应**。这是 Capacitor 社区的常见坑。所以 APK 侧必须真的写文件，不能复用 Web 侧的 blob 方案 |
| **导入** | 先 `readdir` 列出 Documents 里已有的备份让用户选序号（纯 Filesystem），挑不到再退回 `<input type="file">` | 各厂商 ROM 对 WebView 文件选择器的支持差异很大（有的只能选图片）。而"自己刚导出的备份就在文档目录里"是**最常见场景**，走 Filesystem 最可靠 |

写入用分块的 `nativeWriteFile`（复用 Live2D 模型那套）—— Capacitor bridge 对单次
base64 有大小限制，一次性写大备份会失败。

**验证**：
- `check-chat-backup.mjs`（新增，50 项）：键清单与 `data-sync.js` 的 SYNC_KEYS 逐项对齐、
  导出的 zip 能被**后端的读取器**解开（交叉验证）、往返一致、
  **能读 Web 版导出的 zip（含 deflate 条目）**、能读 v1 旧备份、边界与越界键
- 浏览器实测 20 项：Web 环境与**模拟 APK 环境**（注入假 Capacitor + 拦掉 `/api/*`）都正常，
  APK 下「我的数据」不再隐藏、能生成合法 zip
- **假 Filesystem 插件实测 16 项**：确认导出真的调 `writeFile`（而不是 blob 下载）、
  写出的字节是合法 zip、导入走 `readdir` + `readFile` 并完整还原
- **测试全程不点导出按钮**（见 8.31 的教训：这台机器上点下载会弹迅雷）

### 8.35 迁移遗留的旧单文件没被清理（2026-09）

**问题**：`data/conversations.json`（12 KB，旧的单文件形态）在迁移完成后**仍然留在磁盘上**。

**根因**：清理代码被放在 `if (moved.length)` 里面：

```js
if (moved.length) {
    ...
    if (legacySingleConv) await rm(path.join(dataDir, 'conversations.json'), { force: true });
}
```

于是出现这个残留路径：**上一次迁移已经把对话拆进 `conversations/` 了**
（所以这次 `alreadySplit` 为真、`moved` 为空），但旧文件因为某种原因没被删掉 →
**它永久留在磁盘上**：不会被读取（`loadStore` 只认 `conversations/`），
也不会被清理，白占空间还让用户困惑"到底哪个才是我的数据"。

**做法**：把清理移到 `if (moved.length)` **之外**，并给出安全判据：

> 只要新的 `conversations/` 里有内容（说明数据已经安全落位），旧文件就是冗余的，可以删。
> **没内容时不删** —— 那是唯一的数据来源。

**验证**：`check-store-layout.mjs` 新增 7 项（现 78 项），覆盖两种情况：
- 已拆好 + 旧文件残留 → 旧文件被清理，且**不会**把旧文件里的数据也导进去
- 只有旧文件（新目录为空）→ 正确迁移，**不删**（唯一来源）

在真实数据上也验证了：启动日志打出"清理了遗留的旧单文件 conversations.json"，
`data/` 现在只剩 `characters/`、`conversations/`、`memory.json`、`store.json`。

### 8.36 记忆也拆成目录：`memory.json` → `memory/<类目>.json`（2026-09）

**背景**：人设卡与聊天记录都拆成一类/一条一文件了，只有记忆还是一个 `memory.json`。
而记忆内部本来就是**六个类目**（`diary` / `promise` / `preference` / `plan` / `motivation` /
`pivotal_memory`，见前端 `emptyMemoryCore()`）。

**为什么值得拆**：这六类的**增长特性完全不同** ——

| 类目 | 增长特性 |
|---|---|
| `diary` | 越写越长（每条几百字），是唯一会持续膨胀的 |
| `promise` / `preference` / `plan` / `motivation` / `pivotal_memory` | 很小、很少变 |

混在一个文件里意味着：**加一条日记要把整个记忆重写一遍**。拆开后互不干扰，
用户也能单独看/备份/删掉某一类（比如只想清日记）。

**做法**：`server/store.mjs` 加 `writeMemory()` / `readMemory()`，
按类目写 `memory/<类目>.json`（`{version, value}` 形态，与单键文件一致）。

- **未知类目也写出去**（不丢数据）—— 前端将来加字段时不用同步改后端
- **类目名过白名单**（`SAFE_ID`）—— 它直接当文件名用，防 `../../x` 这类键
- 已删除的类目会清掉残留文件

**迁移**：与 `conversations.json` 同一套逻辑 ——
识别两个来源（`store.json` 里的键 / 旧的单文件 `memory.json`），
新位置有内容就不覆盖，旧文件在新位置就绪后清理。

**导出/导入同步**：zip 里从 `memory.json` 变成 `memory/<类目>.json`；
**导入时两种都认**（`memory/<类目>.json` 与旧的 `memory.json`），老备份照样能导。
APK 侧（`chat-backup.js`）也做了同样的事，保持两边同构。

**验证**：
- `check-store-layout.mjs` 79 项、`check-data-api.mjs` 64 项、`check-chat-backup.mjs` 50 项，全绿
- 真实数据迁移后核对：六个类目文件都在，**日记内容完好**
  （"用户多次错叫我为伊雷娜等名字…"）
- 浏览器实测 10 项：记忆能读回（六类目正确拼回）、人设卡 2 张 / 对话 1 个 / 设置 33 项不受影响

### 8.37 AI 手机操作：每步都要切回应用确认 → 系统悬浮窗（2026-09）

**用户反馈**：AI 操作手机时，每一步都要确认，而点确认必须切回应用内部 —— 于是卡死了。

**根因**（这是真实的架构缺陷，不是配置问题）：授权确认走的是**应用内弹窗**
（`showCustomModal`），但 AI 操作的是**别的 App**。链条是：

```
AI 点击目标 App 的按钮
  → 弹授权框（在 ElainaChat 内部，用户此刻在别的 App，看不到）
  → 用户切回 ElainaChat 点「允许」
  → 「切回来」这一步把目标 App 切到后台
  → 那次点击落到错的位置 / 直接失败
  → AI 重试 → 又弹窗 … 死循环
```

而且 `手机点击 / 滑动 / 输入 / 按键 / 打开` 全被归为 `sensitive`，默认策略是
「每次都询问」—— 一次任务十几步就是十几次确认。

**修法**：用 Android 的**系统级悬浮窗**（`SYSTEM_ALERT_WINDOW` +
`TYPE_APPLICATION_OVERLAY`）。它浮在**目标 App 之上**，用户不用离开当前界面就能
看到进度、随时点停止、需要确认时就地允许。

- 新增 `ElainaOverlay.java`。**不持有 Activity** —— 挂在 ApplicationContext 上，
  所以应用退到后台、页面被回收，浮层都还在（这正是"随时能停"的前提）
- `ElainaShellPlugin` 暴露 6 个方法：`overlayStatus / requestOverlay / showOverlay /
  updateOverlay / askOverlayApproval / hideOverlay`
- 两条事件回前端：`overlayStop`（点停止）、`overlayApproval`（点允许/拒绝）
- 前端 `requestApproval` 改为**先试悬浮窗、不可用时回退应用内弹窗**。
  回退是必需的 —— 不能因为浮层不可用就静默拒绝，那会让功能看起来"坏了"

**踩到的坑（自己引入的真 bug）**：用户在浮层上点「停止」而不是「允许/拒绝」时，
`requestApprovalViaOverlay` 里那个 Promise 的 resolve 存在 `overlayApprovalPending`，
**没人调 → `await` 永远挂住**。表现是"点了停止没反应"—— 而这恰恰是最需要它立刻停的时刻。
修法：`requestStop` 与 `overlayStop` **两条路都要 `pending(false)`**。
`check-agent-overlay.mjs` 专门盯这一点。

**权限说明**：`SYSTEM_ALERT_WINDOW` 是 Android 的特殊权限，
**只能由用户在系统设置里手动开启**，应用无法弹运行时权限框索取。
所以设置里做成"跳过去 + 回来复检"，并在拿不到权限时明确提示用户
"必须切回应用才能点允许，而切回来会让被操作的应用退到后台"。

**验证**：`check-agent-overlay.mjs` 42 项；`aapt2 dump permissions` 确认
`SYSTEM_ALERT_WINDOW` 真的进了 APK。

### 8.38 打包进 APK 的 Live2D 模型识别不到：一个单点故障（2026-09）

**用户反馈**：装好 APK，Live2D 模型列表是空的（"之前版本也有，只是没空修"）。

**排查过程**（这一步值得记：**先证明不是哪一层的问题，再动代码**）：

1. 手工解 zip + `aapt2`：**模型确实在 APK 里** —— `assets/public/live2d/models/`
   下 70 个文件，`manifest.json` 也在，两个模型（deepseek / 伊蕾娜·默认）齐全
2. `capacitor.plugins.json` 也在 APK 里，FilesystemPlugin 会被自动注册
3. 纯 Node 复现（从 `index.html` 抠出真实函数 + 假 Filesystem）：
   **播种与列表逻辑本身是通的** —— 能写入 69 个文件、接口能返回 2 个模型

所以坏的不是"模型没打进去"，而是**取模型那条链路上某一环在真机上断了**。

**根因（架构层面）**：原来的实现**只有一条路**：

```
1. 启动时把 assets 里的模型"播种"到应用数据目录（依赖 Capacitor Filesystem 插件）
2. 列模型时只扫**数据目录**
```

第 1 步只要失败（插件不可用 / 权限被拒 / 存储写满 / ROM 限制 / WebView 版本差异），
数据目录就是空的，列表跟着空 —— **而 assets 里那份完好无损**。
一个"锦上添花"的播种功能，把"内置模型可用"这件事一起拖垮了。

**修法**：列模型时把**打包清单**合并进来。

- 新增 `bundledManifestModels()`：直接读 `/live2d/models/manifest.json` ——
  这是 WebView 的静态资源，**不碰任何原生插件**，完全不依赖 Filesystem
- 用**合并**而不是"空了才兜底"：否则用户一旦自己上传过模型（目录非空），
  内置的那两个就会从列表里消失 —— 那是个更奇怪的 bug。同名时以数据目录里的为准
- 资源 URL 按来源分流：`bundled` 的走 assets（`/live2d/models/`），
  其余的走数据目录。不分流的话，bundled 模型会去数据目录找文件而 404

**验证**：`check-bundled-models.mjs` 23 项，含端到端 ——
用真实代码 + 空数据目录，断言列表仍返回全部内置模型、且都带 `bundled` 标记；
再断言播种成功时同名不重复（总数仍等于清单数）。

## 9. 已知限制 / 待办

- [ ] 表情/动作的**语义键**（`EXPRESSION_MAP`，如 `happy → 星星眼`）是按内置模型的命名定的，换模型后这些键多半对不上，需要按新模型的实际表情名调整（精确名与模糊匹配不受影响）
- [x] ~~嘴部动画是模拟正弦波（非真实音频驱动）~~ → 已改为用 Web Audio AnalyserNode 读真实 TTS 音量驱动 `ParamMouthOpenY`，无音量时自动回落到正弦波
- [ ] **参数绑定情况由模型作者决定，换模型必须重新验证**：悠小喵没有绑定任何眉毛参数（`ParamBrowL/R`、`ParamBrowLY/RY`、`ParamBrowLForm/RForm` 设了都没反应），所以情绪里的"皱眉/挑眉"在这个模型上无法通过参数表达；`ParamSmile`/`ParamEyeLSmile` 同样未绑定。要支持眉毛只能依赖模型的 exp 表情文件，或换模型
- [ ] 参数"是否存在"与"是否绑定"无法在运行时便宜地探测（需要逐个设值做像素 diff），目前靠候选名 + 关键字兜底；遇到命名特殊的模型仍可能解析到未绑定的参数
- [ ] 带 `2` 及以上后缀的角度变体（`ParamAngleX2` / `ParamAngleY2`）语义不可靠：在悠小喵上 `ParamAngleY2` 会推动整个模型而不是转头，因此已排除不驱动。若某个模型把头部的俯仰**只**绑在 `Y2` 上，需要单独适配
- [x] ~~鼠标跟随的幅度（X 轴 ±20°、Y 轴 ±16°）是固定值，未做设置项~~ → 已加「设置 → Live2D → 跟随幅度」：滑块 0-10（×0.0~×1.0）+ 数字框解锁到 100（×10）。头部到量程上限后由身体跟随补足，见 8.10
- [ ] 头部转动幅度受模型自身参数量程锁死（实测两个内置模型都是 ±30，约 ×1.5 到顶）。倍率再往上只能靠身体跟随，而身体参数是否绑定由模型作者决定（悠小喵的 `ParamBodyAngle*` 存在但单参数扫描位移为 0，实际介入幅度有限）
- [ ] `ParamEyeBallX/Y` 在两个内置模型上都**未绑定**（设了没反应），所以"视线跟随"实际不生效。只有模型作者绑定过才有眼球跟随；`diag-param-bind.html` 可扫描
- [ ] Live2D 模型二次发布限制：模型目录已 gitignore，但**用户上传的模型版权需自行注意**
- [ ] 安卓（Capacitor）端 Live2D 未实测（网格手柄交互同样未在安卓 WebView 实测）
- [ ] 上传大 zip（>50MB）内存占用较高（整包读入内存）
- [ ] 尺寸记忆按模型名存储，但不同屏幕尺寸（手机/桌面）共用一份，跨端切换屏幕后比例会按当前屏幕高度钳制

### 日志（见 8.16 / 8.20 / 8.23）

- [x] ~~所有会话混在同一个 `server.log` 里，翻历史费劲~~ → 已改为每次启动一个带时间戳的文件
- [x] ~~日志文件无限堆积~~ → 只保留最近 10 **次启动**（同一次的主日志与追踪日志一起留、一起删）
- [x] ~~行内时间是 UTC，和文件名差 8 小时~~ → 一律本地时间，文件名时刻与行内时刻同源
- [x] ~~只有"调了什么"，没有"问了什么 / 答了什么 / 为什么失败"~~ → 加追踪日志，完整记录对话与上游报错（见 8.20）
- [x] ~~级别字段恒为 `LOG`，不能按级别过滤~~ → 四位级别 `DBUG/INFO/WARN/ERRO/CRIT`，WARN 及以上带版本号
- [x] ~~`CRITICAL` 与 `DEBUG` 只是常量表里的死配置，没有任何产生路径~~ → 包装 `console.debug`、新增 `logCritical()`（见 8.23）
- [x] ~~级别写死在启动期，用户改不了~~ → 设置 → 高级 → 日志，运行时可调且持久化（见 8.23）
- [x] ~~没有来源定位~~ → `[来源:行号]`，取栈里第一条非日志模块的帧
- [x] ~~空行也落盘、多行被压平~~ → 纯空白不落盘；主日志压一行，追踪日志原样保留
- [x] ~~客户端上报截断到 300 字符~~ → 服务端放宽到 2000，前端 `toText()` 同步放宽并把 `Error.stack` 带上
- [ ] **追踪日志含完整对话内容**，是隐私面。启动横幅已提醒，设置里可关闭；但**不会自动过期清理以外的东西** —— 用户若把 `data/logs/` 整个发给别人，等于把聊天记录发出去了
- [ ] **请求头一律不记**（`Authorization` 就在里面）。这是刻意的取舍：代价是排查"认证类"问题时看不到到底发了什么头，只能靠状态码与响应体推断
- [ ] 单文件超 8 MB 会拆 `_2` / `_3`，但**拆分后同一次启动的日志被分到多个文件**，看的时候要连着看
- [ ] **级别过滤只作用于文件，不作用于终端**（刻意的，见 8.23）。想让终端也安静只能重定向输出
- [ ] **改级别只影响之后的记录**，不追溯已有内容；切到 `WARN` 后翻旧文件仍会看到当时的 INFO
- [ ] `SIGTERM` 在 Windows 上无法可靠送达，外部强杀进程时不会有收尾行（`[Core] [INFO] … 收到 SIGTERM`）

### 对话接口 / 本地中转（见 8.13）

- [x] ~~浏览器直连撞 CORS 只报 `Failed to fetch`~~ → 已加 `/api/relay` 本机中转；探测到同源服务即自动启用
- [x] ~~「请求模式」下拉只有两个选项且会锁死 Base URL~~ → 已删除，Base URL 永远可编辑
- [x] ~~错误套在 `detail.error.message` 里抓不到~~ → 已统一由 `extractProviderErrorMessage()` 挖文案
- [ ] **中转只在页面由 `serve.mjs` 提供时生效**，`file://` 直接打开时仍走裸 fetch（CORS 限制还在）
- [ ] **中转目前是非流式转发**（前端本来就等完整回复）。接口已支持逐块透传，上流式时服务端不用改
- [ ] **Node 的 `fetch` 不读 `http_proxy` / `https_proxy`**。只有走系统代理才能访问的服务商会失败并报出具体原因；要支持需显式 `setGlobalDispatcher(new ProxyAgent(...))`，目前未做
- [ ] 中转**没有并发/频率限制**。它挂在鉴权之后、且默认只允许本机与已登录设备使用，暂未加限流
- [ ] 安卓侧 `ByokHttpPlugin` 的 `get` 新增了自定义请求头与 `asText` 参数（原先不支持请求头，导致「获取模型」在 APK 上永远拿不到 Authorization），**需要重新打包 APK 才生效**
- [x] ~~部分网关只接受一条 system 消息，多条连续 system 被拒且报错文案误导~~ → 已加 `mergeLeadingSystemMessages()`，发送前把开头连续的 system 合并为一条（见 8.18）
- [x] ~~客户端日志上报把错误截断到 300 字符~~ → 已放宽到 2000（前后端同步），完整原文进追踪日志；`Error` 改为上报 `stack`（见 8.20）
- [ ] 「获取模型列表失败」在部分服务商上仍是**必然失败**（其 `/v1/models` 无 CORS 头、预检 405，见 8.13），此时手填模型名即可，不影响对话

### Anthropic 格式（见 8.21）

- [x] ~~切到 Anthropic 后必然 400 `max_tokens must be greater than 1024 for manual Claude thinking`~~ → 已给 `max_tokens` 一个 4096 的下限，并处理 `-thinking-<数字>` 指定预算的写法
- [x] ~~下限只修在 `callAnthropicChat` 里，OpenAI 格式调 Claude 仍会踩同一个坑~~ → 判据已提到 `callChatAPI`，三种格式共用（Anthropic 无条件保底，OpenAI 系按模型名后缀保底，见 8.21 第六节）
- [x] ~~400 的文案提到"manual Claude thinking"，而用户根本没开过思考~~ → 提示里点明这是模型名 `-thinking` 后缀导致的，并给出"去掉后缀"这一步
- [ ] **应用内的「思考模式」开关在 Anthropic 格式下是空操作**。它只在 DeepSeek 官方端点下用于换成 `deepseek-reasoner`；要不要支持 Anthropic 原生 `thinking` 参数（以及 `budget_tokens` 怎么给）单独评估，没混进 8.21 那次修复
- [ ] **`-thinking` 后缀会让每一次对话都走扩展思考**（更慢、更贵），而应用不会提示这件事 —— 只有在报 400 时才会说。用户若是照抄中转站的示例模型名，多半不知道自己开了思考
- [ ] Anthropic 的 `temperature` 取值范围是 **0~1**（OpenAI 是 0~2）。目前应用不传 `temperature`（`callChatAPI` 只传 `thinking` / `maxTokens`），所以撞不上；将来若开放这个设置项，Anthropic 分支需要单独钳制
- [x] ~~「获取模型」在 Anthropic 格式下**不可用**（`/v1/models` 不是 Anthropic 的协议）~~ → **这条结论本身就是错的**：Anthropic 有 Models API，返回结构与 OpenAI 同构。真因是 Base URL 的版本段拼错（`/models` vs `/v1/models`），已修，见 8.22
- [x] ~~「获取模型」能拉到列表，但模型名输入框不被填上，一对话仍 404~~ → `populateModelSelect` 从没给 input 赋值（只填了下拉框），已按四种情况处理并补齐 12 项回归检查，见 8.24
- [x] ~~报「data 为空，可能该服务不支持 /models」但服务其实支持~~ → 地址少 `/v1` 时拿回的是 HTML 首页，提示词把人带偏了。现已区分 HTML / 非 JSON / 真空列表 / HTTP 错误四种情况，并统一附上实际请求地址，见 8.25

### Agent 文件操作（见 8.19 / 8.27）

- [x] ~~AI 只会猜 `C:\Users\<用户名>\Desktop`，桌面被移动过就必然失败~~ → 已从注册表读真实位置并写进提示词，另提供 `%DESKTOP%` 等别名
- [x] ~~失败信息只有「目录不存在」，AI 没有纠正线索~~ → 404 文案带真实位置；且允许 AI 在解释失败时**自动重试一次**
- [x] ~~操作成功后 AI 不会自己继续，必须用户手动说"继续"~~ → 成功路径从不回调模型（失败路径会）。已在 `insertAgentResult` 接上自动续跑，带预算 / 停止优先 / 防抖，见 8.27
- [x] ~~写文件永远失败（日志：`目标地址无效：/api/agent/write`）~~ → write 分支误用 `postJsonFromDevice`（那是给外部服务商、会走中转的出口），同源接口被当中转目标。已改同源 fetch 并加全局扫描，见 8.28
- [x] ~~续跑产出的回复不显示，要再发一句话才冒出来~~ → 续跑自己拼的渲染漏了 `removeThinkingMessage()`，"正在想"气泡把回复压在下面。已与 `commitAiMessageOnce` 对齐，见 8.28
- [x] ~~Windows 的 junction（`All Users`、`My Documents`…）被当成文件~~ → `listAgentEntries()` 用 stat 跟随链接归类，并标 `link`
- [ ] **真实位置只解析一次**，改了桌面位置必须重启服务。要做到热更新得给 `/api/agent/roots` 加个强制刷新参数
- [ ] **注册表读取依赖 PowerShell 或 reg.exe**。企业策略把两者都禁掉时只能退回按惯例猜 —— 那时桌面被移动过仍然找不到。也没有缓存到磁盘（重启即重解析）
- [ ] **桌面/文档等位置是按当前用户读的**（`HKCU`）。服务以别的用户身份跑时读到的是那个用户的外壳文件夹
- [ ] 该功能**只在网页版可用**（需要 `serve.mjs`）；APK 里没有 `/api/agent/*`，标签会被判为不可用
- [ ] 别名展开**只认显式写法**（`%DESKTOP%` / `~desktop`），裸词「桌面」不展开 —— 这是刻意的，避免劫持同名目录；代价是 AI 必须被提示词教会用别名
- [ ] 仍**没有删除 / 重命名 / 移动**权限，只有列出、读取、新建
- [ ] **自动续跑上限 8 步是固定值**，长任务（如"整理这个文件夹里的 20 个文件"）会中途停下等用户说一句。
  可考虑改成设置项，或按任务类型动态放宽 —— 但它同时也是防跑飞/防烧额度的闸门，放宽要谨慎

### AI 手机操作（见 8.12 / 8.26）

- [x] ~~AI 好像完全不会操作手机（链路却是通的）~~ → 提示词从没告诉模型它能操作手机（【操作标签】里一个 `手机*` 都没有）。已加 `agentPhoneSkillText()` 按可用性三档注入，见 8.26
- [ ] **Agent 循环仍跑在 WebView 里** —— 应用切后台被系统节流会导致长任务走一半停住。第 4 片要把它搬到前台服务（**这是真机上最容易遇到的坑**）
- [ ] **标签协议还没升级成正规 Tool Calling** —— 现在是文本标签 + 正则解析，没有 JSON Schema 校验
- [ ] `su -c id` 能否拿到 `uid=0` 依 KernelSU / Magisk 版本而异，未逐版本验证
- [ ] 无障碍 `dispatchGesture` 是否被某些 ROM 拦截、`ACTION_SET_TEXT` 在具体输入框里的表现 —— **只能真机确认**
- [ ] **Shizuku 的 UserService 绑定稳定性未在真机验证**（四个后端里最没底的一项）
- [ ] 模块网关（3070）的真实接口路径与字段名未对着真模块跑过（代码做了字段名兼容）
- [ ] 各厂商后台管理下长任务是否被掐断未验证；厂商适配表除小米/OPPO 两条来自 Shizuku 官方文档外，其余是社区经验，**标注为「已知」而非「实测」**
- [ ] 中文输入的实际表现未验证
- [ ] 网页版没有原生设备层，手机操作**完全不可用**（执行时会明确提示用 APK；提示词里也刻意不注入这段能力，免得模型空谈）

### 数据存储与备份（见 8.29）

- [x] ~~所有数据挤在 `data/store.json` 一个文件里，写坏就全丢、人设卡没法单独分享~~ → 已按用途分类落盘（人设卡一卡一文件、聊天记录、记忆各自独立），并自动迁移旧数据，见 8.29
- [x] ~~升级/换机器怕丢数据，却没有备份手段~~ → 已加「我的数据」导出/导入（`/api/data/export`、`/api/data/import`），导出是单个 JSON、导入默认合并模式，见 8.29
- [ ] **APK 侧还没有导入导出**。`data/` 那套接口依赖 `serve.mjs`，而 APK 没有本地服务 ——
      目前「我的数据」整块在 APK 里是隐藏的。要做的话得走 Capacitor Filesystem + 系统文件选择器，
      属于**待办**（用户数据在 APK 里存在应用私有目录，卸载即丢失，这个风险比 Web 版更高）
- [ ] **导入是整体覆盖式合并**（按键覆盖），没有逐项 diff 让用户挑选。备份文件里同一张卡如果本机已改过，
      导入会直接覆盖本机版本 —— 目前只靠一次确认弹窗提示，没有版本对比
- [ ] **导出文件包含 API Key 明文**（`elainachat_open_api_secrets` 与备份一起出去）。
      分享备份给他人前必须自己检查 —— 界面上有提示，但没有"导出时排除密钥"的选项
- [ ] **迁移只在启动时跑一次**。如果服务运行期间手工往 `store.json` 里塞了分类键，不会触发迁移（重启即可）
- [ ] `data/characters/` 的卡片 id 直接用作文件名，限制为 `[A-Za-z0-9_-]{1,80}`。
      非法 id 的卡片会被**跳过**（读）或**重新生成 id**（写）—— 不会丢内容，但 id 会变
- [ ] **`Agent能力说明.md`（已删除）并没有被注入** —— 它只是一份给人看的设计文档（代码里没有任何引用）。真正生效的是 `index.html` 里动态拼的那些 `# Agent Skill：…` 段落；改能力时别只改那份 md

## 10. 常见问题（FAQ）

**Q: 对话发不出去 / 点「获取模型」报 `Failed to fetch`？**
A: 这句是 Chromium 对**网络层失败**的兜底文案，没有状态码也没有原因码 —— 它最常见的含义是
"服务商没有返回 CORS 响应头，浏览器把响应丢掉了"，**不是**地址或 Key 写错。
解决办法：用 `启动.bat` 启动、再从 `http://127.0.0.1:4173` 打开页面，前端会自动走本机中转绕开 CORS
（直接双击 `index.html` 打开时 `file://` 不走中转）。想确认真因，可以在浏览器控制台看 Network 里那条请求是
`CORS error` 还是 `net::ERR_*`。细节见 8.13。

**Q: 报 `Model xxx is not available`？**
A: 地址和 Key 都是通的，只是**模型名不在这个服务上**。点「获取模型」从下拉里挑一个，
或按服务商文档填正确名字。第三方中转站常见这种情况 —— 名字听着像，实际没这个模型。
（「获取模型」现在会把列表里的名字**自动填进输入框**；你自己手填过的值不会被覆盖，见 8.24。）

**Q: 点「获取模型」报「data 为空」或「返回的是网页（HTML）」？**
A: 先看提示里的**实际请求地址**。若是「返回的是网页（HTML）」—— 这是 Base URL
**少了版本段**：OpenAI 兼容的地址惯例上要带 `/v1`（如 `https://api.deepseek.com/v1`），
少了它 `/models` 会打到站点首页、拿回一页 HTML，解析不出模型列表。在地址末尾补 `/v1` 即可。
若确认地址没错、返回的也是 JSON，那就是**该账号下确实没有可用模型**（或服务商把模型下掉了），
这时把模型名手填进输入框照样能用。见 8.25。

**Q: 提示"本地中转无法连接目标：getaddrinfo ENOTFOUND / ECONNREFUSED"？**
A: 这是**中转自己没连上目标**，和上游返回的错误是两回事。`getaddrinfo ENOTFOUND` = 域名解析不了（地址拼错或本机 DNS 有问题）；
`ECONNREFUSED` = 端口拒绝（服务没起或端口不对）；`超过 N 秒没有响应` = 超时。
另外 Node 的 `fetch` **不读 `http_proxy` / `https_proxy`**，如果只有走系统代理才能访问该服务商，中转就会失败。

**Q: 视频通话提示"模型加载失败：Network error"？**
A: 模型文件不在 `web/live2d/models/` 里（被清理或未上传）。重新在设置 → Live2D 上传，或把模型文件夹放进去。

**Q: 上传 zip 后找不到 model3.json？**
A: zip 需包含 `.model3.json` 文件（Cubism 3 模型）。确认 zip 里有，且解压后路径正确。

**Q: 模型列表里全是 `model_mty0qqda_eyr8` 这种名字，分不清哪个是哪个？**
A: 设置 → Live2D，选中模型后点「✏️ 重命名」，起个能认出来的名字（例如「伊蕾娜 · 白裙」）。这会**直接把模型的文件夹改名**（中文、空格都可以），视频通话里的下拉会一起变，该模型的缩放/水印设置也会自动迁移；重名会自动加 `(2)`、`(3)`。另外，**新上传的模型现在直接用 zip 里的文件夹名做目录名**，不再是一串 `model_xxx` 了。

**Q: 点重命名提示 `Unexpected token 'N', "Not found" is not valid JSON`？**
A: 这是**服务端还在跑旧代码**：`/api/live2d/rename` 这个接口不存在，请求落到静态文件处理器返回了纯文本 `Not found`，前端解析 JSON 就炸了。到 cmd 窗口按 `Ctrl+C`，重新双击「启动.bat」即可（`serve.mjs` 的改动必须重启服务才生效；`index.html` / `live2d-video.js` 刷新浏览器就行）。新版前端已能识别这种情况并直接给出这条提示。

**Q: 背景/大小没生效？**
A: 强刷（Ctrl+F5）清缓存。背景在设置 → Live2D 里选；大小/位置在视频通话底部点「📐 调整」打开调整框，拖手柄缩放、框内拖动移动（角手柄等比，Shift 自由），−/＋ 可等比微调。

**Q: 浏览器语音识别用不了/报错？**
A: 打开 `http://localhost:4173/diag-asr.html` 语音识别诊断页，先点「测试麦克风」再点「开始语音识别测试」，页面会显示：环境检测（Chromium 内核/安全上下文/API）、麦克风权限结果、实时转写、事件日志和诊断结论（含解决建议）。把结果反馈给开发者即可快速定位。

**Q: 模型水印怎么隐藏（悠小喵按 1、泠按 0）？**
A: 这类模型自带"水印开关"表情（exp 目录里的 `水印开关.exp3.json` 等）。打开视频通话后，若模型有水印开关，按键盘 **1 / 0** 即可切换水印隐藏/显示，也可以直接让 AI「把水印隐藏掉」（它会用 `[操作:隐藏水印]` 触发）。**通话界面没有水印按钮**，只能靠键盘或 AI 触发。开关状态按模型记忆。任意模型只要 exp 目录含"水印/watermark"命名的表情都会自动支持，无需改代码；若某个模型水印机制特殊（无开关表情），需要按模型单独适配。

**Q: 换了新模型后鼠标不动了 / 模型不跟随鼠标？**
A: 先确认「设置 → Live2D → 鼠标跟随」是开着的——关闭时通话界面会显示「鼠标跟随已关闭 · 点此开启」提示条，点一下即可开启。如果开关是开的却仍不动，用 `diag-param-bind.html` 扫一下该模型的 `ParamAngleX` / `ParamAngleY` 是否"已绑定"（模型作者可能没把标准名绑到绘制上）。历史上还有一个已修复的 bug：关闭通话再重新打开会丢监听器（见 8.8）。

**Q: 鼠标跟随幅度太小/太大，怎么调？**
A: 设置 → Live2D → 跟随幅度。滑块 `0-10`（`10 = ×1.0` 原始幅度）先在这个范围里找合适档位；数字框可以填更大数字解锁更高倍率（最大 `100 = ×10`）。注意**头部转动幅度受模型自身参数上限限制**（多数模型是 ±30，约 ×1.5 就到顶），再往上由**身体跟随**补足——×1.0 时身体不动，×3.0 时身体转到该模型上限。所以填到 ×3.0 之后通常就不再变化了。面板上的提示会直接告诉你该模型的头部上限（如「头部上限 ×1.5」）。

**Q: AI 回复里的 `[表情:xxx]` 没触发模型表情？**
A: 检查模型 exp 目录是否有对应表情文件；`EXPRESSION_MAP` 是否匹配（悠小喵：happy→星星眼、sad→哭哭、blush→脸红…）。

**Q: 打开页面提示"请求的 Host 不在允许列表内"？**
A: 这是防 DNS rebinding 的白名单校验（见 8.9）。用本机/局域网 IP 或 `localhost` 访问即可；确实要用主机名或域名访问时，设置环境变量 `ALLOWED_HOSTS=你的域名` 后重启服务。

**Q: 端口被占用，服务起不来？**
A: 提示里会写清楚。换端口启动即可：`set PORT=4174 && node web/serve.mjs`。

**Q: 上传模型后提示"已跳过 N 个不允许的文件"？**
A: 那些是 `.html/.js/.svg/.exe` 之类的可执行类型，出于安全不会落盘（否则上传的文件会以同源身份在浏览器里执行）。正常的 Live2D 模型文件（`.model3.json` / `.moc3` / 纹理 / `.exp3.json` / `.motion3.json`）不受影响。

**Q: 用「允许操作电脑」模式时为什么弹确认框？**
A: 该模式允许 AI 写任意路径，之前是完全无声执行的。现在每个新路径写入前会弹一次确认（同一路径只问一次）。想彻底关闭就把权限改回「限制模式」。

**Q: AI 读写完文件后就不动了，非要我说"继续"？**
A: 这是已修的问题（见 8.27）：成功路径以前不会回调模型，所以 AI 看不到自己刚做完的事。
现在操作完成会自动续跑。两点说明：
- **连续自动执行上限 8 步**，超过会停下并告诉你"先停在这里" —— 这是防止跑飞和烧额度的闸门，
  不是坏了。长任务中途停下时，说一句"继续"就会再给一批预算。
- 若它仍然不动，看对话里有没有「【AI 操作结果】」这条消息：
  **没有**说明操作根本没执行（多半是权限模式或路径问题，AI 会解释原因）；
  **有**但 AI 不接着做，可能是模型本身没有遵守续跑指令（换个模型试试）。

**Q: 让 AI 操作手机，它完全不动 / 说做不到？**
A: 按顺序检查三件事：
1. **是不是在用 APK** —— 网页版没有原生设备层，手机操作完全不可用（设置里那块也不会显示）。
2. **设置 → 高级 → 手机操作** 里有没有点「选择实现方式」选一种，且「允许 AI 操作手机」已勾选。
   两项缺任何一项，AI 都会**如实告诉你还没启用并指出去哪开**（它不会假装操作过）。
3. 选好之后，还要确认那一种方式本身可用：无障碍要去系统设置里开启本应用的服务；
   Shizuku 要启动服务并授权；Root 要授权成功。点卡片右下角的按钮会真跳系统页/弹授权框，
   并把原生返回的文字显示出来 —— 那句话就是下一步该做什么的依据。

如果三项都对、AI 仍然不点击，让它先「[操作:手机查看界面]」读一次控件树 ——
AI 是**先看再点**的，读不到界面时它会说明原因（这比盲点坐标更值得信任）。见 8.26。

## 11. 二次开发入口

- **Live2D 模块**：`web/live2d-video.js`（渲染、驱动、上传、背景、大小）
- **服务端**：`web/serve.mjs`（上传/删除 API、zip 解压）
- **设置界面**：`web/index.html` 的 `tab-live2d` / `tab-vision` 区块
- **日志系统**：`web/serve.mjs` 的「===== 日志系统 =====」区块（格式、级别、双 sink、轮转、脱敏）；
  级别与追踪开关的入口在 `web/index.html` 的 `refreshLogSettings` / `applyLogSettings`（设置 → 高级 → 日志）
- **AI 回复驱动**：`web/index.html` 的 `handleUserInput`（speakStart/speakEnd/drive）
- **视觉转述**：`web/index.html` 的 `callAI` / `describeImageWithVision`
- **Agent 自动续跑（文件/手机操作通用）**：`web/index.html` 的 `insertAgentResult()`（结果收口 + 触发续跑）/ `scheduleAgentContinue()`（防抖合并）/ `continueAgentLoop()`（主链路）/ `resetAgentContinueBudget()`（按用户请求重置预算）—— 见 8.27
- **手机操作（前端）**：`web/index.html` 的 `AGENT_PHONE_OPS`（标签解析）/ `AGENT_RISK`（风险分级）/ `runAgentPhoneOperation`（主链路）/ `AGENT_BACKENDS` + `AGENT_BACKEND_SETUP`（四种实现方式）/ `window.agentRuntime`（停止与授权）/ `deviceBridge()`（原生桥接）/ **`agentPhoneSkillText()`（注入系统提示词的能力声明 —— 漏了它 AI 就不会操作手机，见 8.26）**
- **手机操作（原生）**：`android-app/android/app/src/main/java/com/elainachat/opensource/` 下的 `ElainaShellPlugin` / `ShellProcess` / `ShellBackends` / `ShizukuShell` / `UserService` / `ElainaAccessibilityService` / `PhoneCommand` / `DeviceArtifacts`
- **手机操作（设计）**：见本文档「AI 手机操作」章节（原独立设计文档已并入）

### 构建流程（改前端后必须走这条）

```bash
npm run check        # 内联脚本语法 + DOM 引用完整性（改完先跑这个，比打开浏览器快）
npm run sync:web     # web/ → android-app/www（含内置 Live2D 模型 + manifest.json）
npm run build:apk    # cap sync（www/ → assets/public）+ gradlew → app-debug.apk
```

> `android-app/android/app/src/main/assets/public/` 是 **`cap sync` 的产物**，`sync:web` 不写它。
> 只跑 `sync:web` 时它仍是旧内容，必须跑 `build:apk` 才刷新 —— 排查"APK 里还是旧代码"时先确认这一点。

`web/` 是**唯一编辑入口**，不要直接改 `android-app` 里的副本。只改 `serve.mjs` 时必须重启服务才生效；`index.html` / `live2d-video.js` 刷新浏览器即可。
